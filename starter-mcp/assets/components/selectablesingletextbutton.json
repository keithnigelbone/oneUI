{
  "schemaVersion": "1.0.0",
  "componentName": "SelectableSingleTextButton",
  "generatedAt": "2026-01-01T00:00:00.000Z",
  "machineReadable": true,
  "intentAndPurpose": {
    "intent": "Text-only toggle button for binary selection — no start/end slots, simpler than SelectableButton.",
    "taskContexts": [
      "text-filter-toggle",
      "segmented-text-control",
      "simple-option-selection"
    ],
    "sentiments": [
      "neutral",
      "positive"
    ]
  },
  "compositionRules": {
    "requires": [
      "children (text label — required)"
    ],
    "allows": [
      "condensed mode",
      "fullWidth",
      "loading state"
    ],
    "forbids": [
      "icon slots (use SelectableButton instead)",
      "missing text content"
    ]
  },
  "variantLogic": {
    "rules": [
      {
        "name": "high attention",
        "useWhen": [
          "selected state needs bold fill emphasis"
        ]
      },
      {
        "name": "medium attention",
        "useWhen": [
          "selected state with subtle tinted fill"
        ]
      },
      {
        "name": "low attention",
        "useWhen": [
          "selected state with ghost appearance and accent border"
        ]
      }
    ]
  },
  "relationshipsAndDependencies": {
    "related": [
      "SelectableButton",
      "SelectableIconButton",
      "Toggle",
      "ToggleGroup"
    ],
    "groupsWith": [
      "ToggleGroup"
    ]
  },
  "contextSignals": {
    "density": [
      "compact",
      "default",
      "open"
    ],
    "modality": [
      "desktop",
      "mobile"
    ],
    "brand": [
      "theme-scope aware",
      "recipe-driven"
    ],
    "mode": [
      "light",
      "dark",
      "surface-context"
    ]
  },
  "observabilityHooks": {
    "track": [
      "select",
      "deselect"
    ],
    "health": [
      "a11y_violations",
      "adoption_rate"
    ]
  },
  "props": [
    {
      "name": "children",
      "type": "ReactNode",
      "required": true,
      "description": "Text label content — max 2 characters (e.g. \"Ag\", \"En\", \"A\", \"12\"). This component renders as a circular button; longer text breaks the shape. Text exceeding 2 characters will be truncated in development with a warning."
    },
    {
      "name": "appearance",
      "type": "SelectableSingleTextButtonAppearance",
      "required": false,
      "description": "Multi-accent appearance role. 'auto' resolves to 'primary'."
    },
    {
      "name": "aria-label",
      "type": "string",
      "required": false,
      "description": "Accessible label for screen readers (optional — children text is visible)"
    },
    {
      "name": "attention",
      "type": "SelectableSingleTextButtonAttention",
      "required": false,
      "description": "Attention level — determines the visual prominence of the SELECTED state. high → bold fill; medium → subtle fill; low → ghost with accent border. The UNSELECTED state is always muted ghost regardless of attention. Default: 'high'."
    },
    {
      "name": "className",
      "type": "string",
      "required": false,
      "description": "Additional class name"
    },
    {
      "name": "condensed",
      "type": "boolean",
      "required": false,
      "description": "Condensed mode: reduces height and horizontal padding while keeping the same typography."
    },
    {
      "name": "defaultSelected",
      "type": "boolean",
      "required": false,
      "description": "Default selected state (uncontrolled)."
    },
    {
      "name": "disabled",
      "type": "boolean",
      "required": false,
      "description": "Whether the button is disabled."
    },
    {
      "name": "fullWidth",
      "type": "boolean",
      "required": false,
      "description": "Stretch to fill container width."
    },
    {
      "name": "loading",
      "type": "boolean",
      "required": false,
      "description": "Loading state — shows circular progress indicator replacing the text label. Disables interaction while loading."
    },
    {
      "name": "onSelectedChange",
      "type": "(selected: boolean) => void",
      "required": false,
      "description": "Called when selected state changes."
    },
    {
      "name": "selected",
      "type": "boolean",
      "required": false,
      "description": "Selected state (controlled). Maps to Toggle pressed."
    },
    {
      "name": "size",
      "type": "SelectableSingleTextButtonSize",
      "required": false,
      "description": "Button size. Default: 'm'. S/M/L only (no XS)."
    },
    {
      "name": "style",
      "type": "CSSProperties",
      "required": false,
      "description": "Inline styles"
    },
    {
      "name": "value",
      "type": "string",
      "required": false,
      "description": "Value for use within ToggleGroup."
    }
  ],
  "slots": [],
  "propsSchema": {
    "$id": "jds.docs.selectable-single-text-button",
    "type": "object",
    "properties": {
      "children": {
        "description": "Text label content — max 2 characters (e.g. \"Ag\", \"En\", \"A\", \"12\"). This component renders as a circular button; longer text breaks the shape. Text exceeding 2 characters will be truncated in development with a warning."
      },
      "appearance": {
        "enum": [
          "auto",
          "primary",
          "secondary",
          "neutral",
          "sparkle",
          "brand-bg",
          "positive",
          "negative",
          "warning",
          "informative"
        ],
        "description": "Multi-accent appearance role. 'auto' resolves to 'primary'."
      },
      "aria-label": {
        "type": "string",
        "description": "Accessible label for screen readers (optional — children text is visible)"
      },
      "attention": {
        "enum": [
          "high",
          "medium",
          "low"
        ],
        "description": "Attention level — determines the visual prominence of the SELECTED state. high → bold fill; medium → subtle fill; low → ghost with accent border. The UNSELECTED state is always muted ghost regardless of attention. Default: 'high'."
      },
      "className": {
        "type": "string",
        "description": "Additional class name"
      },
      "condensed": {
        "type": "boolean",
        "description": "Condensed mode: reduces height and horizontal padding while keeping the same typography."
      },
      "defaultSelected": {
        "type": "boolean",
        "description": "Default selected state (uncontrolled)."
      },
      "disabled": {
        "type": "boolean",
        "description": "Whether the button is disabled."
      },
      "fullWidth": {
        "type": "boolean",
        "description": "Stretch to fill container width."
      },
      "loading": {
        "type": "boolean",
        "description": "Loading state — shows circular progress indicator replacing the text label. Disables interaction while loading."
      },
      "onSelectedChange": {
        "description": "Called when selected state changes."
      },
      "selected": {
        "type": "boolean",
        "description": "Selected state (controlled). Maps to Toggle pressed."
      },
      "size": {
        "enum": [
          "s",
          "m",
          "l"
        ],
        "description": "Button size. Default: 'm'. S/M/L only (no XS)."
      },
      "style": {
        "description": "Inline styles"
      },
      "value": {
        "type": "string",
        "description": "Value for use within ToggleGroup."
      }
    },
    "required": [
      "children"
    ]
  },
  "codeSnippets": [
    {
      "id": "selectablesingletextbutton-basic",
      "title": "Basic Usage",
      "language": "tsx",
      "code": "import { SelectableSingleTextButton } from '@oneui/ui';\n\n<SelectableSingleTextButton>Option</SelectableSingleTextButton>"
    },
    {
      "id": "selectablesingletextbutton-controlled",
      "title": "Controlled",
      "language": "tsx",
      "code": "<SelectableSingleTextButton selected={isActive} onSelectedChange={setIsActive}>Active</SelectableSingleTextButton>"
    },
    {
      "id": "selectable-single-text-button-recipe-decisions",
      "title": "Recipe Decisions",
      "language": "json",
      "code": "{\n  \"component\": \"SelectableSingleTextButton\",\n  \"decisions\": [\n    \"Shape\"\n  ]\n}"
    }
  ],
  "tags": [
    "machine-readable",
    "generated",
    "selectable-single-text-button"
  ],
  "importPath": "@jds4/oneui-react"
}
