---
phase: 03.1-daytona-zero-egress-render-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/experience-builder-preview/src/daytona/image.ts
  - packages/experience-builder-preview/src/daytona/image.test.ts
  - packages/experience-builder-preview/src/daytona/harness/capture.js
  - packages/experience-builder-preview/src/daytona/harness/serve.js
  - packages/experience-builder-preview/src/daytona/harness/probe.js
autonomous: true
requirements: [PREV-01, PREV-04]
must_haves:
  truths:
    - "A custom Daytona image recipe bakes Playwright 1.59.1 + Chromium at BUILD time (network OK), so the runtime sandbox needs no install under networkBlockAll (PREV-01)"
    - "capture.js renders file://asset.html in-box with Playwright, settles fonts+transitions, and prints a base64 PNG to stdout (PREV-04)"
    - "serve.js serves the asset over a static server on the preview port for the live iframe"
    - "probe.js attempts an outbound fetch and exits 0 ONLY when the fetch is refused (the PREV-01 zero-egress proof)"
    - "@daytonaio/sdk is NOT imported by the harness scripts; image.ts may import only the Image builder type"
  artifacts:
    - path: "packages/experience-builder-preview/src/daytona/image.ts"
      provides: "buildDaytonaImage(): Image.base(playwright:v1.59.1-jammy).runCommands(npm i playwright@1.59.1).addLocalDir(harness) + named-snapshot constant"
      min_lines: 30
    - path: "packages/experience-builder-preview/src/daytona/harness/capture.js"
      provides: "in-box Playwright screenshot of file://asset.html -> base64 PNG on stdout"
      contains: "require('playwright')"
    - path: "packages/experience-builder-preview/src/daytona/harness/serve.js"
      provides: "in-box static server on the preview port for the live iframe"
      contains: "createServer"
    - path: "packages/experience-builder-preview/src/daytona/harness/probe.js"
      provides: "zero-egress probe: exit 0 iff outbound fetch refused"
      contains: "process.exit"
  key_links:
    - from: "packages/experience-builder-preview/src/daytona/image.ts"
      to: "mcr.microsoft.com/playwright:v1.59.1-jammy"
      via: "Image.base(...).runCommands('npm i ... playwright@1.59.1').addLocalDir(harness)"
      pattern: "v1\\.59\\.1-jammy"
    - from: "packages/experience-builder-preview/src/daytona/harness/capture.js"
      to: "file:///home/pwuser/preview/asset.html"
      via: "page.goto(..., { waitUntil: 'load' })"
      pattern: "waitUntil.*load"
---

<objective>
Provide the runtime environment + in-box scripts the Daytona path needs: a custom
image recipe that bakes Playwright + Chromium at BUILD time (when network is allowed)
so the RUNTIME sandbox runs `networkBlockAll: true` with no install (PREV-01), plus
the three harness scripts that run inside the box — `capture.js` (per-profile
screenshot → base64 PNG on stdout, PREV-04), `serve.js` (static server on the preview
port for the live iframe), and `probe.js` (the PREV-01 zero-egress proof used at the
Plan 05 human-verify checkpoint).

This fixes CONTEXT root-cause findings #2 (capture.js never created/uploaded), #3
(nothing serves the preview port), and #4 (default image has no Node/Playwright/Chromium).

The image MUST pin Playwright to the repo's resolved 1.59.1 and use the matching
`v1.59.1-jammy` base (RESEARCH Pitfall 1: version skew = "Executable doesn't exist"
with no network to recover). `capture.js` mirrors `playwrightRenderer.ts`'s settle
loop but uses `waitUntil:'load'` for `file://` (RESEARCH Pitfall 3: `networkidle` hangs
under zero-egress).

Purpose: the executable + the box to run it in.
Output: `daytona/image.ts` (+ named-snapshot constant) and `daytona/harness/{capture,serve,probe}.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-daytona-zero-egress-render-pipeline/03.1-CONTEXT.md
@.planning/phases/03.1-daytona-zero-egress-render-pipeline/03.1-RESEARCH.md

# The settle loop capture.js mirrors (font/transition settle, per-profile framing):
@apps/platform/src/lib/playwrightRenderer.ts
# Verified SDK Image builder API (read-only):
@node_modules/@daytonaio/sdk/cjs/Image.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the in-box harness scripts (capture.js, serve.js, probe.js)</name>
  <files>packages/experience-builder-preview/src/daytona/harness/capture.js, packages/experience-builder-preview/src/daytona/harness/serve.js, packages/experience-builder-preview/src/daytona/harness/probe.js</files>
  <action>Create three CommonJS scripts that run under the baked Node 24 + `npm i playwright@1.59.1` (NO @daytonaio/sdk import — these run inside the box). `capture.js`: parse `--width`/`--height`/`--format=png-base64` argv (mirror DaytonaExecutor.buildCaptureCommand's existing arg shape), `require('playwright')` chromium, `newContext({ viewport, deviceScaleFactor:2 })`, `page.goto('file:///home/pwuser/preview/asset.html', { waitUntil:'load' })` (NOT networkidle — RESEARCH Pitfall 3), double-`requestAnimationFrame` settle + `document.fonts.ready` (mirror playwrightRenderer.ts:194-201), `page.screenshot({ type:'png', fullPage:false })`, then `process.stdout.write(png.toString('base64'))` so ONLY image bytes cross the boundary; `catch -> process.exit(1)`. `serve.js`: a tiny `http.createServer` static server binding the preview port (default 3000, read from `--port`/`PORT`) that serves `/home/pwuser/preview/asset.html` at `/` with correct content-type and no directory traversal — for the live iframe (PREV-02). `probe.js`: read a URL from argv, `fetch(url)` then on SUCCESS `console.error('EGRESS-LEAK')` + `process.exit(2)`, on REJECTION `console.log('EGRESS-BLOCKED: '+e.message)` + `process.exit(0)` (RESEARCH § Zero-egress Proof). Keep all three credential-free; none receives or logs DAYTONA_API_KEY.</action>
  <verify>
    <automated>node --check packages/experience-builder-preview/src/daytona/harness/capture.js && node --check packages/experience-builder-preview/src/daytona/harness/serve.js && node --check packages/experience-builder-preview/src/daytona/harness/probe.js && grep -L "@daytonaio/sdk" packages/experience-builder-preview/src/daytona/harness/*.js | wc -l | grep -q 3 && echo HARNESS-CLEAN</automated>
  </verify>
  <done>Three syntactically-valid CJS scripts exist; capture.js uses waitUntil:'load' + base64-stdout, serve.js binds the preview port, probe.js exits 0 only on refusal; none import the SDK.</done>
</task>

<task type="auto">
  <name>Task 2: buildDaytonaImage() recipe + named snapshot constant + version-pin test</name>
  <files>packages/experience-builder-preview/src/daytona/image.ts, packages/experience-builder-preview/src/daytona/image.test.ts</files>
  <action>Create `image.ts` exporting `PLAYWRIGHT_VERSION = '1.59.1'`, `DAYTONA_IMAGE_BASE = 'mcr.microsoft.com/playwright:v1.59.1-jammy'`, `PREVIEW_SNAPSHOT_NAME = 'oneui-preview-v1'`, and `buildDaytonaImage(): Image` (import `Image` as a TYPE-only / value import from `@daytonaio/sdk` — note: this is the ONE allowed exception alongside DaytonaExecutor.ts; if the CI import-guard forbids any SDK import outside DaytonaExecutor.ts, instead export a pure recipe descriptor and let DaytonaExecutor.ts construct the Image — decide based on the existing guard, document the choice in the SUMMARY). The recipe: `Image.base(DAYTONA_IMAGE_BASE).runCommands('npm i -g playwright@'+PLAYWRIGHT_VERSION).addLocalDir(<harness dir>, '/home/pwuser/preview').workdir('/home/pwuser')` (RESEARCH Pattern 3). Add `image.test.ts`: assert `DAYTONA_IMAGE_BASE` ends with `v${PLAYWRIGHT_VERSION}-jammy` and equals the repo's resolved playwright version (read `node_modules/playwright/package.json` version and assert match — RESEARCH Pitfall 1), and that the harness dir referenced by addLocalDir exists and contains capture.js/serve.js/probe.js. Document in code comments that the named snapshot is registered ONCE (CI/setup) and reused via `create({ snapshot })` (RESEARCH Pattern 3 / Pitfall 2), with `onSnapshotCreateLogs` surfaced by the caller (Plan 04).</action>
  <verify>
    <automated>cd packages/experience-builder-preview && pnpm vitest run src/daytona/image.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <done>image.ts pins playwright 1.59.1 to the v1.59.1-jammy base, bakes the harness dir, exports the named-snapshot constant; the version-match test passes; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time (network OK) → runtime (networkBlockAll) | Playwright/Chromium baked at build; runtime has zero egress |
| harness stdout → Lab server | only base64 PNG bytes (capture) / exit codes (probe) cross the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-031-03 | Tampering | base image + npm install at build | mitigate | Pin base tag `v1.59.1-jammy` + `playwright@1.59.1`; build-time network only, runtime blocked (PREV-01). Image built AMD64 server-side via `Image.base(...)` not a local arm64 Dockerfile (RESEARCH Pitfall 6). |
| T-031-04 | Information disclosure | capture.js / serve.js output | mitigate | capture.js writes ONLY base64 PNG; serve.js serves only the asset file (no traversal); neither reads DAYTONA_API_KEY. |
| T-031-05 | Spoofing | probe.js false-pass | mitigate | probe exits 0 ONLY on fetch rejection; a successful fetch exits 2 (EGRESS-LEAK) so the Plan 05 checkpoint cannot be fooled. |
| T-031-SC | Tampering | npm install in image | mitigate | `playwright@1.59.1` already pinned/resolved in repo (no new package choice); image install runs at build-time. New host installs gated by Plan 05 checkpoint. |
</threat_model>

<verification>
- `node --check` passes for all three harness scripts.
- `pnpm --filter @oneui/experience-builder-preview test` green (image version-pin test passes).
- `grep -rn "@daytonaio/sdk" packages/experience-builder-preview/src/daytona/harness/` returns nothing.
- Image base tag string equals `v<resolved-playwright-version>-jammy`.
</verification>

<success_criteria>
- A version-matched custom image recipe bakes Playwright+Chromium at build time so the runtime sandbox holds `networkBlockAll` (PREV-01) and can screenshot (PREV-04).
- In-box harness scripts exist for capture (screenshots), serve (live iframe), and probe (zero-egress proof); none imports the SDK.
</success_criteria>

<output>
Create `.planning/phases/03.1-daytona-zero-egress-render-pipeline/03.1-02-SUMMARY.md` when done.
</output>
