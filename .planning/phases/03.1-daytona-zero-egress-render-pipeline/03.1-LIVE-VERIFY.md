---
status: verified
phase: 03.1-daytona-zero-egress-render-pipeline
plan: 05
requirements: [PREV-01, PREV-02, PREV-04]
started: 2026-06-02
updated: 2026-06-02
---

> **2026-06-02 — Attempt 1 BLOCKED (gap found, fix applied).** The first live run
> failed all four checks via a single root cause: the `/api/experience-lab/run`
> route returned **500** because Next/Turbopack tried to bundle `esbuild`'s native
> binary (`@esbuild/darwin-arm64/bin/esbuild` → "invalid utf-8 sequence") pulled in
> by `assembleAsset.ts`. Import trace:
> `route.ts → DaytonaExecutor.ts → assembleAsset.ts → esbuild/lib/main.js`.
> **Fix (attempt 1 — config):** added `serverExternalPackages: ['esbuild',
> '@daytonaio/sdk']` to `apps/platform/next.config.js`. **Insufficient alone** —
> Turbopack still traced esbuild's binary through the workspace-package import and
> 500'd again after restart.
>
> **Fix (attempt 2 — source, the real fix):** removed the static
> `import { build } from 'esbuild'` in `assembleAsset.ts` and load esbuild at
> runtime via the existing `createRequire` (`require_('esbuild')`). Turbopack does
> not trace `createRequire` requires (same reason `require_.resolve('react/...')`
> was always fine), so esbuild's native binary never enters the route's module
> graph. `serverExternalPackages` kept as defence-in-depth. Preview suite still
> 27/27; package typechecks clean.
>
> **2026-06-02 — Attempt 2 result: esbuild fix CONFIRMED, but Daytona path still
> not exercised (env/deploy setup, not code).**
> - ✅ `POST /api/experience-lab/run 200` — no more esbuild 500; full pipeline ran
>   (intent→…→generate-ir valid→compile→validate passed→preview).
> - ❌ The executor that ran was the **LabAstExecutor MVP**, not Daytona — the
>   emitted `previewState.url` was `/internal/render-ast?token=…` with
>   `sameOrigin: true`. Cause: `EXPERIENCE_LAB_PREVIEW_EXECUTOR` is **absent** from
>   `apps/platform/.env.local` (only `DAYTONA_API_KEY` is set; key-presence alone
>   does not flip the gate). The AST path renders inside the Next app → loads the
>   app shell, tiny cards, and `AST not found or expired` (token TTL lapsed during
>   the 26s render). These are MVP-path artefacts, NOT the Daytona renderer.
> - ❌ `persistArtifact` rejected the write: `extra field 'evaluation' …`. NOT a
>   code bug — the mutation source (experienceRuns.ts, commit 038fdeab) already
>   accepts `evaluation`/`previewState`/`variantGroupId`/`thumbnail`/`originRunId`;
>   the **deployed** Convex functions are stale because no `convex dev` is running.
>
> **To exercise the Daytona path (attempt 3):**
> 1. Start `npx convex dev` (pushes the up-to-date `persistArtifact` validator).
> 2. Add `EXPERIENCE_LAB_PREVIEW_EXECUTOR=daytona` to `apps/platform/.env.local`.
> 3. Restart `pnpm dev`, register the snapshot, re-run the four checks below.
>
> **2026-06-02 — Attempt 3: Daytona path REACHED, preview failed → missing
> snapshot-registration tooling (gap found + built).**
> - ✅ With `EXPERIENCE_LAB_PREVIEW_EXECUTOR=daytona`, a concrete prompt produced a
>   valid IR → compile → `Validation passed` → `preview started` → and the
>   **DaytonaExecutor was invoked** (the polarity fix 03.1-03 held: the failure
>   surfaced as `Run completed: error`, NOT `gap`).
> - ❌ `preview FAILED` in ~1s — far too fast for a real snapshot→sandbox→Playwright
>   render. Root cause: `DaytonaExecutor.render` defaults to
>   `create({ snapshot: PREVIEW_SNAPSHOT_NAME })`, but **the named snapshot
>   `oneui-preview-v1` was never registered**. `image.ts` documents a "one-time
>   CI/setup step via the daytona.snapshot service" — but that tooling did not
>   exist (no script; `route.ts` uses `new DaytonaExecutor()`, read-only). Genuine
>   missing deliverable.
> - 🔧 **Built `scripts/register-snapshot.ts`** (`pnpm --filter
>   @oneui/experience-builder-preview register-snapshot`) — registers
>   `oneui-preview-v1` from `buildDaytonaImage()` via `daytona.snapshot.create`,
>   idempotent, `--dry-run`/`--force` flags. Dry-run verified.
>
> **To exercise the Daytona path (attempt 4):**
> 1. `pnpm --filter @oneui/experience-builder-preview register-snapshot`
>    (one-time cold image build on Daytona — several minutes).
> 2. Re-run the Lab with a concrete prompt → preview now reuses the snapshot.
> 3. Record the four checks below.
>
> **2026-06-02 — Snapshot registered ✓.** After an env-key typo fix
> (`ddtn_…`→`dtn_…`; "Invalid credentials" → authenticates) and confirming the
> rotated key has create scope, `register-snapshot` ran the cold build and
> `Created snapshot oneui-preview-v1 (active)`. `snapshot.list` now shows it
> (total 14). The instant-fail cause (snapshot-not-found) is resolved. Next Lab
> run should reach a real Daytona render — record the four checks below.

# 03.1 — Daytona Live Verification Evidence

The Daytona zero-egress render path (Plans 01–04) is fully implemented and all
offline tests pass. This document records the **live** validation against a REAL
Daytona account — the one thing that cannot run in CI (no credentials).

## Prerequisites

- A real Daytona account.
- Local env (NOT committed to a fork's git — use `.env.local`):
  - `DAYTONA_API_KEY=<your key>`
  - `EXPERIENCE_LAB_PREVIEW_EXECUTOR=daytona`
- The named snapshot from Plan 02 (`PREVIEW_SNAPSHOT_NAME`) registered once from
  `buildDaytonaImage()`.

---

> **2026-06-02 — Attempt 4: final root cause fixed; all four checks PASS on the
> real account.** After the snapshot existed, `preview` still failed in ~1s. A raw
> in-box reproduction (`scripts/render-diagnostic.ts` / `probe-diagnostic.ts`)
> captured the hidden stderr: `Error: Cannot find module 'playwright'` from
> `/home/pwuser/preview/capture.js`. Cause: `image.ts` installs Playwright with
> `npm i -g` (global), but `capture.js` does `require('playwright')`, and Node's
> `require` does not search the global modules dir. **Fix:** prefix the in-box
> capture command with `NODE_PATH="$(npm root -g)"` (DaytonaExecutor.buildCaptureCommand)
> — resolves against the existing snapshot, no rebuild. Verified end-to-end below.

## Check 1 — Render + screenshots in a network-blocked sandbox (PREV-04)

- **Command run:** `pnpm --filter @oneui/experience-builder-preview render-diagnostic`
  (calls `DaytonaExecutor.render()` end-to-end against the live account).
- **Expected:** Per-profile screenshots return as PNG buffers.
- **Observed:** `render OK in 3218ms · rendered: true · screenshots: [desktop:28190B]`
  — a real 28 KB PNG produced by in-box Playwright in a `networkBlockAll` sandbox.
- **Result:** [x] PASS

## Check 2 — Live in-box iframe loads from the signed preview URL (PREV-02)

- **Command run:** same render-diagnostic run (serve.js + `getSignedPreviewUrl`).
- **Expected:** A signed preview URL is minted from the in-box server.
- **Observed:** `previewState.url: https://3000-qo2lpuohynjjthar.daytonaproxy01.eu`
  — an external Daytona-proxy signed URL (NOT `/internal/render-ast`), `sandboxId`
  returned for TTL teardown. (Final in-Lab visual confirmation of the iframe in the
  canvas card is the remaining human sign-off.)
- **Result:** [x] PASS (URL minted; in-Lab visual = final human confirm)

## Check 3 — Zero-egress probe: outbound fetch to Convex REFUSED (PREV-01 / 03-HUMAN-UAT item 3)

- **Command run:** `pnpm --filter @oneui/experience-builder-preview probe-diagnostic`
  → `node preview/probe.js https://groovy-spider-923.eu-west-1.convex.cloud` inside
  a `networkBlockAll: true` sandbox from `oneui-preview-v1`.
- **Expected:** `exitCode === 0` and stdout `EGRESS-BLOCKED: ...`.
- **Observed:** `EGRESS-BLOCKED: fetch failed` / `PROBE_EXIT=0` — outbound fetch to
  the Convex deployment was refused. (Closes 03-HUMAN-UAT item 3, deferred in Phase 3.)
- **Result:** [x] PASS

## Check 4 — Named snapshot reused across runs without rebuild (RESEARCH Assumption A4)

- **Command run:** repeated render-diagnostic / probe-diagnostic runs.
- **Expected:** `create({ snapshot })` reuses the snapshot WITHOUT a cold rebuild.
- **Observed:** each `create({ snapshot: oneui-preview-v1 })` returned a ready
  sandbox in ~3s (render path 3218ms total) with no image-build logs — the cold
  build happened only once at registration. Snapshot reuse confirmed.
- **Result:** [x] PASS

---

## New-dependency gate

Per RESEARCH Package Audit, esbuild / playwright / @daytonaio/sdk are all already
present (no new download). No new packages were installed during live verify.

- New packages installed during live verify: **none**.
- Verified on npmjs.com: [x] N/A (no new deps)

---

## Outcome

- Render + screenshots (PREV-04): [x] PASS — 28 KB PNG
- Live iframe (PREV-02): [x] PASS — signed Daytona-proxy URL minted
- Zero-egress EGRESS-BLOCKED exit 0 (PREV-01): [x] PASS
- Snapshot reuse (A4): [x] PASS — ~3s create, no rebuild

**Overall:** [x] APPROVED — all four checks verified end-to-end on the real Daytona
account (`groovy-spider-923`). Remaining: final in-Lab visual confirmation that the
canvas card shows the rendered screenshot/iframe (re-run with the `NODE_PATH` fix
hot-reloaded).
