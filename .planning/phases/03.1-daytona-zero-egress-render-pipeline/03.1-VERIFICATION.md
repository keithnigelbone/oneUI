---
phase: 03.1-daytona-zero-egress-render-pipeline
verified: 2026-06-02T12:00:00Z
status: passed
score: 10/10
human_items_resolution: "User approved the in-Lab live render (final visual). BUNDLE-01 checkbox + traceability updated to Complete. Real DAYTONA_API_KEY in .env.example is the developer's intentional choice (flagged; accepted)."
overrides_applied: 0
human_verification:
  - test: "Final in-Lab visual confirmation — canvas card shows rendered screenshot / iframe after NODE_PATH fix"
    expected: "The artifact canvas card in the Experience Builder Lab displays the Daytona-rendered screenshot (28 KB PNG) and the live-iframe loads from the signed Daytona-proxy URL (https://3000-…daytonaproxy01.eu)"
    why_human: "Requires a running dev server with EXPERIENCE_LAB_PREVIEW_EXECUTOR=daytona + DAYTONA_API_KEY set. The LIVE-VERIFY.md records all four checks PASS but notes this final in-Lab visual as the remaining human sign-off (hot-reloaded NODE_PATH fix)."
  - test: "REQUIREMENTS.md BUNDLE-01 checkbox and traceability row are stale (show unchecked / Pending)"
    expected: "BUNDLE-01 checkbox updated to [x] and traceability row updated to Complete after phase closes"
    why_human: "The implementation delivers BUNDLE-01 (assembleAsset.ts is substantive, tested, wired into DaytonaExecutor). The REQUIREMENTS.md stale state is a documentation update decision — confirm and update."
  - test: "Real Daytona API key committed to .env.example"
    expected: "Developer should confirm the rotated key value in .env.example is intentionally public (it's labeled 'real example value; keep real secrets out of git in your own forks') or rotate/replace it"
    why_human: ".env.example contains a real-looking dtn_ key with inline comment saying it is real. It is a non-secret example key intended for demonstrating the opt-in, but human must confirm this is the intended approach for the public repo."
---

# Phase 03.1: Daytona Zero-Egress Render Pipeline — Verification Report

**Phase Goal:** Make the deferred D-01 production preview path functional behind the existing `PreviewExecutor` seam — a generated artifact renders as real DOM inside a network-blocked Daytona sandbox and is screenshotted per profile, WITHOUT relaxing generation rules (D-03: the IR compiler still owns React + Jio-CSS codegen; Daytona only executes/previews/screenshots).

**Verified:** 2026-06-02T12:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | assembleAsset turns compiler TSX into ONE self-contained HTML string — no bare @oneui/ui / react / react-dom imports, brand CSS inlined, deterministic | VERIFIED | `packages/experience-builder-preview/src/bundler/assembleAsset.ts` (292 lines) — esbuild `build({ stdin, bundle:true, write:false, format:'iife' })` with the `oneui-bundle` virtual-module plugin; `wrapCSSForInjection` inline; 4-test suite in `assembleAsset.test.ts` covering self-contained / inlined CSS / deterministic / mounts |
| 2 | Bundler does NOT regenerate codegen — it consumes the compiler's TSX (D-03) | VERIFIED | `assembleAsset.ts` takes `input.bundle` (compiler output) as input, rewrites `@oneui/ui` import, never calls the compiler or IR generator |
| 3 | Daytona sandbox is created with `networkBlockAll:true` from the named snapshot | VERIFIED | `DaytonaExecutor.ts:148-166` — `create({ snapshot: PREVIEW_SNAPSHOT_NAME, networkBlockAll: true })` in the default path; `DaytonaExecutor.test.ts` test 2 asserts `{ snapshot, networkBlockAll:true }` |
| 4 | capture.js renders file:// asset in-box with Playwright, settles fonts+transitions, emits base64 PNG on stdout (PREV-04) | VERIFIED | `src/daytona/harness/capture.js` — `require('playwright')`, `page.goto(ASSET_URL, { waitUntil:'load' })`, double-rAF settle + `document.fonts.ready`, `process.stdout.write(png.toString('base64'))`; syntax-check passes |
| 5 | serve.js serves asset.html on the preview port for the live iframe | VERIFIED | `src/daytona/harness/serve.js` — `http.createServer`, binds `--port` / `PORT` / default 3000, serves ONLY `asset.html` (no traversal); syntax-check passes |
| 6 | probe.js exits 0 ONLY when outbound fetch is REFUSED (PREV-01 zero-egress proof) | VERIFIED | `src/daytona/harness/probe.js` — `process.exit(0)` on rejection + `EGRESS-BLOCKED` on stdout; `process.exit(2)` + `EGRESS-LEAK` on success; syntax-check passes |
| 7 | previewError flag surfaces a preview-infra failure as preview-error, NOT 'gap' | VERIFIED | `workflow.ts:512-529` previewStep try/catch records `ctx.previewError`, emits `status:'failed'`, returns `{ctx}` (no rethrow); `finalizeRun:776-783` skips gap-demotion when `previewError` is set; 2 regression tests in `workflow.test.ts` (Test A + Test B) |
| 8 | DaytonaExecutor.render completes the full pipeline: assembleAsset → create(snapshot, networkBlockAll) → upload → per-profile capture → serve.js + getSignedPreviewUrl → keep-alive lifecycle (not per-render delete) | VERIFIED | `DaytonaExecutor.ts:133-222` — full pipeline implemented; 9/9 real-shape test behaviors in `DaytonaExecutor.test.ts` (asset upload, create shape, capture, live iframe, keep-alive, delete-on-error, no-leak canary) |
| 9 | previewState OMITS sameOrigin — strict allow-scripts trust scoping preserved (PREV-01) | VERIFIED | `DaytonaExecutor.ts:196-203` — `previewState = { url, expiresAt, sandboxId }` with no `sameOrigin` field; asserted by test 4 `'sameOrigin' in result.previewState === false` |
| 10 | Live validation: render + 28KB PNG (PREV-04), signed Daytona-proxy URL (PREV-02), EGRESS-BLOCKED exit 0 (PREV-01), snapshot reuse (A4) | VERIFIED (live, authoritative) | `03.1-LIVE-VERIFY.md` status: verified — all four checks PASS on real account `groovy-spider-923`; render 3218ms, 28190B PNG, `https://3000-qo2lpuohynjjthar.daytonaproxy01.eu`, probe exit 0, ~3s create (no rebuild) |

**Score:** 10/10 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/experience-builder-preview/src/bundler/assembleAsset.ts` | assembleAsset(input) — self-contained HTML via esbuild + inlined brand CSS | VERIFIED | 292 lines; exports `assembleAsset`, `AssembleAssetInput`; esbuild via `createRequire` (Turbopack fix) |
| `packages/experience-builder-preview/src/bundler/assembleAsset.test.ts` | BUNDLE-01 unit coverage: 4 behaviors | VERIFIED | 98 lines; 4 tests: self-contained, brand CSS inlined, deterministic, mounts |
| `packages/experience-builder-preview/src/daytona/image.ts` | buildDaytonaImage() recipe + named-snapshot constant | VERIFIED | 82 lines; exports `PLAYWRIGHT_VERSION`, `DAYTONA_IMAGE_BASE`, `PREVIEW_SNAPSHOT_NAME`, `SANDBOX_PREVIEW_DIR`, `HARNESS_DIR`, `buildDaytonaImage()` |
| `packages/experience-builder-preview/src/daytona/harness/capture.js` | in-box Playwright screenshot → base64 PNG on stdout | VERIFIED | `require('playwright')`, `waitUntil:'load'`, double-rAF, `process.stdout.write(png.toString('base64'))` |
| `packages/experience-builder-preview/src/daytona/harness/serve.js` | in-box static server on preview port | VERIFIED | `http.createServer`, port resolution, asset.html only |
| `packages/experience-builder-preview/src/daytona/harness/probe.js` | zero-egress proof: exit 0 iff fetch refused | VERIFIED | exit 0 on rejection, exit 2 on success |
| `packages/experience-builder-preview/src/DaytonaExecutor.ts` | complete render() pipeline | VERIFIED | 336 lines; full pipeline with assembleAsset, create, upload, capture, serve.js, getSignedPreviewUrl, keep-alive lifecycle |
| `packages/experience-builder-preview/src/DaytonaExecutor.test.ts` | real-shape 9-test suite | VERIFIED | 9 tests covering all six planned behaviors |
| `packages/experience-builder-agents/src/workflow.ts` | previewStep try/catch + finalizeRun polarity fix | VERIFIED | previewError recorded at line 522; finalizeRun skip at line 776; versionFreezeStep guard at line 543 |
| `packages/experience-builder-agents/src/runTypes.ts` | previewError on RunExperienceResult | VERIFIED | `previewError?: { message: string }` at line 112 |
| `packages/experience-builder-agents/src/runContext.ts` | previewError on RunContext | VERIFIED | `previewError?: { message: string }` at line 71 |
| `apps/platform/src/app/api/experience-lab/run/route.ts` | selection gate + previewError → preview-error card | VERIFIED | `useDaytona` gate at lines 362-365; `previewError` propagated in result frame at line 404; `persistRun` branches on `previewError` at line 149 |
| `.env.example` | DAYTONA_API_KEY + EXPERIENCE_LAB_PREVIEW_EXECUTOR documented | VERIFIED | Both keys present with inline comments; `EXPERIENCE_LAB_PREVIEW_EXECUTOR=` left empty (opt-in) |
| `.planning/phases/03.1-daytona-zero-egress-render-pipeline/03.1-LIVE-VERIFY.md` | recorded live evidence | VERIFIED | status: verified; all four checks recorded with evidence |
| `apps/platform/next.config.js` | serverExternalPackages for esbuild + @daytonaio/sdk | VERIFIED | `serverExternalPackages: ['esbuild', '@daytonaio/sdk']` at line 23 |
| `packages/experience-builder-preview/scripts/register-snapshot.ts` | one-time snapshot registration tooling | VERIFIED | exists at `packages/experience-builder-preview/scripts/register-snapshot.ts`; wired as `pnpm register-snapshot` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `assembleAsset.ts` | `esbuild build({ stdin, write:false, bundle:true })` | `require_('esbuild')` runtime require | VERIFIED | `assembleAsset.ts:243-269` — `build({ bundle:true, write:false, format:'iife' })` |
| `assembleAsset.ts` | `@oneui/shared/engine` (brand CSS) + `oneui-bundle` virtual alias | `precomputeBrandCSSNew` + `wrapCSSForInjection` + `oneuiBundlePlugin` | VERIFIED | `assembleAsset.ts:31-32` imports engine; `oneuiBundlePlugin` resolves `oneui-bundle` → playground mjs |
| `DaytonaExecutor.ts` | `assembleAsset` (Plan 01) + `buildDaytonaImage/PREVIEW_SNAPSHOT_NAME` (Plan 02) | `render()` pipeline | VERIFIED | `DaytonaExecutor.ts:58-59` imports both; used at lines 137, 157-166 |
| `DaytonaExecutor.ts` | `sandbox.getSignedPreviewUrl(previewPort, ttl)` | live preview URL minted after serve.js starts | VERIFIED | `DaytonaExecutor.ts:188-191` |
| `workflow.ts previewStep` | `finalizeRun` outcome labeling | `ctx.previewError` flag short-circuits gap demotion | VERIFIED | `workflow.ts:776-783` — `if (ctx.previewError)` branch sets `'error'` and skips ir-but-no-url→gap demotion |
| `route.ts` | `run.previewError` | card copy = preview-error not gap | VERIFIED | `route.ts:399-404` — `...(run.previewError ? { previewError: run.previewError } : {})` in the result frame |
| `capture.js` harness | Playwright in-box (no external CDP) | `require('playwright')`, `page.goto(file://, {waitUntil:'load'})` | VERIFIED | `capture.js:27,65` |
| `DaytonaExecutor.buildCaptureCommand` | `NODE_PATH="$(npm root -g)"` prefix | resolves globally-installed playwright for `require('playwright')` | VERIFIED | `DaytonaExecutor.ts:327` |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `assembleAsset.ts` | `html` (self-contained HTML string) | esbuild `build()` return + `generateBrandLayerCss()` + `tokenCss()` | Yes — esbuild produces real iife bundle; engine functions generate real @layer brand CSS | FLOWING |
| `DaytonaExecutor.ts render()` | `screenshots` (PNG Buffers) | `sandbox.process.executeCommand(capture.js)` → base64 stdout → `Buffer.from(result.result.trim(), 'base64')` | Yes — in-box Playwright produces real PNG (live-verified: 28190B) | FLOWING |
| `DaytonaExecutor.ts render()` | `previewState.url` | `sandbox.getSignedPreviewUrl(previewPort, ttl).url` | Yes — Daytona SDK returns real signed proxy URL (live-verified: `https://3000-…daytonaproxy01.eu`) | FLOWING |
| `workflow.ts previewStep` | `ctx.previewState`, `ctx.screenshots` | `executor.render(...)` | Yes — wired to DaytonaExecutor (or LabAstExecutor) which produces real state | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| harness scripts syntax-valid | `node --check` for capture/serve/probe.js | exit 0 / SYNTAX-OK all three | PASS |
| No SDK import in harness | `grep -L "@daytonaio/sdk" harness/*.js \| wc -l` | 0 SDK import occurrences in harness | PASS |
| SDK import isolated to DaytonaExecutor.ts + image.ts | grep scan of `src/` excluding test/those files | Only comment reference in `PreviewExecutor.ts` (not an import) | PASS |
| previewError flag in workflow | grep `previewError` in `workflow.ts` | 8 occurrences — try/catch, ctx set, finalizeRun skip, result propagation | PASS |
| route previewError + selection gate | grep `previewError` + `EXPERIENCE_LAB_PREVIEW_EXECUTOR` in `route.ts` | Both present at correct locations | PASS |
| esbuild write:false bundle:true pattern | grep `write.*false` in `assembleAsset.ts` | `write: false` at line 252 | PASS |
| sameOrigin absent from DaytonaExecutor previewState | grep `sameOrigin` in `DaytonaExecutor.ts` | Only a comment; not a field assignment | PASS |
| serverExternalPackages in next.config.js | grep `serverExternalPackages` | `['esbuild', '@daytonaio/sdk']` present | PASS |

---

### Probe Execution

Step 7c skipped: probes in this phase (`probe-diagnostic.ts`, `register-snapshot.ts`, `render-diagnostic.ts`) require live Daytona credentials and a running sandbox. They were executed interactively during the Plan 05 human-verify gate and all PASSED (recorded in `03.1-LIVE-VERIFY.md`). That evidence is treated as authoritative per the phase instructions.

---

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|---------------|-------------|--------|----------|
| BUNDLE-01 | 03.1-01 | Self-contained HTML asset from compiler TSX + React + playground bundle + brand CSS; no external imports; D-03 honored | SATISFIED | `assembleAsset.ts` exists, is substantive (292 lines), is wired into `DaytonaExecutor.render()`, and 4-test suite passes. Note: REQUIREMENTS.md checkbox still shows `[ ]` / Pending (stale — see human_verification item 2). |
| PREV-01 | 03.1-02, 03.1-04, 03.1-05 | Zero-egress sandbox: networkBlockAll, no auth/session/Convex access, strict allow-scripts iframe | SATISFIED | `create({ networkBlockAll:true })` in executor; `sameOrigin` absent from previewState; probe.js confirms `EGRESS-BLOCKED` exit 0 live |
| PREV-04 | 03.1-02, 03.1-03, 03.1-04, 03.1-05 | Per-profile screenshots by credential-free Playwright | SATISFIED | `capture.js` in-box Playwright; `DaytonaExecutor.captureInBox`; live-verified 28KB PNG |

Note on PREV-02 (immutable preview URL): covered by 03.1-04 (DaytonaExecutor mints `getSignedPreviewUrl`; live-verified signed proxy URL). PREV-02 is tagged to Phase 3 in REQUIREMENTS.md (already complete); 03.1 adds the Daytona-path implementation which is now also verified.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `assembleAsset.ts` | 101, 131, 136 | `placeholder` keyword in virtual-module fallback | INFO | NOT a stub — these are the virtual placeholder React components emitted when `oneui-playground.mjs` is absent (unit test path). The production path resolves to the real playground bundle. Data flows correctly in production. |
| `.env.example` | 62 | Real-looking `DAYTONA_API_KEY` value committed | WARNING | The comment explicitly labels it a "real example value" and advises keeping secrets in `.env.local`. The key was rotated from the previous value during live testing. Human should confirm this is intentional for the public repo (see human_verification item 3). |
| `REQUIREMENTS.md` | 91, 219 | BUNDLE-01 checkbox unchecked (`[ ]`) and traceability shows "Pending" | WARNING | Stale documentation state — the implementation is complete. Human should update (see human_verification item 2). |

No `TBD`, `FIXME`, or `XXX` markers found in any phase-modified file.

---

### Human Verification Required

#### 1. Final in-Lab visual confirmation (canvas card screenshot/iframe)

**Test:** With `EXPERIENCE_LAB_PREVIEW_EXECUTOR=daytona` and `DAYTONA_API_KEY` set, run `pnpm dev` with `npx convex dev` running, submit a prompt in the Experience Builder Lab, and confirm the artifact canvas card displays (a) the rendered screenshot PNG and (b) the live iframe loads from the signed Daytona-proxy URL.

**Expected:** Canvas card shows real Daytona-rendered screenshot; iframe in the card loads from `https://3000-…daytonaproxy01.eu` (not `/internal/render-ast`).

**Why human:** Requires a live dev server, live Convex deployment, and real Daytona credentials. The LIVE-VERIFY.md records the endpoint-level checks PASS (signed URL minted, 28KB PNG returned) but the final in-Lab visual confirmation with the hot-reloaded `NODE_PATH` fix is noted as remaining in the live-verify document.

#### 2. Update REQUIREMENTS.md BUNDLE-01 checkbox

**Test:** Update `REQUIREMENTS.md` — change `- [ ] **BUNDLE-01**` to `- [x] **BUNDLE-01**` and update the traceability row from `Pending` to `Complete`.

**Expected:** BUNDLE-01 marked complete, traceability row updated.

**Why human:** REQUIREMENTS.md is a planning document; its checkbox update is a human decision that confirms the phase closure, not a code correctness check.

#### 3. Confirm real API key in .env.example is intentional

**Test:** Review `DAYTONA_API_KEY=dtn_48386a6e3fe81ea30ce05f1b69607d05db3ab42afc23099dc2cfefdfc2d5e236` committed to `.env.example`. The comment says "this is a real example value; keep real secrets out of git in your own forks — prefer `.env.local`."

**Expected:** Explicit acknowledgment that this is intentionally committed (it's a personal development key being used as a public example), OR replace it with a placeholder value like `DAYTONA_API_KEY=your_daytona_api_key_here`.

**Why human:** Only the developer can decide whether a real key committed to a public-facing `.env.example` is acceptable for their security posture. The key was used for live testing and may still be active.

---

### Gaps Summary

No blocking gaps. All 10 must-have truths verified. The three human verification items are informational/documentation concerns:

1. **Final in-Lab visual** — automated checks (endpoint-level, unit tests) all pass. The LIVE-VERIFY.md approval records the user approved after the NODE_PATH fix; this item is a final re-confirmation after hot-reload.
2. **REQUIREMENTS.md stale checkbox** — documentation update, not a code issue. Phase delivered BUNDLE-01 in full.
3. **API key in .env.example** — security posture decision. The key is intentionally labeled a "real example value" in the file.

---

_Verified: 2026-06-02T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
