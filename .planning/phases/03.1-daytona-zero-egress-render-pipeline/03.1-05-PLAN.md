---
phase: 03.1-daytona-zero-egress-render-pipeline
plan: 05
type: execute
wave: 3
depends_on: ["03.1-04"]
files_modified:
  - .env.example
  - .planning/phases/03.1-daytona-zero-egress-render-pipeline/03.1-LIVE-VERIFY.md
autonomous: false
requirements: [PREV-01, PREV-04]
must_haves:
  truths:
    - "Against a REAL Daytona account, a generated artifact renders in a network-blocked sandbox and screenshots are returned (PREV-04)"
    - "The live in-box iframe loads from the signed preview URL (PREV-02)"
    - "The zero-egress probe (probe.js) confirms an outbound fetch from inside the sandbox to the Convex URL is REFUSED (PREV-01 — UAT item 3 from 03-HUMAN-UAT.md)"
    - "The named snapshot is reused across runs without rebuild (RESEARCH Assumption A4)"
    - "Any genuinely NEW dependency install is approved at this checkpoint before being added"
  artifacts:
    - path: ".planning/phases/03.1-daytona-zero-egress-render-pipeline/03.1-LIVE-VERIFY.md"
      provides: "recorded live-verification evidence: render+screenshot, live iframe, zero-egress probe result, snapshot reuse"
      min_lines: 20
    - path: ".env.example"
      provides: "DAYTONA_API_KEY + EXPERIENCE_LAB_PREVIEW_EXECUTOR documented (server-only, opt-in)"
      contains: "DAYTONA_API_KEY"
  key_links:
    - from: "probe.js (Plan 02)"
      to: "Convex deployment URL"
      via: "fetch refused inside networkBlockAll sandbox"
      pattern: "EGRESS-BLOCKED"
---

<objective>
The non-negotiable live gate: the Daytona path can only be claimed against a REAL
account. This plan documents the opt-in env, then runs a BLOCKING human-verify
checkpoint validating four things on a live Daytona account:
  1. A generated artifact RENDERS in a network-blocked sandbox and screenshots return (PREV-04).
  2. The live in-box iframe LOADS from the signed preview URL (PREV-02).
  3. The PREV-01 zero-egress probe: a `fetch` from inside the sandbox to the Convex URL
     is REFUSED (probe.js exit 0). This is UAT item 3 from 03-HUMAN-UAT.md, explicitly
     deferred in Phase 3 for lack of credentials.
  4. The named snapshot is reused across runs without a rebuild (RESEARCH Assumption A4).

This checkpoint is also the gate for any genuinely NEW dependency install: per the
RESEARCH Package Audit, slopcheck was unavailable so packages are `[ASSUMED]`; esbuild/
playwright/@daytonaio/sdk are all already present (no new download), but if execution
surfaces ANY new install it must be approved here against npmjs.com before being added.

Purpose: prove zero-egress + live render/iframe on real infra before flipping the opt-in on.
Output: documented env + recorded live-verification evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-daytona-zero-egress-render-pipeline/03.1-CONTEXT.md
@.planning/phases/03.1-daytona-zero-egress-render-pipeline/03.1-RESEARCH.md
@.planning/phases/03-preview-eval-repair/03-HUMAN-UAT.md
@.planning/phases/03.1-daytona-zero-egress-render-pipeline/03.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the opt-in env + write the live-verify checklist scaffold</name>
  <files>.env.example, .planning/phases/03.1-daytona-zero-egress-render-pipeline/03.1-LIVE-VERIFY.md</files>
  <action>Add `DAYTONA_API_KEY` (server-only, read by `new Daytona()`; NEVER written to bundle/previewState/screenshot) and `EXPERIENCE_LAB_PREVIEW_EXECUTOR` (set to `daytona` to opt-in; default LabAstExecutor) to `.env.example` with one-line comments. Create `03.1-LIVE-VERIFY.md` as an empty-result evidence template with four sections matching the four checkpoint checks (render+screenshot, live iframe, zero-egress probe, snapshot reuse), each with fields: command run, expected, observed, pass/fail. Do NOT put any real key in either file.</action>
  <verify>
    <automated>grep -q "DAYTONA_API_KEY" .env.example && grep -q "EXPERIENCE_LAB_PREVIEW_EXECUTOR" .env.example && test -f .planning/phases/03.1-daytona-zero-egress-render-pipeline/03.1-LIVE-VERIFY.md && echo ENV-DOCS-OK</automated>
  </verify>
  <done>.env.example documents the opt-in + key (no secrets); the live-verify evidence template exists with the four check sections.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Live Daytona validation — render, live iframe, zero-egress probe, snapshot reuse</name>
  <what-built>The complete Daytona zero-egress render path (Plans 01–04): self-contained bundler, version-pinned Playwright image + harness, complete DaytonaExecutor (render + screenshot + live serve + signed URL + keep-alive/TTL teardown), and the preview-error fix. All offline tests pass. This step validates it on a REAL Daytona account — the one thing that cannot be automated (no credentials in CI).</what-built>
  <how-to-verify>
    Prereqs: a real Daytona account; set `DAYTONA_API_KEY` and `EXPERIENCE_LAB_PREVIEW_EXECUTOR=daytona` in the local env (NOT committed).

    1. Register the named snapshot ONCE from the custom image (Plan 02 `buildDaytonaImage()` / `PREVIEW_SNAPSHOT_NAME`). Confirm `onSnapshotCreateLogs` surface the build; note first-build duration.
    2. Run a generation through the Lab with the Daytona executor opt-in ON. Confirm: per-profile screenshots return (PREV-04), and the canvas card's live iframe LOADS from the signed preview URL (PREV-02).
    3. Zero-egress probe (PREV-01 / UAT item 3): in a `networkBlockAll:true` sandbox built from the real image, run `node preview/probe.js <your-Convex-deployment-URL>`. PASS requires `exitCode === 0` and stdout `EGRESS-BLOCKED: ...` (the fetch was refused). A `EGRESS-LEAK` / exit 2 is a HARD FAIL. Positive control: the file:// asset render still succeeds (the block does not break local rendering).
    4. Run a SECOND generation; confirm `create({ snapshot })` reuses the snapshot WITHOUT a rebuild (RESEARCH Assumption A4) — note the latency delta vs the cold first build.
    5. NEW-DEP GATE: if execution required ANY new package install beyond esbuild/playwright/@daytonaio/sdk (all already present), verify each on npmjs.com (registry age, downloads, source repo) BEFORE approving — `[ASSUMED]` packages are never auto-approved.

    Record command/expected/observed/pass for each in `03.1-LIVE-VERIFY.md`.
  </how-to-verify>
  <resume-signal>Type "approved" once all four checks pass (render+screenshots, live iframe loads, EGRESS-BLOCKED exit 0, snapshot reused) and any new dep is verified — or describe the failing check.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inside sandbox → Convex/auth | the probe proves this boundary is sealed under networkBlockAll |
| developer machine → committed files | the live key must never be committed (.env.example carries placeholders only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-031-11 | Information disclosure | zero-egress not actually enforced | mitigate | The live probe (probe.js) HARD-FAILS on a successful fetch (EGRESS-LEAK, exit 2); checkpoint passes only on exit 0 / EGRESS-BLOCKED. PREV-01 cannot be claimed without it. |
| T-031-12 | Information disclosure | DAYTONA_API_KEY committed | mitigate | Only `.env.example` placeholders are committed; the real key stays in the uncommitted local env. |
| T-031-SC | Tampering | npm/pip/cargo installs | mitigate | New-dep gate at this BLOCKING human-verify: any `[ASSUMED]`/new package verified on npmjs.com before approval; never auto-approved (workflow.auto_advance ignored). |
</threat_model>

<verification>
- `.env.example` documents the opt-in + key with no real secret.
- The checkpoint records all four checks passing in `03.1-LIVE-VERIFY.md`, including `EGRESS-BLOCKED` exit 0 (PREV-01) and snapshot reuse (A4).
</verification>

<success_criteria>
- On a real Daytona account: artifact renders + screenshots (PREV-04), live iframe loads (PREV-02), outbound fetch to Convex is refused (PREV-01 / UAT item 3), snapshot reused without rebuild.
- Any new dependency was verified before install; no secret committed.
</success_criteria>

<output>
Create `.planning/phases/03.1-daytona-zero-egress-render-pipeline/03.1-05-SUMMARY.md` when done.
</output>
