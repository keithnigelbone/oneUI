---
phase: 01-isolated-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/experience-builder-registry/package.json
  - packages/experience-builder-registry/tsconfig.json
  - packages/experience-builder-registry/vitest.config.ts
  - packages/experience-builder-registry/src/index.ts
  - packages/experience-builder-registry/src/queryRegistry.ts
  - packages/experience-builder-registry/src/queryRegistry.test.ts
autonomous: true
requirements: [REG-01, REG-02, REG-03]
user_setup: []

must_haves:
  truths:
    - "queryRegistry() returns production-shaped JioComponentRegistryItem[] derived from the real jioAlphaCatalog (REG-01/REG-02)"
    - "Membership is exact lookup — an unregistered component id yields a component gap, never a fuzzy match (REG-03)"
    - "Known-drift components (Modal, Text) are excluded from the generatable set per CONCERNS.md"
  artifacts:
    - path: "packages/experience-builder-registry/src/queryRegistry.ts"
      provides: "deterministic registry adapter over jioAlphaCatalog + componentRegistry + meta"
      contains: "queryRegistry"
  key_links:
    - from: "packages/experience-builder-registry/src/queryRegistry.ts"
      to: "@oneui/ui jioAlphaCatalog + componentRegistry"
      via: "deep-path import (never the barrel)"
      pattern: "jioAlphaCatalog|componentRegistry"
    - from: "packages/experience-builder-registry/src/queryRegistry.ts"
      to: "@oneui/experience-builder-core JioComponentRegistryItem"
      via: "import type"
      pattern: "JioComponentRegistryItem"
---

<objective>
Build `@oneui/experience-builder-registry` — the deterministic adapter that exposes the single source of truth (`jioAlphaCatalog` + `componentRegistry` + `@oneui/shared/meta`) as production-shaped `JioComponentRegistryItem`s and answers `queryRegistry()` membership by exact lookup. This is the registry slice: the generator (plan 04) retrieves candidates from here before generating, and only registry-approved components may ever be emitted (REG-02/REG-03).

Purpose: Make the Core-Value allowlist real and drift-proof. Per Pitfall 5 the adapter derives its shape from the existing metadata, never a hand-authored parallel, so P2's freshness gate (REG-04) shapes cleanly. Known-drift components (Modal, Text) are excluded from the generatable set.
Output: A pure-TS adapter package with Vitest node-env tests for production-shape conformance + exact-membership gap behaviour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-isolated-foundation/01-RESEARCH.md
@.planning/phases/01-isolated-foundation/01-PATTERNS.md
@.planning/phases/01-isolated-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold registry package + queryRegistry adapter over jioAlphaCatalog</name>
  <read_first>
    - packages/ui/src/registry/jioAlphaCatalog.ts (lines 10-25: JioAlphaComponentCatalogEntry; importPath = @oneui/ui/components/<Folder>)
    - packages/ui/src/registry/componentRegistry.ts (component refs + meta join)
    - packages/shared/src/meta/generateAIContext.ts (deriveSchemaFromMeta / generateAIContext — props/slots source)
    - packages/experience-builder-core/src/contracts/registryItem.ts (JioComponentRegistryItem — the target shape from plan 01)
    - packages/experience-builder-core/package.json (workspace dep pattern to mirror)
    - .planning/phases/01-isolated-foundation/01-PATTERNS.md (§ registryItem.ts + queryRegistry.ts; § Deep-import boundary)
    - .planning/phases/01-isolated-foundation/01-RESEARCH.md (Pitfall 5; § Don't Hand-Roll — component metadata row)
  </read_first>
  <behavior>
    - Test: `queryRegistry()` returns a non-empty `JioComponentRegistryItem[]` whose every item conforms to the plan-01 `JioComponentRegistryItem` shape (id, importPath, props, variants, slots, states, usageRules, antiPatterns, supportedBrands/Profiles, tokenDependencies).
    - Test: every returned `importPath` matches `@oneui/ui/components/<Folder>` (the VAL-02 allowlist source) — no external/non-Jio import paths.
    - Test: looking up a registered component id (e.g. `Button`) returns an item; looking up an unregistered id (e.g. `FancyHero`) returns a component-gap result, not a near-match (exact membership, REG-03).
    - Test: `Modal` and `Text` are NOT in the generatable set (known-drift exclusion per CONCERNS.md), while still derived from the same catalog.
    - Test: items are derived from `jioAlphaCatalog` — the count/ids trace to the catalog, proving no hand-authored parallel (assert a known catalog entry surfaces).
  </behavior>
  <action>
    Create package `@oneui/experience-builder-registry` at `packages/experience-builder-registry/` with `package.json` (deps: `@oneui/experience-builder-core` workspace, `@oneui/ui` + `@oneui/shared` as `import type`/runtime deep-path sources; `scripts.test` = `vitest run`), `tsconfig.json`, and `vitest.config.ts` (node env). Run `pnpm install` to link.
    Create `src/queryRegistry.ts` — a deterministic adapter that joins `jioAlphaCatalog` (status, importPath, surfaceAware, multiAccent) with `componentRegistry` (component refs) and `@oneui/shared/meta` (`deriveSchemaFromMeta`/`generateAIContext` for props/variants/slots) into `JioComponentRegistryItem[]`. Import via deep paths only (`@oneui/ui/components/...`), NEVER the `@oneui/ui` barrel (eslint `no-restricted-imports`). Export `queryRegistry(filter?)` returning candidate items and `getRegistryItem(id)` returning an item OR a component-gap descriptor (exact lookup — Membership, not embeddings; REG-05 deferred). Exclude `Modal` and `Text` from the generatable set (CONCERNS.md drift), keeping a documented exclusion list.
    Write `src/queryRegistry.test.ts` per the behavior block. Export from `src/index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-registry test --run 2>&1 | tail -15; pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/experience-builder-registry test` passes; `pnpm typecheck` clean.
    - `grep -c "from '@oneui/ui'" packages/experience-builder-registry/src/queryRegistry.ts` returns 0 (no barrel import; deep paths only).
    - Test asserts `Modal`/`Text` excluded and an unregistered id returns a gap, not a fuzzy match.
    - Every returned `importPath` starts with `@oneui/ui/components/`.
  </acceptance_criteria>
  <done>The registry adapter derives production-shaped items from the real catalog, answers membership by exact lookup with a gap path for unregistered components, and excludes known-drift components — ready for the generator and validator to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated artifact components → registry allowlist | Only ids present in the registry may be emitted; everything else is a component gap. |
| Lab registry shape ↔ real component metadata | Drift between the two is the P2 failure mode; the adapter must derive, not duplicate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | Unregistered component emitted into an artifact | mitigate | `getRegistryItem` is exact lookup; unregistered id → component-gap descriptor, never a near-match (REG-03). |
| T-01-05 | Tampering | Registry drift baked into a hand-authored parallel | mitigate | Adapter derives from `jioAlphaCatalog`+`componentRegistry`+meta; test asserts traceability; Modal/Text excluded (Pitfall 5). |
| T-01-06 | Spoofing | Non-Jio importPath smuggled as "approved" | mitigate | Test asserts every `importPath` ∈ `@oneui/ui/components/*`; deep-import-only boundary. |
</threat_model>

<verification>
- `pnpm --filter @oneui/experience-builder-registry test` green (REG-01/02/03).
- `pnpm typecheck` clean.
- No barrel `@oneui/ui` import; all importPaths are Jio component deep paths.
- git diff touches only `packages/experience-builder-registry/` + lockfile.
</verification>

<success_criteria>
- `queryRegistry()` returns production-shaped, catalog-derived `JioComponentRegistryItem[]`.
- Membership is exact; unregistered components yield a component gap (REG-03).
- Known-drift components excluded; ready for plan 04 retrieval + plan 03 validation.
</success_criteria>

<output>
Create `.planning/phases/01-isolated-foundation/01-02-SUMMARY.md` when done.
</output>
