---
phase: 01-isolated-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/experience-builder-core/package.json
  - packages/experience-builder-core/tsconfig.json
  - packages/experience-builder-core/vitest.config.ts
  - packages/experience-builder-core/src/index.ts
  - packages/experience-builder-core/src/ir/schema.ts
  - packages/experience-builder-core/src/ir/artifactTypes.ts
  - packages/experience-builder-core/src/ir/irToAst.ts
  - packages/experience-builder-core/src/ir/patch.ts
  - packages/experience-builder-core/src/profiles/outputProfileTable.ts
  - packages/experience-builder-core/src/contracts/foundationResolve.ts
  - packages/experience-builder-core/src/contracts/registryItem.ts
  - packages/experience-builder-core/src/contracts/validation.ts
  - packages/experience-builder-core/src/contracts/events.ts
  - packages/experience-builder-core/src/ir/schema.test.ts
  - packages/experience-builder-core/src/ir/artifactTypes.test.ts
  - packages/experience-builder-core/src/ir/irToAst.test.ts
  - packages/experience-builder-core/src/ir/patch.test.ts
  - packages/experience-builder-core/src/profiles/outputProfileTable.test.ts
  - eslint.config.mjs
  - scripts/check-single-ai-version.ts
  - package.json
autonomous: false
requirements: [IR-01, IR-02, IR-03, IR-04, INPUT-02, INPUT-03, CANVAS-03, FND-01, FND-03, REG-01, ORCH-03, ORCH-04, VAL-01, LAB-03]
user_setup: []

must_haves:
  truths:
    - "A markup-free Jio Experience IR Zod schema rejects any string field carrying HTML/JSX/tag/className (IR-02)"
    - "Valid IR parses with all IR-04 fields; missing required fields fail parse (IR-01/IR-04)"
    - "IR → AST mapping emits only ComponentASTNode + TextASTNode, never ElementASTNode raw tags (IR-03)"
    - "JSON-patch diff/apply round-trips two IR documents (IR-03)"
    - "The output-profile table maps each of the 8 artifact types to its valid output profiles (D-03/INPUT-02/INPUT-03)"
    - "A foundation-resolve result type has a first-class typed gap variant (FND-03)"
    - "CI fails if more than one ai@6 version resolves inside the Lab packages (LAB-03)"
  artifacts:
    - path: "packages/experience-builder-core/src/ir/schema.ts"
      provides: "JioExperienceIR Zod schema with zero markup fields"
      contains: "JioExperienceIR"
    - path: "packages/experience-builder-core/src/ir/irToAst.ts"
      provides: "IR -> ComponentASTNode/TextASTNode mapper (no ElementASTNode)"
      contains: "irToAst"
    - path: "packages/experience-builder-core/src/profiles/outputProfileTable.ts"
      provides: "artifact-type -> output-profile mapping (D-03)"
      contains: "outputProfileTable"
    - path: "packages/experience-builder-core/src/contracts/events.ts"
      provides: "ExperienceBuilderEvent discriminated union"
      contains: "ExperienceBuilderEvent"
    - path: "scripts/check-single-ai-version.ts"
      provides: "single ai@6 version CI gate for Lab packages"
  key_links:
    - from: "packages/experience-builder-core/src/ir/irToAst.ts"
      to: "@oneui/shared componentAST types"
      via: "import type ComponentASTNode/TextASTNode/ASTRoot"
      pattern: "ComponentASTNode|TextASTNode|ASTRoot"
    - from: "eslint.config.mjs"
      to: "experience-builder-* <-> (builder)/ExperienceCanvas boundary"
      via: "no-restricted-imports patterns"
      pattern: "no-restricted-imports"
---

<objective>
Build `@oneui/experience-builder-core` — the contracts package that blocks every other plan in this phase — and stand up the isolation CI guards (eslint Lab↔Builder boundary + single-`ai`-version gate) and the gated Mastra package-install checkpoint. This is the first task of the Walking Skeleton: it produces the canonical, markup-free Jio Experience IR (Zod), the 13-member artifact/card object model, the output-profile table, the four contract types (foundation-resolve with a first-class gap variant, registry item, validation result, event union), the IR↔AST mapper that emits only registry-safe AST nodes, and the JSON-patch diff/apply — all production-shaped so P2 is a data swap, not a migration.

Purpose: Freeze every contract the rest of the milestone consumes, with the four HIGH-severity risks (raw-JSX smuggling, isolation leakage, Mastra/AI-SDK version drift) designed out up front. Per D-05 the full union is defined now; per IR-02 the schema can carry no markup.
Output: A pure-TS, framework-agnostic contracts package with Vitest node-env tests (Wave 0 scaffold for IR-01..04), extended eslint isolation rules, a `pnpm why ai` single-version script, and a human-verify checkpoint clearing the Mastra install for plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-isolated-foundation/01-CONTEXT.md
@.planning/phases/01-isolated-foundation/01-RESEARCH.md
@.planning/phases/01-isolated-foundation/01-PATTERNS.md
@.planning/phases/01-isolated-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify Mastra packages before any install (supply-chain gate)</name>
  <read_first>
    - .planning/phases/01-isolated-foundation/01-RESEARCH.md (§ Package Legitimacy Audit, § Open Questions #1, Pitfall 1)
  </read_first>
  <what-built>
    Nothing is installed yet. Per RESEARCH.md § Package Legitimacy Audit, slopcheck could not run in the research sandbox, so `@mastra/core` and `@mastra/ai-sdk` are tagged [ASSUMED] and MUST be human-verified before plan 04 installs them. This checkpoint clears the install for plan 04; it does not install anything itself (the agents package does not exist until plan 04).
  </what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/@mastra/core — confirm it is the official `mastra-ai/mastra` monorepo package, mature 1.x line, high downloads.
    2. Visit https://www.npmjs.com/package/@mastra/ai-sdk — confirm official Mastra streaming bridge.
    3. Run `npm view @mastra/core scripts.postinstall` and `npm view @mastra/ai-sdk scripts.postinstall` — confirm NO out-of-tree network/filesystem postinstall script.
    4. Run `npm view @mastra/ai-sdk versions --json` and `npm view @mastra/ai-sdk peerDependencies` — pick the latest `@mastra/ai-sdk` build whose `@mastra/core` peer range includes the core pin (RESEARCH targets `@mastra/core@1.37.1`; resolve Open Question #1 here). Record the exact pinned pair for plan 04.
    5. Run `npm view @mastra/core version` and `npm view @mastra/ai-sdk version` and note the exact current versions.
  </how-to-verify>
  <action>
    Pause for human supply-chain verification of `@mastra/core` and `@mastra/ai-sdk` before plan 04 installs them (slopcheck unavailable -> both are [ASSUMED] per RESEARCH § Package Legitimacy Audit). Install nothing in this plan. Run the npm-registry + postinstall + peer-range checks in `<how-to-verify>`, resolve the exact `@mastra/core` + `@mastra/ai-sdk` version pair (matching peer floor; RESEARCH Open Question #1), and record that exact pair in this plan's SUMMARY so plan 04 installs it verbatim. Never auto-approvable.
  </action>
  <acceptance_criteria>
    - Human confirms both packages are official Mastra packages with no suspicious postinstall.
    - The exact `@mastra/core` + `@mastra/ai-sdk` version pair (with matching peer range) is recorded in this plan's SUMMARY for plan 04 to consume.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the exact pinned version pair, or describe the concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold experience-builder-core package + isolation CI guards (Wave 0)</name>
  <read_first>
    - pnpm-workspace.yaml (packages/* glob already covers new packages)
    - packages/convex/package.json (sibling package.json shape: name, main, types, scripts, deps)
    - eslint.config.mjs (lines ~201-212: existing `no-restricted-imports` rule to extend in place)
    - package.json (root scripts block; `ci:gates` composition; `test` = turbo test)
    - .planning/phases/01-isolated-foundation/01-PATTERNS.md (§ eslint.config.mjs MODIFY IN PLACE; § Pure-function module convention)
    - .planning/phases/01-isolated-foundation/01-VALIDATION.md (§ Test Infrastructure — Vitest node env, per-package vitest.config.ts)
  </read_first>
  <action>
    Create package `@oneui/experience-builder-core` at `packages/experience-builder-core/` with `package.json` (name `@oneui/experience-builder-core`, private, `main`/`types` to `src/index.ts` or built `dist`, `scripts.test` = `vitest run`, deps: `zod` and a `@oneui/shared` workspace `import type` dependency only — no React, no tldraw), a `tsconfig.json` extending the repo base config, and `vitest.config.ts` with `test.environment: 'node'`. Add an empty `src/index.ts` barrel (filled by later tasks). Run `pnpm install` to link the workspace.
    Extend `eslint.config.mjs` `no-restricted-imports` IN PLACE (do not add a custom script): add `patterns` enforcing the Lab↔Builder boundary — `(builder)`/`ExperienceCanvas` files MUST NOT import `@oneui/experience-builder-*`, and `experience-builder-*` packages MUST NOT deep-import `apps/platform/src/design-tools/ExperienceCanvas/*` or `(builder)` internals. Keep the existing `@oneui/ui` barrel restriction untouched.
    Create `scripts/check-single-ai-version.ts` that runs `pnpm why ai` (or reads the workspace resolution tree) scoped to the Lab packages + the `(experience-lab)` route resolution subtree and fails (non-zero exit) if more than one `ai@6.x` version resolves there. Scope the assertion to the Lab subtree, NOT repo-wide (the repo already legitimately resolves 6.0.111 + 6.2.2 — see RESEARCH Open Question #2). Wire it into root `package.json` scripts as `check:single-ai` and append it to the `ci:gates` chain.
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-core test --run 2>&1 | tail -5; pnpm lint 2>&1 | tail -5; pnpm tsx scripts/check-single-ai-version.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/experience-builder-core test` runs (zero tests OK at this point, exit 0).
    - `eslint.config.mjs` contains `no-restricted-imports` `patterns` entries naming both `experience-builder` and `ExperienceCanvas`/`(builder)` (grep: `grep -c "experience-builder" eslint.config.mjs` ≥ 1 AND `grep -c "ExperienceCanvas\|(builder)" eslint.config.mjs` ≥ 1).
    - `scripts/check-single-ai-version.ts` exists and exits 0 today (single ai version in the empty Lab subtree); `package.json` `ci:gates` references `check:single-ai`.
    - No file outside `packages/experience-builder-core/`, `eslint.config.mjs`, `scripts/`, `package.json`, `pnpm-workspace.yaml`/`pnpm-lock.yaml` is modified (git diff scope check).
  </acceptance_criteria>
  <done>Core package links into the workspace, isolation eslint boundary + single-ai gate are wired into CI, and no existing-Builder file is touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: IR schema, artifact/card union, output-profile table + Wave 0 tests</name>
  <read_first>
    - packages/shared/src/types/componentAST.ts (lines 17-71: ASTSerializableValue, ComponentASTNode, ElementASTNode (the COUNTER-pattern — do NOT mirror), TextASTNode, ASTRoot.version)
    - packages/ui/src/registry/jioAlphaCatalog.ts (lines 10-25: JioAlphaComponentCatalogEntry shape, importPath = @oneui/ui/components/<Folder>)
    - .planning/phases/01-isolated-foundation/01-PATTERNS.md (§ schema.ts — markup-free slot pattern, FORBIDDEN fields list; § Required IR field set IR-04)
    - .planning/phases/01-isolated-foundation/01-RESEARCH.md (§ Code Examples — markup-free SlotValue; Pitfall 2)
    - .planning/phases/01-isolated-foundation/01-VALIDATION.md (§ Per-Task Verification Map — IR-01/IR-02/IR-03/IR-04 rows)
  </read_first>
  <behavior>
    - Test: a fully-populated IR object (all IR-04 fields: artifactType, targetProfile, brandId, foundationRefs, sections, componentInstances, content, a11yRequirements, validationStatus, version literal) parses successfully via the Zod schema.
    - Test: omitting any required IR-04 field fails `safeParse`.
    - Test: a slot string containing `<div>`, `className=`, or `style=` fails validation (markup-free invariant — IR-02). Include the adversarial "just give me the HTML" fixture: an IR whose content tries to smuggle markup is rejected.
    - Test: the schema exposes NO field named `rawHtml`, `html`, `tag`, `dangerouslySetInnerHTML`, or a free string-of-markup `children` (assert by attempting to parse objects carrying those keys → stripped/rejected, and by a static key audit).
    - Test: `outputProfileTable` returns the expected valid profile list for `social-post` (IG square/portrait/story/carousel) and `web-ui` (desktop/mobile/responsive); an invalid type→profile pair is reported invalid (D-03).
    - Test: the artifact/card union enumerates all 13 members (8 artifact types + foundation-profile/component-reference/evaluation-report/variant-group/export) (D-05).
  </behavior>
  <action>
    Create `src/ir/artifactTypes.ts` — the 8 artifact-type enum (web-ui, app-screen, dashboard, social-post, instagram-carousel, outdoor-display, slide, image) plus the canvas card-kind union adding foundation-profile, component-reference, evaluation-report, variant-group, export (D-05). Export as Zod enums + inferred TS types.
    Create `src/ir/schema.ts` — the `JioExperienceIR` Zod schema. Slot values are `z.union([z.array(z.lazy(() => JioIRComponentInstance)), z.string()])` ONLY; add a string refinement rejecting `<`, `className`, `style=`, `dangerouslySetInnerHTML` (the markup-free guard). Capture all IR-04 fields: artifactType, targetProfile, brandId, foundationRefs, sections (layout sections), componentInstances, content, a11yRequirements, validationStatus, and a `version` literal (mirror `ASTRoot.version`). NEVER add rawHtml/html/tag fields.
    Create `src/profiles/outputProfileTable.ts` — a typed const table mapping each artifact type → its valid output profiles + per-profile dimensions/aspect/safe-area/export-rule placeholders (the one honest new foundation concept; web profiles real, non-web structurally present for P4). Export a `getValidProfilesForType(type)` and an `isValidTypeProfilePair(type, profile)` helper (D-03 UI filtering source).
    Write the Wave 0 tests `src/ir/schema.test.ts`, `src/ir/artifactTypes.test.ts`, `src/profiles/outputProfileTable.test.ts` per the behavior block. Export all from `src/index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-core test --run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/experience-builder-core test` passes including the adversarial markup-rejection test.
    - `grep -nE "rawHtml|dangerouslySetInnerHTML|\\btag\\b" packages/experience-builder-core/src/ir/schema.ts` returns no markup-bearing field definitions (only refinement strings that REJECT them are allowed).
    - `outputProfileTable` covers all 8 artifact types (test asserts coverage count).
  </acceptance_criteria>
  <done>The markup-free IR schema, full 13-member object model, and artifact-type→profile table exist and are covered by passing Wave 0 tests including the "give me HTML" red-team fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Contract types, IR↔AST mapper, JSON-patch diff/apply + Wave 0 tests</name>
  <read_first>
    - packages/shared/src/codegen/astToReact.ts (lines 1-21 module header + pure-fn convention; lines 58-79 recursive node-switch — note the `element` branch the IR mapper must NEVER emit)
    - packages/shared/src/engine/buildThemeConfig.ts (lines 26-51: ThemeConfigAppearanceInput; the ThemeConfig output shape the resolve result must match)
    - packages/ui/src/registry/jioAlphaCatalog.ts (JioAlphaComponentCatalogEntry — basis for JioComponentRegistryItem)
    - apps/platform/src/app/api/composition/critique (result-shape reference for JioValidationResult)
    - .planning/phases/01-isolated-foundation/01-PATTERNS.md (§ irToAst.ts; § foundationResolve.ts gap variant; § registryItem.ts; § contracts/validation.ts; § No Analog Found — events.ts)
    - .planning/phases/01-isolated-foundation/01-RESEARCH.md (§ Architecture Patterns — Pattern 1; Pitfall 6 gap variant; events AG-UI-inspired)
  </read_first>
  <behavior>
    - Test: `irToAst(validIr)` returns an `ASTRoot` whose nodes are exclusively `kind: 'component'` or `kind: 'text'` — assert NO node has `kind: 'element'` (IR-03, Pitfall 2). A layout primitive in the IR maps to a registry ComponentASTNode (Container/Grid/Surface), never a raw tag.
    - Test: `diffIr(a, b)` then `applyPatch(a, patch)` deep-equals `b` (JSON-patch round-trip, IR-03).
    - Test: `FoundationResolveResult` discriminates on `ok` — the `ok: false` gap variant carries `{ artifactType, outputProfile, reason }` and never any dimension numbers (FND-03).
    - Test: a `JioComponentRegistryItem` parses with importPath/props/variants/slots fields (REG-01 shape conformance).
    - Test: `JioValidationResult` carries passed + blocking[] + warnings[] + repairSuggestions[] + gaps[] (VAL-01).
    - Test: the `ExperienceBuilderEvent` union round-trips each variant (run-started, step, ir-produced, validation, gap, run-completed) through Zod parse (ORCH-03).
  </behavior>
  <action>
    Create `src/contracts/foundationResolve.ts` — `FoundationResolveInput` + `FoundationResolveResult` as a discriminated union: `{ ok: true; theme: <ThemeConfig-shaped> }` | `{ ok: false; gap: { artifactType, outputProfile, reason } }` (FND-03; the gap short-circuits to the gap-report card — never invent round numbers like 1080).
    Create `src/contracts/registryItem.ts` — `JioComponentRegistryItem` derived from `JioAlphaComponentCatalogEntry` (id, importPath, props schema, variants, slots, states, usage rules, anti-patterns, supported brands/profiles, token dependencies) (REG-01).
    Create `src/contracts/validation.ts` — `JioValidationResult` = { passed: boolean; blocking: Violation[]; warnings: Warning[]; repairSuggestions: string[]; componentGaps: …; foundationGaps: … } (VAL-01).
    Create `src/contracts/events.ts` — the `ExperienceBuilderEvent` Zod discriminated union (AG-UI-inspired; no in-repo analog) covering run lifecycle + per-step + ir-produced + validation-result + gap + completion events; this rides the `@mastra/ai-sdk` transport in plan 04 (ORCH-03).
    Create `src/ir/irToAst.ts` — pure Node fn (mirror `astToReact.ts` module convention) mapping `JioExperienceIR` → `ASTRoot` of `ComponentASTNode`/`TextASTNode` ONLY (import types from `@oneui/shared`). The recursive switch has NO `element` case — that absence IS the enforcement (Pitfall 2). Layout sections/primitives map to named registry components.
    Create `src/ir/patch.ts` — `diffIr(a,b)` and `applyPatch(ir, patch)` JSON-patch over the IR (IR-03).
    Write Wave 0 tests `src/ir/irToAst.test.ts`, `src/ir/patch.test.ts`, and contract-shape tests per the behavior block. Export all contracts + mappers from `src/index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-core test --run 2>&1 | tail -15; pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All `@oneui/experience-builder-core` tests pass; `pnpm typecheck` is clean.
    - `grep -nE "kind:\s*'element'|kind:\s*\"element\"" packages/experience-builder-core/src/ir/irToAst.ts` returns nothing (no element-node emission).
    - The irToAst test asserts every produced node `kind` ∈ {component, text}.
    - `FoundationResolveResult` gap variant test confirms no numeric dimension is present on a gap.
    - `src/index.ts` re-exports schema, artifactTypes, outputProfileTable, all four contracts, irToAst, patch.
  </acceptance_criteria>
  <done>All four contract types, the registry-safe IR↔AST mapper, and IR JSON-patch exist with passing tests; the contracts package is the frozen, importable foundation for plans 02–05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → IR schema (future P2) | IR is untrusted generated content; the Zod schema is the input-validation boundary that must reject markup before any compile. |
| Lab packages ↔ existing Builder | One-way isolation: Lab code must not be importable by the existing Builder, and must not deep-import Builder internals. |
| npm registry → workspace install | Net-new Mastra packages enter the dependency tree (installed in plan 04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering / Elevation | IR schema (`schema.ts`) markup smuggling | mitigate | Markup-free slot union + string refinement rejecting `<`/`className`/`style=`/`dangerouslySetInnerHTML`; adversarial "give me HTML" Wave 0 fixture (IR-02). |
| T-01-02 | Tampering / Elevation | `irToAst.ts` emitting raw element nodes | mitigate | Mapper emits only `ComponentASTNode`/`TextASTNode`; no `element` case; grep gate asserts no `kind:'element'` (IR-03, Pitfall 2). |
| T-01-03 | Denial of Service (to existing app) | Isolation leakage via cross-imports | mitigate | eslint `no-restricted-imports` Lab↔Builder patterns; single-`ai`-version gate scoped to Lab subtree (LAB-03, Pitfall 3). |
| T-01-SC | Tampering | npm install of Mastra packages | mitigate | slopcheck unavailable → `[ASSUMED]`; blocking-human checkpoint (Task 1) verifies registry + postinstall before plan 04 installs; never auto-approvable. |
</threat_model>

<verification>
- `pnpm --filter @oneui/experience-builder-core test` green (IR-01..04, profile table, contracts, events).
- `pnpm typecheck` + `pnpm lint` clean.
- `pnpm tsx scripts/check-single-ai-version.ts` exits 0.
- git diff touches only `packages/experience-builder-core/`, `eslint.config.mjs`, `scripts/`, `package.json`, lockfile/workspace — no existing-Builder file.
</verification>

<success_criteria>
- Markup-free `JioExperienceIR` Zod schema + 13-member object model + output-profile table exist with passing red-team tests.
- All four contract types (foundationResolve with gap variant, registryItem, validation, events) and the registry-safe IR↔AST mapper + JSON-patch are exported.
- Isolation eslint boundary + single-`ai` gate are wired into `ci:gates`.
- The Mastra-install human-verify checkpoint is cleared with an exact pinned version pair recorded for plan 04.
</success_criteria>

<output>
Create `.planning/phases/01-isolated-foundation/01-01-SUMMARY.md` when done. Record the exact pinned `@mastra/core` + `@mastra/ai-sdk` versions from Task 1 for plan 04.
</output>
