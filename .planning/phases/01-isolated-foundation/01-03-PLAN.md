---
phase: 01-isolated-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/experience-builder-validation/package.json
  - packages/experience-builder-validation/tsconfig.json
  - packages/experience-builder-validation/vitest.config.ts
  - packages/experience-builder-validation/src/index.ts
  - packages/experience-builder-validation/src/astValidator.ts
  - packages/experience-builder-validation/src/astValidator.test.ts
  - packages/experience-builder-validation/src/fixtures/redteam.ts
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03]
user_setup: []

must_haves:
  truths:
    - "The validator operates on the AST (parsed, alias-resolved), never string denylists (VAL-05 skeleton, Pitfall 4)"
    - "A non-Jio import (incl. aliased `import { Button as X } from 'shadcn'`) is blocked (VAL-02)"
    - "A component not in the registry, or an invalid prop/variant/slot, is blocked (VAL-03)"
    - "The validator returns a JioValidationResult (passed + blocking + warnings + repairSuggestions + gaps) (VAL-01)"
  artifacts:
    - path: "packages/experience-builder-validation/src/astValidator.ts"
      provides: "AST allowlist compliance validator returning JioValidationResult"
      contains: "validateAst"
    - path: "packages/experience-builder-validation/src/fixtures/redteam.ts"
      provides: "aliased-import + markup red-team seed corpus"
  key_links:
    - from: "packages/experience-builder-validation/src/astValidator.ts"
      to: "@oneui/experience-builder-registry queryRegistry"
      via: "membership check against the allowlist"
      pattern: "queryRegistry|getRegistryItem"
    - from: "packages/experience-builder-validation/src/astValidator.ts"
      to: "@oneui/experience-builder-core JioValidationResult"
      via: "import type"
      pattern: "JioValidationResult"
---

<objective>
Build `@oneui/experience-builder-validation` — the basic AST-level compliance validator that is the security boundary for the Core Value: it walks the AST (parsed, with import aliases resolved), checks every import path against the Jio catalog allowlist and every component against the registry, validates props/variants/slots against meta, and returns a `JioValidationResult`. This is the validation slice; per Pitfall 4 it is AST-based (never regex/substring) from day one so P3 extends the walk rather than rewrites it.

Purpose: Prove the system blocks Tailwind / non-Jio imports / unregistered components / invalid props at the AST level (VAL-01/02/03). The full literal/token scan + red-team corpus is P3 (VAL-04/05/06), but the AST-walk skeleton + an aliased-import red-team seed land now.
Output: A pure-TS validator package consuming the plan-01 contracts + plan-02 registry, with Vitest node-env tests over allowlist + red-team fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-isolated-foundation/01-RESEARCH.md
@.planning/phases/01-isolated-foundation/01-PATTERNS.md
@.planning/phases/01-isolated-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold validation package + AST allowlist validator + red-team seed</name>
  <read_first>
    - packages/ui/src/runtime/ASTRenderer.tsx (lines 16-86: LEAF_COMPONENT_ALLOWED_PROPS, ALWAYS_ALLOWED_PREFIXES — the prop-allowlist enforcement the validator complements AST-side)
    - packages/shared/src/types/componentAST.ts (ComponentASTNode/TextASTNode/ElementASTNode — the validator must FLAG any element node / non-registry type)
    - packages/shared/src/engine/tokenManifest.ts + tokenBoundary.ts (the 22-family token allowlist; literal/token-boundary check source — skeleton now, full scan P3)
    - packages/experience-builder-core/src/contracts/validation.ts (JioValidationResult shape from plan 01)
    - packages/experience-builder-registry/src/queryRegistry.ts (membership source from plan 02)
    - .planning/phases/01-isolated-foundation/01-PATTERNS.md (§ astValidator.ts — allowlist enforcement, parse-not-substring)
    - .planning/phases/01-isolated-foundation/01-RESEARCH.md (Pitfall 4; § AST Validator; Security Domain threat table)
  </read_first>
  <behavior>
    - Test: a valid AST (only registry components + text, only `@oneui/ui/components/*` imports, valid props/variants) → `passed: true`, zero blocking.
    - Test: an import from `tailwindcss`/`shadcn`/any non-`@oneui` path → blocked (VAL-02); the aliased form `import { Button as X } from 'shadcn'` is ALSO blocked (alias resolution, not substring — Pitfall 4).
    - Test: an `ElementASTNode` (raw `tag: 'div'`) anywhere in the AST → blocked (markup smuggling, defence-in-depth with plan 01's IR guard).
    - Test: a `ComponentASTNode` with `type` not in the registry → component gap + blocked (VAL-03).
    - Test: a registry component with an invalid prop/variant/slot (not in meta allowlist) → blocked with a repair suggestion (VAL-03).
    - Test: the result always conforms to `JioValidationResult` (passed + blocking[] + warnings[] + repairSuggestions[] + componentGaps[] + foundationGaps[]).
    - Test (red-team seed): each fixture in `redteam.ts` (aliased import, raw element node, unregistered component) is caught.
  </behavior>
  <action>
    Create package `@oneui/experience-builder-validation` at `packages/experience-builder-validation/` with `package.json` (deps: `@oneui/experience-builder-core` + `@oneui/experience-builder-registry` workspace; `@oneui/shared` for tokenManifest; `scripts.test` = `vitest run`), `tsconfig.json`, `vitest.config.ts` (node env). Run `pnpm install`.
    Create `src/astValidator.ts` — `validateAst(ast, ctx)` that WALKS the AST (parse/traverse, resolve import aliases — never substring match): (1) every import path ∈ catalog allowlist (`@oneui/ui/components/*`) else blocking VAL-02; (2) every `ComponentASTNode.type` ∈ registry (via plan-02 `getRegistryItem`) else component gap + blocking VAL-03; (3) any `ElementASTNode`/raw tag → blocking (markup); (4) every prop/variant/slot ∈ meta allowlist for that component else blocking + repair suggestion VAL-03; (5) wire a tokenManifest/tokenBoundary literal-check HOOK (skeleton — full scan deferred to P3 VAL-04). Return `JioValidationResult`. Reuse the `ASTRenderer` prop-allowlist philosophy AST-side.
    Create `src/fixtures/redteam.ts` — the seed evasion corpus: aliased non-Jio import, raw `element` node, unregistered component, invalid prop (the P3 corpus extends this).
    Write `src/astValidator.test.ts` per the behavior block. Export `validateAst` from `src/index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-validation test --run 2>&1 | tail -15; pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/experience-builder-validation test` passes including every red-team seed fixture.
    - The validator parses/traverses (no `.includes(`/`.indexOf(` substring denylist as the import check): `grep -nE "\.includes\(|\.indexOf\(|RegExp\(" packages/experience-builder-validation/src/astValidator.ts` shows no import-allowlist decision made via raw substring (token-boundary regex from the shared manifest is acceptable; the import/component decision must be structural).
    - Aliased-import test (`Button as X` from a non-Jio path) is blocked.
    - Result conforms to `JioValidationResult` in every branch.
  </acceptance_criteria>
  <done>The AST-based validator blocks non-Jio imports (including aliased), raw element nodes, unregistered components, and invalid props/variants/slots, returning a `JioValidationResult`, with an extensible red-team seed corpus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compiled artifact AST → validator | The validator is the AST-level security boundary enforcing the Jio-DS-only Core Value before an artifact is marked ready. |
| Import statements → catalog allowlist | Aliased / obfuscated imports must be resolved structurally, not string-matched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | Aliased non-Jio import evading the check | mitigate | AST parse + alias resolution (never substring); red-team seed fixture asserts `Button as X` from shadcn is blocked (VAL-02, Pitfall 4). |
| T-01-08 | Tampering / Elevation | Raw element node / markup reaching output | mitigate | Validator flags any `ElementASTNode`/raw tag as blocking — defence-in-depth with the IR markup-free guard (VAL-03). |
| T-01-09 | Tampering | Unregistered component or invalid prop emitted | mitigate | Registry membership (exact) + meta prop/variant/slot allowlist → blocking + repair suggestion (VAL-03). |
| T-01-10 | Repudiation | Validation result not auditable | mitigate | Every run returns a structured `JioValidationResult` (passed + blocking + warnings + suggestions + gaps) (VAL-01). |
</threat_model>

<verification>
- `pnpm --filter @oneui/experience-builder-validation test` green (VAL-01/02/03 + red-team seed).
- `pnpm typecheck` clean.
- Import/component decisions are structural (AST), not substring.
- git diff touches only `packages/experience-builder-validation/` + lockfile.
</verification>

<success_criteria>
- AST validator blocks Tailwind/non-Jio imports (incl. aliased), unregistered components, raw element nodes, and invalid props/variants/slots.
- Returns `JioValidationResult` in all branches.
- Red-team seed corpus passes; the AST-walk skeleton is ready for P3 extension.
</success_criteria>

<output>
Create `.planning/phases/01-isolated-foundation/01-03-SUMMARY.md` when done.
</output>
