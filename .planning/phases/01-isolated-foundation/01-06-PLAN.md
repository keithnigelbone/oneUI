---
phase: 01-isolated-foundation
plan: 06
type: execute
wave: 5
depends_on: ["01-04", "01-05"]
files_modified:
  - apps/platform/src/app/(platform)/(experience-lab)/_panels/RequestPanel.tsx
  - apps/platform/src/app/(platform)/(experience-lab)/_panels/RequestPanel.module.css
  - apps/platform/src/app/(platform)/(experience-lab)/_panels/RunInspectorPanel.tsx
  - apps/platform/src/app/(platform)/(experience-lab)/_panels/RunInspectorPanel.module.css
  - apps/platform/src/app/(platform)/(experience-lab)/__tests__/requestPanel.test.tsx
  - packages/experience-builder-preview/package.json
  - packages/experience-builder-preview/PREVIEW-DECISION.md
  - packages/experience-builder-export/package.json
  - packages/experience-builder-core/README.md
  - packages/experience-builder-core/FOUNDATION-COVERAGE.md
  - scripts/smoke-existing-builder.ts
  - package.json
autonomous: true
requirements: [LAB-02, LAB-03, LAB-04, INPUT-01, INPUT-02, INPUT-03, INPUT-04, ORCH-03, FND-01]
user_setup: []

must_haves:
  truths:
    - "A docked request panel edits the currently-selected prompt card; brand/type/profile/prompt persist on the card (D-01/D-04/INPUT-01..04)"
    - "The brand Select populates from the real read-only Convex brands query (D-02/INPUT-01)"
    - "The output-profile Select is filtered by the selected artifact type — invalid pairings unselectable (D-03/INPUT-02/INPUT-03)"
    - "A docked run-inspector panel streams the ExperienceBuilderEvent timeline (ORCH-03)"
    - "An existing-Builder smoke test confirms the (builder) route still boots (LAB-03)"
    - "FOUNDATION-COVERAGE.md documents which non-web profiles Jio defines vs assumes (FND-01)"
    - "PREVIEW-DECISION.md documents the separate-origin preview hosting model"
    - "A README documents how the Lab stays isolated + how to run it (LAB-04)"
  artifacts:
    - path: "apps/platform/src/app/(platform)/(experience-lab)/_panels/RequestPanel.tsx"
      provides: "docked request panel editing the selected prompt card"
      contains: "useQuery"
    - path: "apps/platform/src/app/(platform)/(experience-lab)/_panels/RunInspectorPanel.tsx"
      provides: "docked event-timeline panel"
      contains: "ExperienceBuilderEvent"
    - path: "packages/experience-builder-core/FOUNDATION-COVERAGE.md"
      provides: "non-web foundation-coverage audit"
    - path: "packages/experience-builder-preview/PREVIEW-DECISION.md"
      provides: "separate-origin preview hosting decision"
    - path: "scripts/smoke-existing-builder.ts"
      provides: "existing-Builder boot smoke test (isolation gate)"
    - path: "packages/experience-builder-core/README.md"
      provides: "Lab isolation + run docs (LAB-04)"
  key_links:
    - from: "apps/platform/src/app/(platform)/(experience-lab)/_panels/RequestPanel.tsx"
      to: "api.brands.list (read-only Convex)"
      via: "useQuery"
      pattern: "useQuery"
    - from: "apps/platform/src/app/(platform)/(experience-lab)/_panels/RequestPanel.tsx"
      to: "@oneui/experience-builder-core outputProfileTable"
      via: "type-filtered profile options (D-03)"
      pattern: "outputProfileTable|getValidProfilesForType"
    - from: "scripts/smoke-existing-builder.ts"
      to: "(builder) canvas route"
      via: "boot smoke test in CI"
      pattern: "builder"
---

<objective>
Build the docked request + run-inspector panels (D-01) that complete the request-input slice (INPUT-01..04) and surface the event stream (ORCH-03), add the "Experience Lab" nav entry, ship the LAB-04 README + the two HIGH-severity P1 documentation deliverables (FOUNDATION-COVERAGE.md audit, PREVIEW-DECISION.md), stub the `-preview` and `-export` packages, and land the existing-Builder smoke test that completes the LAB-03 isolation gate. This is the final slice: it turns the bare canvas round-trip into the full configure → run → inspect experience and closes every P1 contract + risk deliverable.

Purpose: Complete the request UX (brand from real Convex query D-02, type→profile filtering D-03, prompt + config persisted on the card D-04), make the live event timeline visible (ORCH-03), and discharge the front-loaded HIGH-severity P1 decisions (foundation-coverage audit FND-01/Pitfall 6; separate-origin preview decision; isolation smoke test LAB-03/Pitfall 3).
Output: Two Jio-DS-only docked panels, the nav entry, three docs, two stub packages, the smoke-test gate wired into CI, with jsdom tests for selection-driven editing + type→profile filtering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-isolated-foundation/01-CONTEXT.md
@.planning/phases/01-isolated-foundation/01-UI-SPEC.md
@.planning/phases/01-isolated-foundation/01-PATTERNS.md
@.planning/phases/01-isolated-foundation/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docked request panel + run-inspector panel (selection-driven, real brands, type→profile filtering) + nav entry</name>
  <read_first>
    - apps/platform/src/design-tools/ExperienceCanvas/PropPanel.tsx (lines 9-33 Jio-only deep-path imports; panel structure to MIRROR, never import)
    - apps/platform/src/design-tools/ExperienceCanvas/useCanvasEditor.ts (lines 31-70: getSelectedComponentInfo + updateShapeProp selection-driven editing)
    - packages/convex/convex/brands.ts (lines 24-26: read-only `list` query — D-02, reused as-is)
    - packages/experience-builder-core/src/profiles/outputProfileTable.ts (getValidProfilesForType / isValidTypeProfilePair — D-03 filter source)
    - packages/experience-builder-core/src/ir/artifactTypes.ts (the 8 artifact types for the type Select)
    - packages/experience-builder-core/src/contracts/events.ts (ExperienceBuilderEvent — the inspector timeline)
    - apps/platform/src/app/(platform)/(experience-lab)/_canvas/ExperienceLabCanvas.tsx (the shell + Run flow from plan 05 to dock these panels into)
    - apps/platform/src/app/(platform)/layout.tsx + _layout (nav structure to add a sibling "Experience Lab" entry, distinct from "Builder")
    - .planning/phases/01-isolated-foundation/01-UI-SPEC.md (§ Component Inventory; § Copywriting Contract — exact section labels + empty states; § Color — accent reserved for the single Run CTA; § Typography)
    - .planning/phases/01-isolated-foundation/01-PATTERNS.md (§ RequestPanel.tsx; § RunInspectorPanel.tsx; § Focus Halo; § Surface Context)
    - CLAUDE.md (Surface Context, Typography V2, Focus Halo, deep-import boundary)
  </read_first>
  <action>
    Create `_panels/RequestPanel.tsx` + `.module.css` — a docked panel (mirror `PropPanel.tsx`) editing the CURRENTLY-SELECTED prompt card (read `editor.getSelectedShapeIds()`, persist config back via the shape-prop update pattern, D-01/D-04). Controls (Jio deep-path imports only): brand `Select` populated from `useQuery(api.brands.list)` (D-02/INPUT-01); artifact-type `Select` from the 8-type enum (INPUT-02); output-profile `Select` whose options come from `getValidProfilesForType(selectedType)` so invalid pairings are unselectable at the UI (D-03/INPUT-03); a multi-line prompt `Input`/`InputField` mirroring the card text (INPUT-04). The single accent moment is `<Button variant="bold" appearance="primary">Run generation</Button>` triggering the plan-05 Run flow. Use exact UI-SPEC copy ("Brand", "Artifact type", "Output profile", "Prompt"). Selected secondary controls default to `secondary` appearance per design-composition. Focus halo on every control.
    Create `_panels/RunInspectorPanel.tsx` + `.module.css` — a docked panel rendering the active run's `ExperienceBuilderEvent` timeline (ListItem + Badge + Spinner; `informative` for in-progress), with the exact empty-state copy ("No run yet. Select a prompt card and run a generation to see the live event timeline."). `bg-subtle` Surface (UI-SPEC Color secondary). Dock both panels into `ExperienceLabCanvas.tsx`.
    Add the "Experience Lab" nav entry to the platform nav (sibling to "Builder", visibly distinct — Claude's-discretion entry-point), pointing at the `(experience-lab)` route.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -8; pnpm check:literals 2>&1 | tail -5; pnpm check:layers 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `RequestPanel.tsx` calls `useQuery` against the brands list and derives profile options from `getValidProfilesForType`/`outputProfileTable` (grep match for both).
    - The single primary `Button variant="bold" appearance="primary"` appears exactly once (the Run CTA); no `@oneui/ui` barrel import (grep: 0 for `from '@oneui/ui'`).
    - `pnpm check:literals` + `pnpm typecheck` clean; `pnpm lint` passes the Lab↔Builder boundary.
    - `RunInspectorPanel.tsx` references `ExperienceBuilderEvent` and uses `bg-subtle` Surface.
    - Nav entry "Experience Lab" added, pointing at the `(experience-lab)` route, distinct from "Builder".
  </acceptance_criteria>
  <done>The docked request panel (real brands, type-filtered profiles, prompt, single Run CTA) edits the selected prompt card and the run-inspector panel streams the event timeline; the nav exposes a distinct "Experience Lab" entry.</done>
</task>

<task type="auto">
  <name>Task 2: jsdom tests for selection-driven editing + type→profile filtering + existing-Builder smoke test (LAB-03)</name>
  <read_first>
    - apps/platform/src/app/(platform)/(experience-lab)/_panels/RequestPanel.tsx (from Task 1)
    - apps/platform/src/app/(platform)/(builder)/canvas/page.tsx (the existing route the smoke test must boot)
    - scripts/check-single-ai-version.ts (plan-01 gate — the smoke test joins it in ci:gates)
    - package.json (ci:gates chain to extend)
    - .planning/phases/01-isolated-foundation/01-VALIDATION.md (INPUT-01..04 rows; LAB-03 row — existing-Builder smoke test; Manual-Only Verifications)
    - .planning/phases/01-isolated-foundation/01-RESEARCH.md (Pitfall 3 — existing-Builder smoke test; § Isolation)
  </read_first>
  <action>
    Create `apps/platform/src/app/(platform)/(experience-lab)/__tests__/requestPanel.test.tsx` (jsdom): with a mocked editor + selected prompt card and a mocked `api.brands.list`, the panel renders brand options (INPUT-01); selecting an artifact type updates the output-profile options to exactly `getValidProfilesForType(type)` and an invalid pairing is not offered (D-03/INPUT-02/INPUT-03); editing brand/type/profile/prompt persists onto the selected card's props (D-04); the Run CTA invokes the run flow (INPUT-04).
    Create `scripts/smoke-existing-builder.ts` — boots/renders the existing `(builder)` canvas route and asserts it still mounts without error (LAB-03 / Pitfall 3: the existing Builder must keep booting unchanged). Wire it into root `package.json` as `smoke:builder` and append to the `ci:gates` chain alongside `check:single-ai`.
  </action>
  <verify>
    <automated>pnpm vitest run "apps/platform/src/app/(platform)/(experience-lab)/__tests__/requestPanel.test.tsx" 2>&1 | tail -12; pnpm tsx scripts/smoke-existing-builder.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The requestPanel jsdom test passes: brand options render; type→profile filtering matches `getValidProfilesForType`; config persists on the card.
    - `scripts/smoke-existing-builder.ts` exits 0 (existing Builder still boots).
    - `package.json` `ci:gates` references both `smoke:builder` and `check:single-ai`.
  </acceptance_criteria>
  <done>Request-panel behaviour (real brands, type→profile filtering, card persistence) is covered by passing jsdom tests, and the existing-Builder smoke test completes the LAB-03 isolation gate in CI.</done>
</task>

<task type="auto">
  <name>Task 3: README (LAB-04) + foundation-coverage audit + separate-origin preview decision + preview/export stubs</name>
  <read_first>
    - apps/platform/src/app/internal/render-ast/page.tsx (server-side token handoff — the basis for the separate-origin preview decision; Pattern 4)
    - packages/shared/src/engine/buildThemeConfig.ts (what Jio foundations actually define — web/density/viewport-shaped; basis for the coverage audit)
    - packages/experience-builder-core/src/profiles/outputProfileTable.ts (the profiles whose coverage the audit documents)
    - packages/convex/package.json (sibling stub package.json shape)
    - .planning/phases/01-isolated-foundation/01-RESEARCH.md (§ Pattern 4 server-side handoff; Pitfall 6 + A3 foundation coverage; Open Questions #4/#5; Security Domain V12 sandbox controls)
    - .planning/phases/01-isolated-foundation/01-CONTEXT.md (§ specifics — separate preview origin decided in P1; coverage audit is a P1 deliverable)
    - .planning/phases/01-isolated-foundation/01-VALIDATION.md (Manual-Only Verifications — preview decision + coverage audit are documentation deliverables)
  </read_first>
  <action>
    Create `packages/experience-builder-core/README.md` (LAB-04) — documents the Lab's isolation model ("isolation by package, reuse by contract": the six `experience-builder-*` packages + the `(experience-lab)` route; MUST-NOT-TOUCH list = `ExperienceCanvas`/`(builder)`/`FoundationStyleProvider`; the eslint boundary + single-ai + smoke-test gates) and how to run it (route URL, `pnpm dev`, per-package `pnpm --filter ... test`).
    Create `packages/experience-builder-core/FOUNDATION-COVERAGE.md` (FND-01 / Pitfall 6 / A3) — enumerate which output profiles Jio foundations actually DEFINE (web: density/viewport/`data-Breakpoint` 3 breakpoints, confirmed) vs which the docs ASSUME (non-web: IG square/portrait/story, carousel, LinkedIn, slide, outdoor/banner — mark as gap-until-P4). State explicitly that uncovered profiles return a typed gap report, never invented dimensions. This is a first-class P1 deliverable, not a stub.
    Create `packages/experience-builder-preview/` stub package (`package.json` only + `PREVIEW-DECISION.md`) and `packages/experience-builder-export/` stub package (`package.json` only). `PREVIEW-DECISION.md` documents the separate-origin preview hosting model (Open Question #5): previews served from a DISTINCT origin, fed a server-resolved bundle via the `/internal/render-ast` token-handoff pattern (Pattern 4), `allow-scripts` WITHOUT `allow-same-origin`, strict CSP + `frame-ancestors`, `postMessage` origin checks, ZERO auth/Convex tokens in preview, credential-free Playwright workers — decision now, build P3. Run `pnpm install` to link the two stub packages.
  </action>
  <verify>
    <automated>pnpm install 2>&1 | tail -3; ls packages/experience-builder-core/README.md packages/experience-builder-core/FOUNDATION-COVERAGE.md packages/experience-builder-preview/PREVIEW-DECISION.md packages/experience-builder-export/package.json; pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All four doc/stub artifacts exist (README.md, FOUNDATION-COVERAGE.md, PREVIEW-DECISION.md, export/package.json).
    - `FOUNDATION-COVERAGE.md` lists web profiles as DEFINED and non-web profiles as ASSUMED/gap-until-P4, and states uncovered → typed gap (grep for "gap" + a non-web profile name).
    - `PREVIEW-DECISION.md` specifies separate origin + `allow-scripts` without `allow-same-origin` + no auth/Convex tokens (grep for "allow-same-origin").
    - The two stub packages link (`pnpm install` succeeds) and `pnpm typecheck` stays clean.
  </acceptance_criteria>
  <done>The LAB-04 README, the FND-01 foundation-coverage audit, the separate-origin preview decision, and the preview/export stub packages all exist — discharging every P1 documentation + HIGH-severity-decision deliverable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser request panel → Convex brands query | Read-only query (D-02); no write path, no isolation risk to the existing Builder's foundation injection. |
| Lab route ↔ existing Builder | The smoke test is the runtime proof the Lab cannot break the existing Builder. |
| Future preview iframe → host app | The PREVIEW-DECISION documents the separate-origin/no-token boundary built in P3. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-19 | Denial of Service (to existing app) | Lab panel/nav change breaking the existing Builder | mitigate | Existing-Builder smoke test in `ci:gates`; nav entry is additive; read-only brands query never touches `FoundationStyleProvider` (LAB-03, Pitfall 3). |
| T-01-20 | Tampering | Invalid type→profile pair submitted | mitigate | Output-profile Select offers only `getValidProfilesForType(type)` — invalid pairings unselectable at the UI (D-03). |
| T-01-21 | Information Disclosure | Future preview leaking auth/Convex tokens | accept (P1) / mitigate (P3) | PREVIEW-DECISION documents separate origin, `allow-scripts` without `allow-same-origin`, zero tokens in preview; build + security review is P3 (decision-only in P1). |
| T-01-22 | Tampering | Foundation resolver inventing non-web dimensions | mitigate | FOUNDATION-COVERAGE audit documents defined-vs-assumed profiles; uncovered → typed gap, never invented dimensions (FND-01/FND-03, Pitfall 6). |
</threat_model>

<verification>
- requestPanel jsdom test green (INPUT-01..04, D-03 filtering, D-04 persistence).
- `scripts/smoke-existing-builder.ts` exits 0; `ci:gates` references `smoke:builder` + `check:single-ai` (LAB-03).
- README.md + FOUNDATION-COVERAGE.md + PREVIEW-DECISION.md + export stub all exist.
- `pnpm check:literals` + `pnpm typecheck` clean for the new panels.
</verification>

<success_criteria>
- Docked request panel completes the request UX (real brands D-02, type→profile filtering D-03, prompt + card persistence D-04, single Run CTA) (INPUT-01..04).
- Run-inspector panel streams the event timeline (ORCH-03); nav exposes a distinct "Experience Lab" entry (LAB-01).
- LAB-04 README + FND-01 foundation-coverage audit + separate-origin preview decision shipped; preview/export stubs link.
- Existing-Builder smoke test completes the LAB-03 isolation gate.
</success_criteria>

<output>
Create `.planning/phases/01-isolated-foundation/01-06-SUMMARY.md` when done.
</output>
