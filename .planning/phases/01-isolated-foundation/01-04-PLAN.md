---
phase: 01-isolated-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - packages/experience-builder-agents/package.json
  - packages/experience-builder-agents/tsconfig.json
  - packages/experience-builder-agents/vitest.config.ts
  - packages/experience-builder-agents/src/index.ts
  - packages/experience-builder-agents/src/modelAdapter.ts
  - packages/experience-builder-agents/src/foundationResolver.ts
  - packages/experience-builder-agents/src/mockGeneration.ts
  - packages/experience-builder-agents/src/workflow.ts
  - packages/experience-builder-agents/src/workflow.test.ts
  - packages/experience-builder-agents/src/mockGeneration.test.ts
  - packages/experience-builder-agents/src/foundationResolver.test.ts
  - packages/convex/convex/schema.ts
  - packages/convex/convex/experienceRuns.ts
autonomous: true
requirements: [ORCH-01, ORCH-03, ORCH-04, GEN-01, GEN-08, FND-01, FND-03, VAL-02, VER-03]
user_setup:
  - service: mastra
    why: "Orchestration brain for the generation workflow (net-new dependency)"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "A Mastra workflow skeleton sequences intent(mock) -> resolve(mock) -> retrieve(mock) -> IR-gen(mock) -> validate(basic) (ORCH-01)"
    - "The workflow emits ExperienceBuilderEvents over the @mastra/ai-sdk v6 transport (ORCH-03)"
    - "No sequencing/repair/HITL logic lives in any AI SDK callback — Mastra owns orchestration (ORCH-04)"
    - "Mock generation produces a valid IR that passes the plan-01 schema and the plan-03 validator (GEN-08)"
    - "The mock foundation resolver returns a ThemeConfig-shaped result OR a typed gap report (FND-01/FND-03)"
    - "A generation run + its IR persist to the experience* Convex tables (VER-03)"
  artifacts:
    - path: "packages/experience-builder-agents/src/workflow.ts"
      provides: "Mastra workflow skeleton with mock steps + event emission"
      contains: "createWorkflow"
    - path: "packages/experience-builder-agents/src/mockGeneration.ts"
      provides: "deterministic mock IR generator producing valid IR"
      contains: "mockGenerate"
    - path: "packages/experience-builder-agents/src/foundationResolver.ts"
      provides: "mock resolver (ThemeConfig-shaped) with first-class gap variant"
      contains: "resolveFoundation"
    - path: "packages/convex/convex/experienceRuns.ts"
      provides: "run + IR persistence queries/mutations"
      contains: "mutation"
  key_links:
    - from: "packages/experience-builder-agents/src/workflow.ts"
      to: "@oneui/experience-builder-validation validateAst"
      via: "validation step"
      pattern: "validateAst"
    - from: "packages/experience-builder-agents/src/workflow.ts"
      to: "@oneui/experience-builder-core ExperienceBuilderEvent"
      via: "emitted event stream"
      pattern: "ExperienceBuilderEvent"
    - from: "packages/convex/convex/experienceRuns.ts"
      to: "experience* tables in schema.ts"
      via: "ctx.db.insert"
      pattern: "experienceRuns|experienceArtifacts"
---

<objective>
Build `@oneui/experience-builder-agents` — the Mastra orchestration brain — plus the Convex `experience*` persistence layer. This is the orchestration + generation slice: a Mastra workflow skeleton sequences mock steps (intent → resolve → retrieve → IR-gen → validate) emitting an `ExperienceBuilderEvent` stream over `@mastra/ai-sdk` v6, the mock generator produces a VALID IR that passes the plan-01 schema and the plan-03 validator, the mock foundation resolver returns a `ThemeConfig`-shaped result or a typed gap report, and runs + IR persist to three new Convex tables. After this plan, the full backend round-trip exists — the next plan wires it to the canvas.

Purpose: Wire the safest-possible Walking Skeleton backend — all edges deterministic mocks, no real LLM — proving orchestration ownership (ORCH-04), the event stream (ORCH-03), the valid-IR happy path (GEN-08), the gap path (FND-03), and durable persistence (VER-03). Pins Mastra to the exact versions cleared by the plan-01 checkpoint, behind one model adapter.
Output: A Node-runtime agents package + `experienceRuns`/`experienceArtifacts`/`experienceArtifactVersions` Convex tables with mutations, covered by Vitest integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-isolated-foundation/01-RESEARCH.md
@.planning/phases/01-isolated-foundation/01-PATTERNS.md
@.planning/phases/01-isolated-foundation/01-VALIDATION.md
@.planning/phases/01-isolated-foundation/01-01-SUMMARY.md
@.planning/phases/01-isolated-foundation/01-02-SUMMARY.md
@.planning/phases/01-isolated-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold agents package, install pinned Mastra, build model adapter + mock foundation resolver</name>
  <read_first>
    - .planning/phases/01-isolated-foundation/01-01-SUMMARY.md (the EXACT @mastra/core + @mastra/ai-sdk version pair cleared by the plan-01 human-verify checkpoint — install these, no `^`)
    - packages/shared/src/engine/buildThemeConfig.ts (lines 26-51 ThemeConfigAppearanceInput + ThemeConfig output — the resolver success shape MUST match, FND-01/FND-04)
    - packages/experience-builder-core/src/contracts/foundationResolve.ts (FoundationResolveResult discriminated union from plan 01)
    - packages/experience-builder-core/src/profiles/outputProfileTable.ts (profile coverage source for the gap decision)
    - .planning/phases/01-isolated-foundation/01-RESEARCH.md (Standard Stack install block; Pitfall 1 single-ai + version: 'v6'; Pitfall 6 resolver gap; § Architectural Responsibility Map — agents are Node/backend, never Edge)
    - .planning/phases/01-isolated-foundation/01-PATTERNS.md (§ foundationResolver.ts)
  </read_first>
  <action>
    Create package `@oneui/experience-builder-agents` at `packages/experience-builder-agents/` with `package.json` (deps: the three Lab workspace packages + `@oneui/shared`; `scripts.test` = `vitest run`), `tsconfig.json`, `vitest.config.ts` (node env).
    Install Mastra INSIDE this package only (run from `packages/experience-builder-agents/`): `pnpm add @mastra/core@<EXACT> @mastra/ai-sdk@<EXACT>` using the exact pair recorded in `01-01-SUMMARY.md` — no `^`, no repo-root install. Do NOT re-add `ai`/`@ai-sdk/*`/`zod` (already present). After install, run `pnpm tsx scripts/check-single-ai-version.ts` (from plan 01) to confirm the Lab subtree still resolves a single `ai@6`.
    Create `src/modelAdapter.ts` — the ONE module that owns all AI SDK access (centralize per Pitfall 1). P1 has no real model calls; the adapter exposes a typed boundary + a `version: 'v6'` stream-conversion helper for `@mastra/ai-sdk`. No orchestration/sequencing/repair/HITL logic here (ORCH-04 boundary).
    Create `src/foundationResolver.ts` — `resolveFoundation(input): FoundationResolveResult`. On a covered (web) profile, return `{ ok: true, theme }` shaped to `ThemeConfig`. On an uncovered profile, return `{ ok: false, gap: { artifactType, outputProfile, reason } }` — NEVER invent dimensions/round numbers (Pitfall 6 / FND-03). Coverage is determined from the plan-01 output-profile table.
    Write `src/foundationResolver.test.ts`: covered profile → ThemeConfig-shaped ok; uncovered profile → typed gap with no numeric dimensions.
  </action>
  <verify>
    <automated>pnpm tsx scripts/check-single-ai-version.ts; pnpm --filter @oneui/experience-builder-agents test --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `@mastra/core` and `@mastra/ai-sdk` appear in `packages/experience-builder-agents/package.json` with EXACT versions (no `^`/`~`) matching `01-01-SUMMARY.md`.
    - `pnpm tsx scripts/check-single-ai-version.ts` exits 0 after install (single `ai@6` in Lab subtree).
    - `grep -c "version: 'v6'\|version: \"v6\"" packages/experience-builder-agents/src/modelAdapter.ts` ≥ 1.
    - Resolver test: uncovered profile returns a gap with no numeric dimension field present.
  </acceptance_criteria>
  <done>The agents package exists, Mastra is installed at the cleared exact pins with a single `ai` version preserved, the model adapter owns the AI-SDK boundary, and the mock resolver returns ThemeConfig-shaped success or a typed gap.</done>
</task>

<task type="auto">
  <name>Task 2: Mock IR generator + Mastra workflow skeleton with ExperienceBuilderEvent stream</name>
  <read_first>
    - apps/platform/src/app/api/agent/executors (executor boundary shape — nearest in-repo analog for a step body)
    - packages/experience-builder-core/src/ir/schema.ts (the valid-IR target — mockGenerate output must parse)
    - packages/experience-builder-core/src/contracts/events.ts (ExperienceBuilderEvent union to emit)
    - packages/experience-builder-registry/src/queryRegistry.ts (retrieve step source)
    - packages/experience-builder-validation/src/astValidator.ts (validate step)
    - packages/experience-builder-core/src/ir/irToAst.ts (IR -> AST before validation)
    - .planning/phases/01-isolated-foundation/01-RESEARCH.md (§ Architecture Patterns — Pattern 1 deterministic edges; § System Architecture Diagram — workflow step order; Pitfall 1 version:'v6'; ORCH-04)
    - .planning/phases/01-isolated-foundation/01-PATTERNS.md (§ No Analog Found — workflow.ts net-new; § events.ts)
    - .planning/phases/01-isolated-foundation/01-VALIDATION.md (ORCH-01/03/04 + GEN-08 rows)
  </read_first>
  <action>
    Create `src/mockGeneration.ts` — `mockGenerate(input)`: a DETERMINISTIC mock that hand-shapes a VALID `JioExperienceIR` from retrieved (mock) registry components + resolved (mock) foundation, for a covered request. It uses only registry-approved component ids so the produced IR → AST → validator passes. No LLM. For an uncovered profile / unregistered-component request, it short-circuits and returns the gap (no IR), mirroring "missing profile/component → gap report, never the repair loop."
    Create `src/workflow.ts` — a Mastra `createWorkflow()` skeleton sequencing steps: intent(mock GEN-01) → resolve (calls `resolveFoundation`) → retrieve (calls `queryRegistry`) → IR-gen (calls `mockGenerate`) → validate (irToAst then `validateAst`). Each step EMITS the matching `ExperienceBuilderEvent` (run-started, per-step, ir-produced, validation-result, gap, run-completed) over the `@mastra/ai-sdk` v6 transport via the model adapter helper. ALL orchestration (sequencing, branching to the gap short-circuit, completion) lives in the Mastra workflow — never in an AI SDK callback (ORCH-04). On a gap, branch to emit a gap event and complete WITHOUT producing an artifact.
    Write `src/mockGeneration.test.ts` (GEN-08: output parses against the IR schema AND passes the validator) and `src/workflow.test.ts` (ORCH-01: runs end-to-end emitting the expected event sequence; gap input branches to a gap event + no artifact; assert no orchestration call sits inside an AI-SDK callback). Export from `src/index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-agents test --run 2>&1 | tail -15; pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `mockGenerate` output `safeParse`s against `JioExperienceIR` AND `validateAst(irToAst(ir))` returns `passed: true` (GEN-08 test green).
    - The workflow test asserts the full event sequence is emitted and a gap input yields a gap event + zero artifacts (ORCH-01/03).
    - `grep -c "createWorkflow" packages/experience-builder-agents/src/workflow.ts` ≥ 1.
    - No sequencing/branch logic in any `@ai-sdk` callback (test/structural assertion; ORCH-04).
  </acceptance_criteria>
  <done>The Mastra workflow skeleton runs the mock pipeline end-to-end, emits the `ExperienceBuilderEvent` stream, branches correctly on gaps, and produces a validator-passing valid IR — with orchestration owned entirely by Mastra.</done>
</task>

<task type="auto">
  <name>Task 3: Convex experience* tables + run/IR persistence (VER-03)</name>
  <read_first>
    - packages/convex/convex/schema.ts (lines 4-24: defineTable conventions, createdAt/updatedAt, .index pattern — APPEND ONLY, do not modify existing tables)
    - packages/convex/convex/brands.ts (lines 13-49: query/mutation with args validators — D-02 list query is read-only, reused untouched)
    - packages/experience-builder-core/src/ir/schema.ts (IR shape persisted per artifact version)
    - packages/experience-builder-core/src/contracts/events.ts (run event/state persisted with the run)
    - .planning/phases/01-isolated-foundation/01-PATTERNS.md (§ experienceRuns.ts + new schema tables)
    - .planning/phases/01-isolated-foundation/01-CONTEXT.md (D-08: runs + IR durable; canvas layout ephemeral)
    - .planning/phases/01-isolated-foundation/01-RESEARCH.md (§ Runtime State Inventory — append tables, run `npx convex dev`)
  </read_first>
  <action>
    Extend `packages/convex/convex/schema.ts` — APPEND three tables (do NOT modify existing defs): `experienceRuns` (run state + event log + originating prompt-card config + status), `experienceArtifacts` (artifact identity + type/profile/brand + current version pointer), `experienceArtifactVersions` (the IR JSON + validation result + parent version + originating run id) — D-08 / VER-03. Add `.index` entries by run id / artifact id following the existing convention. Persist the IR as a JSON value (not markup). Canvas layout is NOT persisted (D-08).
    Create `packages/convex/convex/experienceRuns.ts` — `mutation`s to create a run, append events / update run status, and persist an artifact + version; `query`s to read a run with its event state and to read an artifact's version history (the latter shapes VER-01/VER-02 for P3, but P1 only needs run + IR persisted). Follow `brands.ts` `query`/`mutation` + `args` validator conventions.
    Run `npx convex dev` once (or `npx convex codegen`) to regenerate `_generated` types so the new tables/functions typecheck.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -8; grep -c "experienceRuns\|experienceArtifacts\|experienceArtifactVersions" packages/convex/convex/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `schema.ts` contains all three `experience*` table definitions (grep count ≥ 3); existing table defs are byte-unchanged (git diff is append-only).
    - `experienceRuns.ts` exports mutations that `ctx.db.insert` into `experienceRuns` and `experienceArtifactVersions`, and a query reading run + event state.
    - `pnpm typecheck` clean after `_generated` regeneration (no missing Convex types).
    - The persisted artifact-version field stores the IR as structured JSON, never an HTML/string-markup blob.
  </acceptance_criteria>
  <done>Three `experience*` Convex tables and their run/IR persistence functions exist (append-only to schema), regenerated and typechecked, so a generation run and its IR persist durably (VER-03 / D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → run route → Mastra (backend) | Orchestration runs server-side (Node); the browser never sequences. |
| AI SDK ↔ Mastra | Mastra owns sequencing/branching/HITL; AI SDK is model/stream layer only (ORCH-04). |
| Workflow output → Convex persistence | Only structured IR JSON + run state persist; never markup. |
| npm → agents package | Mastra packages enter the tree here (pinned exact, pre-cleared by plan-01 checkpoint). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Elevation | AI SDK callback carrying orchestration logic | mitigate | All sequencing/branching in the Mastra workflow; model adapter is the sole AI-SDK touch; structural test asserts no orchestration in callbacks (ORCH-04). |
| T-01-12 | Tampering | Mock generator emitting invalid/markup IR | mitigate | `mockGenerate` output must pass IR schema + `validateAst`; gap inputs short-circuit (GEN-08, Pitfall 2/6). |
| T-01-13 | Tampering | Resolver inventing non-web dimensions | mitigate | Uncovered profile → typed gap, never round-number defaults; test asserts no numeric dimension on gap (FND-03, Pitfall 6). |
| T-01-14 | Tampering | Modifying existing Convex tables (isolation) | mitigate | Append-only schema edits; git diff confirms existing defs unchanged (D-08, Pitfall 3). |
| T-01-SC | Tampering | Mastra install supply chain | mitigate | Exact pins cleared by plan-01 blocking-human checkpoint; single-`ai`-version gate re-run post-install. |
</threat_model>

<verification>
- `pnpm --filter @oneui/experience-builder-agents test` green (ORCH-01/03/04, GEN-01/08, FND-01/03).
- `pnpm tsx scripts/check-single-ai-version.ts` exits 0 after Mastra install.
- `pnpm typecheck` clean (incl. regenerated Convex `_generated`).
- `schema.ts` git diff is append-only; existing Builder untouched.
</verification>

<success_criteria>
- Mastra workflow skeleton runs the mock pipeline, emits `ExperienceBuilderEvent`s over v6 transport, and branches on gaps — orchestration owned by Mastra (ORCH-01/03/04).
- Mock generation yields a valid IR passing schema + validator (GEN-08); resolver returns ThemeConfig-shaped or typed gap (FND-01/03).
- Runs + IR persist to three new `experience*` Convex tables (VER-03 / D-08).
</success_criteria>

<output>
Create `.planning/phases/01-isolated-foundation/01-04-SUMMARY.md` when done.
</output>
