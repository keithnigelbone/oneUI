---
phase: 03-preview-eval-repair
plan: 05
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - packages/experience-builder-preview/package.json
  - packages/experience-builder-preview/src/DaytonaExecutor.ts
  - packages/experience-builder-preview/src/index.ts
  - packages/experience-builder-preview/src/DaytonaExecutor.test.ts
  - .env.example
autonomous: false
user_setup:
  - service: daytona
    why: "Zero-egress isolated sandbox for rendering + screenshotting untrusted compiled artifacts (PREV-01 prod path, D-01)"
    env_vars:
      - name: DAYTONA_API_KEY
        source: "Daytona dashboard → API keys (server-side only; never reaches preview/bundle)"
requirements: [PREV-01, PREV-04]
must_haves:
  truths:
    - "@daytonaio/sdk is verified authentic at a blocking human checkpoint BEFORE install (slopcheck was offline → [ASSUMED])"
    - "DaytonaExecutor implements PreviewExecutor and is the ONLY module importing @daytonaio/sdk"
    - "The sandbox is created with networkBlockAll: true at create time — it cannot reach Convex/auth (PREV-01)"
    - "DaytonaExecutor is swappable behind the same seam with zero workflow rework (D-02)"
  artifacts:
    - path: "packages/experience-builder-preview/src/DaytonaExecutor.ts"
      provides: "Zero-egress prod preview executor (D-01)"
      contains: "networkBlockAll"
    - path: "packages/experience-builder-preview/src/DaytonaExecutor.test.ts"
      provides: "Credential-free executor-contract test (mocked SDK)"
      contains: "DaytonaExecutor"
  key_links:
    - from: "DaytonaExecutor"
      to: "PreviewExecutor seam"
      via: "implements render(input): RenderResult"
      pattern: "implements PreviewExecutor"
    - from: "DaytonaExecutor"
      to: "zero-egress isolation"
      via: "create({ networkBlockAll: true })"
      pattern: "networkBlockAll"
---

<objective>
Add the `DaytonaExecutor` — the production zero-egress preview/screenshot path (D-01) — behind the existing `PreviewExecutor` seam from Plan 02. This is the runtime confidence layer: per D-03 it does NOT own React output and does NOT relax generation rules (the IR compiler still owns React+Jio-CSS codegen — Daytona only executes/previews/screenshots), it renders the already-compiled artifact per output profile (D-04) in a network-blocked Daytona sandbox that cannot reach Convex/auth, screenshots per profile, and returns an immutable signed preview URL. The `@daytonaio/sdk` package is `[ASSUMED]` (slopcheck was offline) so its install is gated behind a BLOCKING human-verify checkpoint. Because the IframeCsp path (Plan 02) already ships the full lifecycle→eval→repair→version loop, this plan is a clean swap-in: if Daytona is deferred, the phase still ships (D-02).

Purpose: D-01 makes Daytona the MVP prod isolation backend; the seam (D-02) de-risks the vendor. PREV-01 demands the preview cannot read host credentials — `networkBlockAll: true` is the VM-enforced guarantee.
Output: A verified-then-installed `@daytonaio/sdk`, a `DaytonaExecutor` implementing the seam, `.env.example` entry, and a credential-free contract test (mocked SDK).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-preview-eval-repair/03-RESEARCH.md
@.planning/phases/03-preview-eval-repair/03-PATTERNS.md
@.planning/phases/03-preview-eval-repair/03-02-SUMMARY.md
@packages/experience-builder-preview/PREVIEW-DECISION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify @daytonaio/sdk authenticity BEFORE install (supply-chain gate)</name>
  <what-built>
    Nothing yet — this is the mandatory pre-install supply-chain gate. RESEARCH tagged `@daytonaio/sdk@0.183.0` as `[ASSUMED]` because slopcheck could not run offline. Manual checks recorded in RESEARCH § Package Legitimacy Audit: official repo `github.com/daytonaio/daytona`, empty postinstall, active multi-channel dist-tags. This checkpoint requires a human to confirm authenticity before any code depends on it. This checkpoint is NEVER auto-approvable (workflow.auto_advance is ignored).
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@daytonaio/sdk and confirm: publisher is the official Daytona org, repository link is `github.com/daytonaio/daytona`, the latest version is on an active release train (~0.183.x or newer), and there is no suspicious `postinstall` script.
    2. Run `npm view @daytonaio/sdk repository.url` and confirm it is `git+https://github.com/daytonaio/daytona.git` (not a typosquat).
    3. Run `npm view @daytonaio/sdk scripts.postinstall` and confirm it is empty (no install-time code execution).
    4. Re-verify the version is current (RESEARCH valid-until 2026-07-01 noted the SDK is on an active release train — pin the verified version).
  </how-to-verify>
  <action>Do NOT install or import `@daytonaio/sdk` yet. Perform the four authenticity checks in <how-to-verify> (npmjs publisher/repo/postinstall, `npm view @daytonaio/sdk repository.url`, `npm view @daytonaio/sdk scripts.postinstall`, current version), then PAUSE for human approval. This gate is mandatory because the package is `[ASSUMED]` (slopcheck offline); it is never auto-approvable. Only on an explicit "approved: <version>" does Task 2 proceed; on "blocked" the phase still ships via the IframeCsp executor (Plan 02), so record the deferral and skip Tasks 2-3.</action>
  <verify>Human confirms authenticity at this blocking checkpoint; no automated command (pre-install trust gate).</verify>
  <done>A human has verified `@daytonaio/sdk` is authentic and approved a specific version for install, OR explicitly deferred Daytona (IframeCsp remains the MVP path).</done>
  <acceptance_criteria>
    - `npm view @daytonaio/sdk repository.url` returns `git+https://github.com/daytonaio/daytona.git`.
    - `npm view @daytonaio/sdk scripts.postinstall` is empty.
    - Human typed "approved: <version>" (or "blocked: <reason>" to defer).
  </acceptance_criteria>
  <resume-signal>Type "approved: <version>" to proceed with install, or "blocked: <reason>" to defer Daytona (IframeCsp from Plan 02 remains the MVP path with no rework, D-02).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install @daytonaio/sdk + implement DaytonaExecutor (PREV-01/PREV-04, D-01)</name>
  <read_first>
    - packages/experience-builder-preview/src/PreviewExecutor.ts (the seam to implement — `PreviewExecutor`, `RenderInput`, `RenderResult` from Plan 02)
    - packages/experience-builder-preview/src/IframeCspExecutor.ts (the sibling executor — mirror its per-profile capture semantics + previewState shape)
    - apps/platform/src/lib/playwrightRenderer.ts (screenshot semantics analog: per-profile loop, font/transition settle, PNG buffer return — re-implemented INSIDE the sandbox)
    - packages/experience-builder-preview/PREVIEW-DECISION.md (isolation invariants)
    - .planning/phases/03-preview-eval-repair/03-RESEARCH.md (§ Pattern 2 DaytonaExecutor skeleton; § Pitfall 1 NO external CDP — run Playwright inside the box or use native screenshot; § Open Q4 set networkBlockAll at CREATE time; § Assumption A4 inline fonts or pin networkAllowList CIDR)
    - .planning/phases/03-preview-eval-repair/03-PATTERNS.md (§ "DaytonaExecutor.ts")
  </read_first>
  <action>
    Install at the verified version: `pnpm --filter @oneui/experience-builder-preview add @daytonaio/sdk@<approved-version>`. Create `DaytonaExecutor.ts` implementing `PreviewExecutor` — this is the ONLY module importing `@daytonaio/sdk`. In `render()`: `new Daytona()` reads `DAYTONA_API_KEY` server-side only; `await daytona.create({ networkBlockAll: true })` (set at CREATE time — works on all account tiers, Open Q4; this is the PREV-01 zero-egress guarantee). Inside a `try/finally`, write the compiled bundle into the sandbox, render + screenshot per profile INSIDE the sandbox (run Playwright in-box OR use Daytona's native screenshot API — do NOT connect an external CDP, Pitfall 1 / issue #4456), obtain an immutable live URL via `getSignedPreviewUrl(port, expiresInSeconds)` for `previewState` (PREV-02), and `await sandbox.delete()` in `finally` (per-run teardown; warm-pool tuning is discretion). Inline the Jio fonts/CSS into the preview bundle so `networkBlockAll` holds, or pin a `networkAllowList` CIDR (IPv4 + /N only) if a CDN is required (Assumption A4). Return `RenderResult` matching the seam (`screenshots`, `previewState`, `rendered`). Add `@oneui/experience-builder-preview` build re-link. Export from `index.ts`. INVARIANT: the API key is never written into the bundle/previewState/screenshot path (PREV-01).
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-preview typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rln "@daytonaio/sdk" packages/experience-builder-preview/src` returns ONLY `DaytonaExecutor.ts` (the single vendor importer).
    - `grep -n "networkBlockAll: true" packages/experience-builder-preview/src/DaytonaExecutor.ts` present and set at `create(...)` time.
    - `grep -n "connect_over_cdp\|connectOverCDP\|connect_over" packages/experience-builder-preview/src/DaytonaExecutor.ts` returns nothing (no external CDP — Pitfall 1).
    - `grep -n "sandbox.delete\|.delete()" packages/experience-builder-preview/src/DaytonaExecutor.ts` shows teardown in a `finally`.
    - `pnpm --filter @oneui/experience-builder-preview typecheck` zero errors.
  </acceptance_criteria>
  <done>@daytonaio/sdk is installed at the verified version; DaytonaExecutor implements the seam with create-time `networkBlockAll`, in-box screenshot, signed preview URL, and per-run teardown.</done>
</task>

<task type="auto">
  <name>Task 3: Credential-free DaytonaExecutor contract test + .env.example (PREV-01)</name>
  <read_first>
    - packages/experience-builder-preview/src/DaytonaExecutor.ts (the subject — from Task 2)
    - packages/experience-builder-preview/src/PreviewExecutor.test.ts (the seam test idiom from Plan 02 — credential-free mock injection)
    - .env.example (the file being extended — confirm existing env var documentation style)
    - .planning/phases/03-preview-eval-repair/03-RESEARCH.md (§ Runtime State Inventory — `DAYTONA_API_KEY` server-side only, never crosses the isolation boundary)
  </read_first>
  <behavior>
    - Test: with the `@daytonaio/sdk` `Daytona` client mocked (no `DAYTONA_API_KEY`, no live sandbox), `DaytonaExecutor.render()` calls `create` with `{ networkBlockAll: true }`, returns a `RenderResult` satisfying the seam shape, and calls `sandbox.delete()` even when capture throws (finally teardown).
    - Test: the API key / any secret never appears in the returned `previewState`/screenshots (assert the previewState url carries only the signed token).
  </behavior>
  <action>
    Create `DaytonaExecutor.test.ts` that mocks the `@daytonaio/sdk` `Daytona` class (vi.mock) so the suite runs credential-free with NO `DAYTONA_API_KEY`. Assert: `create` is called with `networkBlockAll: true`; the result conforms to `RenderResult`; `delete()` is invoked in the `finally` even on a thrown capture; no secret leaks into `previewState`. Add `DAYTONA_API_KEY=` (with a "server-side only; never reaches preview" comment) to `.env.example`. Note in the SUMMARY that the live zero-egress network probe (PREV-01) is a manual verification per 03-VALIDATION.md (requires a real sandbox + network probe to the Convex URL).
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-preview test -t DaytonaExecutor</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/experience-builder-preview test` passes with zero env vars set (mocked SDK, credential-free).
    - The test asserts `create({ networkBlockAll: true })` and `finally` teardown on a thrown capture.
    - `grep -n "DAYTONA_API_KEY" .env.example` present with the server-side-only comment.
  </acceptance_criteria>
  <done>DaytonaExecutor has a credential-free contract test proving create-time zero-egress + finally teardown + no secret leak; `.env.example` documents the key. Live network-egress probe deferred to manual verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiled bundle → Daytona sandbox | Untrusted LLM-authored artifact executes in a VM-isolated runtime |
| sandbox → Convex/auth | The sandbox must NOT be able to reach host services |
| npm registry → repo | `@daytonaio/sdk` is `[ASSUMED]` (slopcheck offline) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-05-EGRESS | Information Disclosure | Daytona sandbox | mitigate | `networkBlockAll: true` set at create time → zero egress; the sandbox cannot reach Convex/auth (PREV-01). Live network-probe is a manual verification (03-VALIDATION.md). |
| T-3-05-KEY | Information Disclosure | DAYTONA_API_KEY | mitigate | Read server-side only by the Daytona client; never written into bundle/previewState/screenshot; documented in `.env.example` (V4 access control). |
| T-3-05-CDP | Tampering | external CDP path | mitigate | Render Playwright IN-BOX or use native screenshot; no external CDP endpoint (Pitfall 1 / issue #4456). |
| T-3-05-SC | Tampering | `@daytonaio/sdk` install (`[ASSUMED]`) | mitigate | BLOCKING human-verify checkpoint (Task 1) verifies npmjs/repo/postinstall BEFORE install; never auto-approvable; `[SLOP]`/`[SUS]` would forbid. |
</threat_model>

<verification>
- `pnpm --filter @oneui/experience-builder-preview test` green, credential-free (mocked SDK).
- `pnpm --filter @oneui/experience-builder-preview typecheck` zero errors.
- `@daytonaio/sdk` imported only by `DaytonaExecutor.ts`.
- Manual: live sandbox network-egress probe to the Convex URL is blocked (PREV-01, 03-VALIDATION.md).
</verification>

<success_criteria>
The production preview path renders untrusted artifacts in a zero-egress Daytona sandbox that cannot read host credentials (PREV-01), screenshots per profile credential-free (PREV-04), and is a clean swap behind the `PreviewExecutor` seam — installed only after a blocking human authenticity verification of the `[ASSUMED]` package (D-01/D-02).
</success_criteria>

<output>
Create `.planning/phases/03-preview-eval-repair/03-05-SUMMARY.md` when done
</output>
