---
phase: 03-preview-eval-repair
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - packages/experience-builder-agents/src/evaluatorRubric.ts
  - packages/experience-builder-agents/src/steps/evaluate.ts
  - packages/experience-builder-agents/src/steps/repair.ts
  - packages/experience-builder-agents/src/modelAdapter.ts
  - packages/experience-builder-agents/src/workflow.ts
  - packages/experience-builder-agents/src/index.ts
  - packages/experience-builder-agents/src/steps/evaluate.test.ts
  - packages/experience-builder-agents/src/steps/repair.test.ts
  - packages/experience-builder-agents/src/workflow.test.ts
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03, ORCH-02, GEN-07, PREV-04, VAL-06]
must_haves:
  truths:
    - "After validate, the workflow runs preview → evaluate → bounded repair → version-freeze in BOTH the .then chain and ORDERED_STEPS"
    - "Objective track: a blocking validation violation short-circuits straight to repair with NO model call (D-06)"
    - "Subjective track: the vision judge scores hierarchy/spacing/density/brandFit 0-5 from the screenshot, routed ONLY through callModel (ORCH-04)"
    - "Repair emits a targeted IrPatch against failing nodes and applies it via applyPatch — never JSX, never whole-IR regen (D-09)"
    - "The repair loop terminates on cap N=3 OR composite≥threshold OR scoreDelta<epsilon OR sameValidationError OR gap (D-10/D-11)"
    - "A missing component/profile short-circuits to a gap with zero repair attempts (D-11)"
    - "Best-of-N variants are produced and the judge ranks the best (GEN-07/D-08)"
  artifacts:
    - path: "packages/experience-builder-agents/src/evaluatorRubric.ts"
      provides: "Zod rubric (Anthropic-safe) + composite/threshold/epsilon config (D-07)"
      contains: "z.number()"
    - path: "packages/experience-builder-agents/src/steps/evaluate.ts"
      provides: "Two-track scoring step (D-06)"
      contains: "evaluateStep"
    - path: "packages/experience-builder-agents/src/steps/repair.ts"
      provides: "IR-patch repair step (D-09)"
      contains: "applyPatch"
    - path: "packages/experience-builder-agents/src/workflow.ts"
      provides: "preview→evaluate→bounded-repair→version-freeze wiring"
      contains: "versionFreezeStep"
  key_links:
    - from: "evaluate.ts subjective track"
      to: "callModel (modelAdapter)"
      via: "vision image-message path"
      pattern: "callModel"
    - from: "repair.ts"
      to: "@oneui/experience-builder-core applyPatch"
      via: "targeted IrPatch on failing nodes"
      pattern: "applyPatch"
    - from: "workflow.ts repair loop"
      to: "termination predicate"
      via: "attempt>=3 || composite>=threshold || scoreDelta<epsilon || sameValidationError || halted"
      pattern: "attempt"
---

<objective>
Close the quality loop (D-05 steps 6-10): insert `preview → evaluate → bounded repair → version-freeze` into the Mastra workflow after `validate`. The evaluate step is two-track (D-06) — objective compliance from the deterministic validator (a blocking violation short-circuits straight to repair, no model call) plus a subjective multimodal rubric judge reading the preview screenshot via the single `callModel` seam (ORCH-04). Repair patches the IR through the frozen `applyPatch`/`diffIr` contract (D-09), never JSX, and the loop is bounded (cap N=3 + convergence + per-run budget + gap short-circuit, D-10/D-11). Best-of-N variants are produced and ranked (GEN-07/D-08). The version-freeze step persists the approved version.

Purpose: This is the heart of Phase 3 — the run shape locked by the user's 10-step contract. Everything else (validator, preview, schema, canvas) feeds this loop.
Output: New `evaluatorRubric.ts`, `steps/evaluate.ts`, `steps/repair.ts`, a vision path on `modelAdapter.ts`, extended `workflow.ts` (chain + runner + RunContext), and credential-free tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-preview-eval-repair/03-RESEARCH.md
@.planning/phases/03-preview-eval-repair/03-PATTERNS.md
@.planning/phases/03-preview-eval-repair/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: evaluatorRubric.ts + vision path on modelAdapter + two-track evaluate step (EVAL-01/D-06/D-07)</name>
  <read_first>
    - packages/experience-builder-agents/src/irGenerator.ts (the canonical Zod-4↔Anthropic gotcha solution — `SectionFillSchema` lines 111-126: plain `z.number()`, `z.object({}).catchall(...)`, no `.int()/.min()/.max()`, no keyed `z.record`)
    - packages/experience-builder-agents/src/modelAdapter.ts (the file being modified — `CallModelArgs` lines 73-82, `callModelReal` lines 84-97 using `generateText` + `Output.object`; the seam this is the ONLY `ai`/`@ai-sdk` importer)
    - apps/platform/src/app/api/composition/verify/route.ts (the VERIFIED vision-judge call shape: `generateText({ model: anthropic(CLAUDE_VISION_MODEL), system, messages:[{role:'user', content:[{type:'text'},{type:'image', image:`data:image/png;base64,...`},{type:'text'}]}] })` ~lines 207-228; `CLAUDE_VISION_MODEL` from `@oneui/shared/agent`)
    - packages/experience-builder-agents/src/workflow.ts (the step convention — `validateStep` lines 532-556: `createStep({id,inputSchema:ctxSchema,outputSchema:ctxSchema,execute})`, `if (ctx.halted) return {ctx}` guard, `emit(...)` started/completed; the `RunContext` interface lines 201-221; `ctx.validation` already populated by validateStep)
    - .planning/phases/03-preview-eval-repair/03-RESEARCH.md (§ Pattern 6 vision judge; § Pitfall 2 Zod gotchas; § "Bias mitigation" analytic scoring)
    - .planning/phases/03-preview-eval-repair/03-PATTERNS.md (§ "evaluate.ts", § "modelAdapter.ts (EXTEND)", § "evaluatorRubric.ts")
  </read_first>
  <behavior>
    - Test (D-06 objective short-circuit): when `ctx.validation.passed === false` (blocking violations exist), `evaluateStep` sets the composite to a failing value and does NOT call `callModel` — the objective track alone routes to repair.
    - Test (D-06 subjective): when validation passes, `evaluateStep` calls the injected (mock) `callModel` with an image content part and parses a `VisualRubric` (hierarchy/spacing/density/brandFit 0-5 + notes); scores out of range are clamped to 0-5 after parse.
    - Test (D-07 composite): the composite is computed via the config-tunable weighted formula from `evaluatorRubric.ts`; `composite >= threshold` is the pass decision.
    - Test (Zod safety): the rubric schema uses plain `z.number()` and `z.object({}).catchall(...)` — no `.int()/.min()/.max()`, no keyed `z.record`.
  </behavior>
  <action>
    Create `evaluatorRubric.ts` exporting `VisualRubric` (Zod object: `hierarchy`, `spacing`, `density`, `brandFit` as plain `z.number()`, `notes: z.string()`) following `irGenerator.ts`'s gotcha rules verbatim; export a tunable config object `{ weights, threshold, epsilon }` and a pure `composite(rubric, objectivePass): number` formula (D-07 — start simple, all values config constants at the top of the file). Extend `modelAdapter.ts` `CallModelArgs` with an optional `images?: Array<{ png: Buffer }>`; in `callModelReal`, when `images` is present, build the `messages:[{role:'user', content:[...text, ...image parts...]}]` form (image as `data:image/png;base64,<...>` from the verify-route excerpt) instead of `prompt`, keeping `experimental_output: Output.object({ schema })`. Use `CLAUDE_VISION_MODEL` from `@oneui/shared/agent` as the default model when images are present. This stays the ONLY file importing `ai`/`@ai-sdk/*`. Create `steps/evaluate.ts` exporting `evaluateStep` (mirror `validateStep`'s shape): guard on `ctx.halted`; OBJECTIVE track first — if `ctx.validation && !ctx.validation.passed`, set `ctx.composite` below threshold, set `ctx.objectivePass=false`, emit an evaluate event, return (NO model call, D-06 short-circuit); else run the SUBJECTIVE track — call `callModel({ schema: VisualRubric, images: ctx.screenshots, system: <analytic rubric prompt with 0-5 ranges stated>, ... })`, clamp scores, compute `ctx.composite` via the rubric formula, store `ctx.evaluation`. Branching/sequencing stays in the workflow, never a model callback (ORCH-04).
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-agents test -t evaluate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "z.number()" packages/experience-builder-agents/src/evaluatorRubric.ts` present; `grep -c "\.int()\|\.min(\|\.max(\|z.record(" packages/experience-builder-agents/src/evaluatorRubric.ts` returns 0.
    - `grep -rn "from 'ai'\|@ai-sdk" packages/experience-builder-agents/src/steps/evaluate.ts` returns nothing (no direct AI import in the step).
    - `grep -n "images\|content:" packages/experience-builder-agents/src/modelAdapter.ts` shows the vision image-message path.
    - `pnpm --filter @oneui/experience-builder-agents test -t evaluate` passes with a mocked `callModel` (credential-free) including the objective short-circuit assertion.
  </acceptance_criteria>
  <done>Two-track scoring works: objective fail short-circuits with no model call; subjective rubric scores via the single vision seam; composite is config-tunable; Zod schema is Anthropic-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IR-patch repair step (D-09/EVAL-02) + gap short-circuit (D-11/EVAL-03)</name>
  <read_first>
    - packages/experience-builder-core/src/ir/patch.ts (FROZEN — reuse directly: `applyPatch(ir, patch)` line 193, `diffIr` line 129, `PatchOperation`/`IrPatch` types lines 25-33; ops are add/remove/replace with JSON-Pointer paths)
    - apps/platform/src/app/api/composition/repair/route.ts (the repair-prompt shape analog ~lines 116-142: re-prompt with the design-system contract + failing artifact + validator error feedback, then re-validate + gate on `validation.valid`)
    - packages/experience-builder-agents/src/workflow.ts (the gap-branch shape to mirror — `generateStep` gap branch lines 481-497 sets `ctx.outcome='gap'`, `ctx.halted=true`, emits a `gap` event; `compileStep` lines 506-530 + `validateStep` lines 532-556 to re-enter compile→validate after a patch)
    - packages/experience-builder-agents/src/irGenerator.ts (Zod patch-op schema gotcha rules for the model-emitted patch — same as Task 1)
    - .planning/phases/03-preview-eval-repair/03-PATTERNS.md (§ "repair.ts (NEW — IR-patch repair)")
    - .planning/phases/03-preview-eval-repair/03-RESEARCH.md (§ Pattern 5 IR-only repair; § Pitfall 4 non-termination; § Pitfall 6 dountil polarity)
  </read_first>
  <behavior>
    - Test (D-09): `repairStep` produces a NEW IR via `applyPatch(failingIr, patch)` where `patch` is a targeted `IrPatch` (add/remove/replace on failing node paths) — the original IR object is unchanged (pure), and the result is re-compiled + re-validated.
    - Test (no JSX): the repair never edits a code/JSX string and never regenerates the whole IR (assert the patch is a small op list, not a full IR).
    - Test (D-11 gap): when the failure is a missing component/profile (a component/foundation gap), `repairStep` sets `ctx.halted=true`, `ctx.outcome='gap'`, emits a `gap` event, and makes ZERO model/patch attempts.
    - Test (D-10 sameError): repair sets `ctx.sameValidationError=true` when the post-repair blocking-error set equals the pre-repair set.
  </behavior>
  <action>
    Create `steps/repair.ts` exporting `repairStep` (mirror `validateStep` shape). Entry guard: if the current failure is a component/foundation gap (inspect `ctx.validation.componentGaps`/`foundationGaps` or the gap event), set `ctx.halted=true`, `ctx.outcome='gap'`, emit a `gap` event, and return with NO attempt (D-11 — never loop on an unsatisfiable gap). Otherwise: build a repair prompt mirroring `composition/repair/route.ts` (design-system contract + the failing IR + the validator's blocking violations + repairSuggestions as feedback) and call `callModel` with a Zod patch-op schema (an `IrPatch`-shaped array — same Anthropic-safe gotcha rules). Apply the returned patch with `applyPatch(ctx.ir, patch)` from `@oneui/experience-builder-core` to get a NEW IR; set `ctx.ir` to it; track the previous composite + previous blocking-error code set on `ctx` so the loop's termination predicate can detect convergence: set `ctx.sameValidationError=true` if the new blocking set equals the prior set, and increment `ctx.attempt`. Repair then re-enters compile→validate→evaluate (the workflow loop drives this). NEVER edit a JSX/code string; NEVER call `generateIR` for a whole-IR regen (D-09).
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-agents test -t repair</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "applyPatch" packages/experience-builder-agents/src/steps/repair.ts` present; `grep -c "generateIR\|dangerouslySetInnerHTML" packages/experience-builder-agents/src/steps/repair.ts` returns 0 (no whole-IR regen, no JSX).
    - `pnpm --filter @oneui/experience-builder-agents test -t repair` passes including the gap-short-circuit (zero attempts) and sameValidationError assertions.
    - The repair patch in the test is a small op array (length ≤ a handful), not a full IR.
  </acceptance_criteria>
  <done>Repair patches the IR via the frozen contract (never JSX/whole-regen), short-circuits gaps with zero attempts, and exposes convergence signals to the loop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire preview→evaluate→bounded-repair→version-freeze + best-of-N (ORCH-02/EVAL-03/GEN-07/VAL-06)</name>
  <read_first>
    - packages/experience-builder-agents/src/workflow.ts (the file being modified — read in full: the `.then(...)` chain lines 562-576, `ORDERED_STEPS` lines 592-602, the `runExperienceWorkflow` runner lines 609-663, `RunContext` interface lines 201-221, `RunExperienceInput` lines 167-182 and `RunExperienceResult` lines 184-199, the `foundationsLoader` opaque-injection idiom lines 163-182)
    - packages/experience-builder-preview/src/PreviewExecutor.ts (the seam to inject — `PreviewExecutor`, `RenderInput`, `RenderResult`, `getPreviewExecutor`/`setPreviewExecutor` from Plan 02)
    - packages/experience-builder-agents/src/steps/evaluate.ts (from Task 1) and steps/repair.ts (from Task 2)
    - packages/experience-builder-agents/src/index.ts (barrel — export new steps/types)
    - packages/experience-builder-agents/src/workflow.test.ts (the existing workflow test to extend)
    - .planning/phases/03-preview-eval-repair/03-RESEARCH.md (§ Pattern 4 `.dountil` bounded loop + termination predicate; § Open Q3 best-of-N as N sequential `runExperienceWorkflow` calls sharing resolved foundation; § Pitfall 6 polarity)
    - .planning/phases/03-preview-eval-repair/03-PATTERNS.md (§ "workflow.ts (EXTEND)")
  </read_first>
  <behavior>
    - Test (sequence): `runExperienceWorkflow` on a passing IR runs intent…validate then `preview`, `evaluate`, (no repair needed), `version-freeze`, emitting started/completed step events for each new step in order.
    - Test (VAL-06): `previewStep` sets `ctx.rendered` from the executor's `RenderResult.rendered`; a `rendered:false` is treated as a failing objective signal.
    - Test (ORCH-02/EVAL-03 bounded loop): given a mock that keeps failing, repair runs at most 3 times then stops (cap); given a mock that stops improving, it stops on `scoreDelta<epsilon`; given a repeated blocking error, it stops on `sameValidationError`; a gap stops immediately (`halted`). The termination predicate returns TRUE to STOP (Pitfall 6 polarity).
    - Test (GEN-07/D-08): a `runVariants(input, n)` helper runs N sequential `runExperienceWorkflow` calls sharing the resolved foundation, the judge composite ranks them, and the best variant is returned with a shared `variantGroupId`.
  </behavior>
  <action>
    Add `previewStep` (calls the injected `PreviewExecutor.render({ bundle: ctx.bundle, brandId, profiles })`, stores `ctx.screenshots` + `ctx.previewState` + `ctx.rendered`; emit started/completed; guard on `ctx.halted`) and `versionFreezeStep` (assembles the terminal `RunExperienceResult` carrying `ir`, `bundle`, `previewState`, `evaluation`, `validation`; emits `run-completed`/freeze event). Inject the `PreviewExecutor` on `RunExperienceInput` exactly like `foundationsLoader` (opaque interface, defaulting to `getPreviewExecutor()` so the agents package stays vendor-free; tests pass a mock). Extend `RunContext` with `attempt`, `composite`, `threshold`, `epsilon`, `scoreDelta`, `sameValidationError`, `previewState`, `screenshots`, `evaluation`, `rendered`; extend `RunExperienceResult` with `previewState`/`evaluation`. Insert `previewStep → evaluateStep → repairStep(bounded) → versionFreezeStep` after `validateStep` in BOTH the committed `.then` chain (use Mastra `.dountil(repairStep, terminationPredicate)`) AND `ORDERED_STEPS` (the runner mirrors the bounded loop with a `for (attempt<3)` exactly as the current runner drives steps). The termination predicate returns TRUE to STOP: `ctx.halted || ctx.attempt>=3 || ctx.composite>=ctx.threshold || ctx.scoreDelta<ctx.epsilon || ctx.sameValidationError` (Pitfall 6). Add a `runVariants(input, n)` exported helper (GEN-07/D-08): N sequential `runExperienceWorkflow` calls sharing the already-resolved `brandFoundations`, rank by `evaluation` composite, assign a shared `variantGroupId`, return the ranked siblings. Export the new steps + `runVariants` + types from `index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-agents test -t workflow</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "previewStep\|evaluateStep\|repairStep\|versionFreezeStep" packages/experience-builder-agents/src/workflow.ts` shows all four in BOTH the chain and `ORDERED_STEPS`.
    - `grep -n "attempt >= 3\|>= ctx.threshold\|sameValidationError" packages/experience-builder-agents/src/workflow.ts` shows the termination predicate.
    - `grep -n "runVariants" packages/experience-builder-agents/src/index.ts` shows the export.
    - `pnpm --filter @oneui/experience-builder-agents test` passes including the bounded-loop (cap 3 / convergence / gap) + best-of-N ranking tests, all credential-free (mock callModel + mock PreviewExecutor).
    - `grep -rn "from 'ai'\|@ai-sdk\|@daytonaio" packages/experience-builder-agents/src/workflow.ts` returns nothing (vendor-free orchestration, ORCH-04).
  </acceptance_criteria>
  <done>The full preview→evaluate→bounded-repair→version-freeze loop runs in both the chain and runner, terminates correctly on all four conditions + gap, and best-of-N variants are ranked and grouped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: HITL suspend/resume checkpoint at non-convergence (ORCH-02 second half)</name>
  <read_first>
    - packages/experience-builder-agents/src/workflow.ts (the file being modified — the repair loop + termination predicate from Task 3; the `RunContext` + `RunExperienceInput`/`RunExperienceResult` shapes; the `createStep` convention)
    - node_modules/@mastra/core/dist/workflows/workflow.d.ts and .../types.d.ts (VERIFIED in RESEARCH: `suspend()` / `resumeData` / `resumeSchema` on the step execute signature — read the `suspend`/`resumeData`/`resumeSchema` members)
    - packages/experience-builder-agents/src/steps/repair.ts (from Task 2 — the non-convergence signals `ctx.sameValidationError`/`ctx.scoreDelta`/`ctx.attempt` this checkpoint reads)
    - .planning/phases/03-preview-eval-repair/03-RESEARCH.md (§ Phase Requirements ORCH-02 row — `.dountil` + `suspend`/`resumeData` confirmed in @mastra/core@1.37.1; § Validation Architecture ORCH-02)
    - .planning/phases/03-preview-eval-repair/03-PATTERNS.md (§ "workflow.ts (EXTEND)")
  </read_first>
  <behavior>
    - Test (ORCH-02 HITL suspend): when the bounded loop exhausts without converging (attempt reaches the cap OR `sameValidationError`/`scoreDelta<epsilon`) AND a HITL checkpoint is enabled on the run input, the workflow SUSPENDS via Mastra `suspend()` before emitting the terminal gap-report — surfacing the suspend payload (the failing validation + last composite + the candidate IR) rather than auto-finalizing.
    - Test (ORCH-02 HITL resume): resuming with `resumeData` matching the resume schema (`{ decision: 'accept' | 'repair-again' | 'abandon', note?: string }`) continues deterministically — `accept` proceeds to version-freeze, `repair-again` runs one more bounded repair attempt, `abandon` emits the gap-report.
    - Test (default off): when the HITL checkpoint is NOT enabled on the run input, behavior is unchanged from Task 3 (no suspend; the loop finalizes to gap/artifact as before) — preserves the deterministic credential-free runner.
    - Test: the resume decision/branching lives in the workflow, never in a model callback (ORCH-04).
  </behavior>
  <action>
    Add a `repairCheckpoint` step (or extend `repairStep`) that, when `ctx.request.hitl === true` AND the loop has hit a non-convergence terminal (cap reached OR `sameValidationError` OR `scoreDelta < epsilon`) but NOT a gap, calls Mastra's `suspend({ ... })` with a payload of `{ runId, validation: ctx.validation, composite: ctx.composite, ir: ctx.ir, reason: 'non-converging-repair' }`, and declares a `resumeSchema` Zod object `{ decision: z.enum(['accept','repair-again','abandon']), note: z.string().optional() }` (Anthropic-safe shape rules still apply: plain types, no keyed `z.record`). On resume, read `resumeData.decision` and branch IN THE WORKFLOW: `accept` → proceed to `versionFreezeStep`; `repair-again` → reset `sameValidationError`/increment budget for ONE additional bounded attempt; `abandon` → set `ctx.outcome='gap'`, `ctx.halted=true`, emit the gap-report. Gate the whole checkpoint on `ctx.request.hitl` (a new optional `hitl?: boolean` on `RunExperienceInput`, default false) so the deterministic runner/tests stay unchanged when off. Add a `suspended` terminal possibility to `RunExperienceResult.outcome` (or a `suspendPayload?`) so the route/canvas can surface the HITL prompt. Keep the runner mirroring: in the plain `for`-loop runner, when `hitl` is on and the loop is non-converging, return a `suspended` result carrying the payload instead of finalizing (the committed `.dountil`/`suspend` path is the Mastra-native equivalent — keep both in sync, as the file already does). NO sequencing/branching in any AI-SDK callback (ORCH-04).
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-agents test -t hitl</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "suspend(\|resumeData\|resumeSchema\|hitl" packages/experience-builder-agents/src/workflow.ts` shows the suspend/resume wiring + the `hitl` gate.
    - `pnpm --filter @oneui/experience-builder-agents test -t hitl` passes: a non-converging run with `hitl:true` suspends; resuming with each of `accept`/`repair-again`/`abandon` continues deterministically; with `hitl:false` behavior is unchanged.
    - `grep -rn "from 'ai'\|@ai-sdk" packages/experience-builder-agents/src/workflow.ts` returns nothing (resume branching is workflow-owned, ORCH-04).
    - The resume schema uses plain Zod types (no keyed `z.record`, no integer `.min/.max`).
  </acceptance_criteria>
  <done>ORCH-02 is fully delivered: the bounded repair loop AND a Mastra `suspend()`/`resumeData` HITL checkpoint at non-convergence, gated off by default so the deterministic runner is unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screenshot/IR → vision model | Rendered artifact image + IR cross into the model layer (callModel only) |
| repair model output → IR | Model-emitted patch crosses into the canonical IR via the frozen apply contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-04-DOS | Denial of Service | repair loop | mitigate | Hard cap N=3 + convergence (scoreDelta<epsilon, sameValidationError) + per-run budget; gap short-circuits with zero attempts (D-10/D-11). |
| T-3-04-ORCH | Tampering | branching in model callback | mitigate | All sequencing/branching/repair-decision lives in workflow.ts; `callModel` is model-only; no `ai`/`@ai-sdk` import outside modelAdapter.ts (ORCH-04). |
| T-3-04-JSX | Tampering | repair target | mitigate | Repair patches IR via frozen `applyPatch` only; no JSX edit, no whole-IR regen (D-09 — grep-asserted). |
| T-3-04-KEY | Information Disclosure | vision/repair model calls | mitigate | API key read server-side by `@ai-sdk/anthropic` only; never written into IR/bundle/event/patch (existing modelAdapter invariant). |
| T-3-04-400 | Tampering | rubric/patch Zod schema | mitigate | Plain `z.number()` + `z.object({}).catchall(...)`; no `.int()/.min()/.max()`/keyed `z.record` (Pitfall 2). |
| T-3-04-HITL | Elevation of Privilege | resume decision branching | mitigate | The `resumeData` decision (`accept`/`repair-again`/`abandon`) is validated against a Zod `resumeSchema` and branched in the workflow, never in a model callback; default-off `hitl` gate keeps the autonomous path deterministic (ORCH-02/ORCH-04). |
| T-3-04-SC | Tampering | npm/pip/cargo installs | accept | No package install in this plan (uses installed `ai`/`@mastra/core` + frozen `-core` patch). Daytona gate is Plan 05. |
</threat_model>

<verification>
- `pnpm --filter @oneui/experience-builder-agents test` green, credential-free (mock callModel + mock PreviewExecutor).
- `pnpm --filter @oneui/experience-builder-agents typecheck` zero errors.
- `pnpm ci:gates` single-ai gate intact (no new `ai`/`@ai-sdk` import outside modelAdapter.ts).
</verification>

<success_criteria>
The Visual Evaluator scores artifacts two-track (EVAL-01/D-06), repair patches the IR within a bounded loop with convergence + gap short-circuit PLUS a Mastra suspend/resume HITL checkpoint at non-convergence (EVAL-02/EVAL-03/ORCH-02 — full requirement, both halves), best-of-N variants are produced and ranked (GEN-07), the preview render-success feeds VAL-06, and all orchestration stays in Mastra with the AI SDK as model-layer-only.
</success_criteria>

<output>
Create `.planning/phases/03-preview-eval-repair/03-04-SUMMARY.md` when done
</output>
