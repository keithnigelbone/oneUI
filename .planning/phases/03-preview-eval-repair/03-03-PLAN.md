---
phase: 03-preview-eval-repair
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/convex/convex/schema.ts
  - packages/convex/convex/experienceRuns.ts
  - packages/convex/convex/experienceRuns.test.ts
autonomous: true
requirements: [VER-01, VER-02, CANVAS-05]
must_haves:
  truths:
    - "An artifact version persists the full VER-01 object: ir, compiledBundle, previewState, thumbnail, validation, evaluation, parentVersionId, originRunId"
    - "Existing rows (no previewState/thumbnail/evaluation/variantGroupId) round-trip with no migration (append-only)"
    - "N sibling artifacts sharing a variantGroupId can be read back as a group (CANVAS-05)"
    - "getArtifactHistory returns the version chain; listVariantGroup returns the sibling set"
  artifacts:
    - path: "packages/convex/convex/schema.ts"
      provides: "Additive v.optional fields: variantGroupId, previewState, thumbnail, evaluation"
      contains: "variantGroupId"
    - path: "packages/convex/convex/experienceRuns.ts"
      provides: "Extended persistArtifact + listVariantGroup query"
      contains: "listVariantGroup"
    - path: "packages/convex/convex/experienceRuns.test.ts"
      provides: "Round-trip + variant-group + version-chain tests"
      contains: "variantGroup"
  key_links:
    - from: "experienceArtifacts.variantGroupId"
      to: "listVariantGroup query"
      via: "by_variant_group index scan"
      pattern: "variantGroupId"
    - from: "persistArtifact"
      to: "experienceArtifactVersions D-13 fields"
      via: "additive v.optional insert"
      pattern: "previewState"
---

<objective>
Extend the append-only Convex persistence so a fully-evaluated artifact version is durable and browsable (VER-01/VER-02) and best-of-N siblings cluster into a variant group (CANVAS-05). Per D-12/D-13: NO new variant-group table — add an optional `variantGroupId` to `experienceArtifacts`, and add the D-13 version-object fields (`previewState`, `thumbnail` as `v.id('_storage')`, `evaluation`, `originRunId`) to `experienceArtifactVersions`. All additive `v.optional` so existing rows round-trip with zero migration (the `compiledBundle` field already proves this pattern).

Purpose: This is the terminal persistence slice of the D-05 contract (step 10, "frozen as a versioned, browsable artifact") plus variant grouping. Plans 04 (version-freeze step) and 06 (timeline + variant frame UI) read/write through these.
Output: Extended schema + `persistArtifact` mutation + new `listVariantGroup` query + a Convex round-trip test, with the live schema pushed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-preview-eval-repair/03-RESEARCH.md
@.planning/phases/03-preview-eval-repair/03-PATTERNS.md
@packages/convex/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive schema fields — variantGroupId + D-13 version object (VER-01/CANVAS-05)</name>
  <read_first>
    - packages/convex/convex/schema.ts (the file being modified — read the `experienceArtifacts` block ~line 2007 and `experienceArtifactVersions` block ~line 2025-2052, including the `compiledBundle` additive `v.optional` + comment that demonstrates the no-migration pattern)
    - packages/convex/convex/_generated/ai/guidelines.md (MANDATORY per packages/convex/CLAUDE.md — Convex schema/index rules)
    - .planning/phases/03-preview-eval-repair/03-PATTERNS.md (§ "schema.ts (EXTEND — additive, D-12/D-13)" — exact field list)
    - .planning/phases/03-preview-eval-repair/03-RESEARCH.md (§ Runtime State Inventory — additive, no migration)
  </read_first>
  <action>
    On `experienceArtifacts`: add `variantGroupId: v.optional(v.string())` (D-12 — NO new variant table) and add an index `by_variant_group` on `['variantGroupId']`. On `experienceArtifactVersions`: add `previewState: v.optional(v.any())`, `thumbnail: v.optional(v.id('_storage'))`, `evaluation: v.optional(v.any())`, and `originRunId: v.optional(v.id('experienceRuns'))` (D-13 — the version already has `runId`; `originRunId` records the run that first originated the artifact lineage). All fields are `v.optional` so existing rows round-trip with NO data migration, exactly mirroring the `compiledBundle` field's comment + pattern. Keep `parentVersionId` and the `by_artifact` index unchanged.
  </action>
  <verify>
    <automated>pnpm --filter @oneui/convex typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "variantGroupId\|by_variant_group" packages/convex/convex/schema.ts` shows the field + index.
    - `grep -n "previewState\|thumbnail\|evaluation" packages/convex/convex/schema.ts` shows all three D-13 fields under `experienceArtifactVersions`.
    - Every new field is wrapped in `v.optional(...)` (no required field added — append-only).
    - `pnpm --filter @oneui/convex typecheck` zero errors.
  </acceptance_criteria>
  <done>Schema carries `variantGroupId` + the full D-13 version object, all additive `v.optional`, existing rows round-trip.</done>
</task>

<task type="auto">
  <name>Task 2: Extend persistArtifact + add listVariantGroup query (VER-01/VER-02/CANVAS-05)</name>
  <read_first>
    - packages/convex/convex/experienceRuns.ts (the file being modified — `persistArtifact` mutation lines 89-144, the artifact insert lines 116-123, the version insert lines 128-136, `getArtifactHistory` query lines 173-184, `listRunsByBrand` `withIndex` scan lines 159-167 as the query analog)
    - packages/convex/convex/_generated/ai/guidelines.md (MANDATORY — Convex mutation/query/validator conventions)
    - .planning/phases/03-preview-eval-repair/03-PATTERNS.md (§ "experienceRuns.ts (EXTEND — VER-01/02)")
  </read_first>
  <action>
    Extend `persistArtifact` args with the new optional D-13 fields (`previewState: v.optional(v.any())`, `thumbnail: v.optional(v.id('_storage'))`, `evaluation: v.optional(v.any())`, `originRunId: v.optional(v.id('experienceRuns'))`) and `variantGroupId: v.optional(v.string())`. Write `variantGroupId` into the `experienceArtifacts` insert (lines 116-123) and the new version fields into the `experienceArtifactVersions` insert (lines 128-136). Keep the existing 3-step pattern (artifact identity → version → wire current-version pointer) and the append-only invariant — never patch/delete an existing version. Add a `listVariantGroup` query taking `{ variantGroupId: v.string() }` that scans `experienceArtifacts` via the `by_variant_group` index (mirror `listRunsByBrand`'s `withIndex` scan) and returns the sibling artifacts. `getArtifactHistory` (the VER-02 version-chain read) stays as-is.
  </action>
  <verify>
    <automated>pnpm --filter @oneui/convex typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "listVariantGroup\|by_variant_group" packages/convex/convex/experienceRuns.ts` shows the query + index scan.
    - `grep -n "previewState\|thumbnail\|evaluation\|variantGroupId" packages/convex/convex/experienceRuns.ts` shows the new args threaded into the inserts.
    - No `ctx.db.delete`/destructive patch on existing versions (append-only) — `grep -c "\.delete(" packages/convex/convex/experienceRuns.ts` returns 0.
    - `pnpm --filter @oneui/convex typecheck` zero errors.
  </acceptance_criteria>
  <done>persistArtifact persists the full version object + variantGroupId; listVariantGroup reads sibling artifacts by group.</done>
</task>

<task type="auto">
  <name>Task 3: Convex round-trip + variant-group test + push schema (VER-01/VER-02/CANVAS-05)</name>
  <read_first>
    - packages/convex/convex/experienceRuns.ts (the mutations/queries under test — from Tasks 1-2)
    - packages/convex/convex/_generated/ai/guidelines.md (MANDATORY — convex-test patterns if used)
    - any existing `*.test.ts` under packages/convex/convex/ (the test idiom analog; if none exists, create the first using the convex-test harness convention)
    - .planning/phases/03-preview-eval-repair/03-RESEARCH.md (§ Validation Architecture — Wave-0 gap "Convex test for additive fields + variantGroupId round-trip")
  </read_first>
  <behavior>
    - Test (VER-01): `persistArtifact` with all D-13 fields set, then `getArtifactHistory` returns a version carrying `previewState`, `thumbnail`, `evaluation`, `originRunId`, `compiledBundle`, `ir`, `validation`, `parentVersionId`.
    - Test (round-trip / no migration): a version persisted WITHOUT the new fields reads back without error (existing-row round-trip).
    - Test (CANVAS-05): two artifacts persisted with the same `variantGroupId` are both returned by `listVariantGroup`.
  </behavior>
  <action>
    Create `experienceRuns.test.ts` using the convex-test harness (per packages/convex guidelines). Cover: full-version persist + `getArtifactHistory` read-back (VER-01/VER-02), a no-new-fields round-trip (append-only safety), and a two-sibling `listVariantGroup` group read (CANVAS-05). After tests pass, push the live schema so the verification gate sees the new fields/index (the project pushes the schema before verification — mirror the Phase 2 P4 `T-02-14` schema-push step noted in STATE.md).
  </action>
  <verify>
    <automated>pnpm --filter @oneui/convex test -t variantGroup</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/convex test` passes including the round-trip + variant-group + version-chain assertions.
    - `npx convex dev --once` (or the project's schema-push command) applies the additive schema without a destructive-migration prompt.
    - `pnpm --filter @oneui/convex typecheck` zero errors.
  </acceptance_criteria>
  <done>Version persistence + variant grouping are tested green and the live schema is pushed additively.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow/run-route → Convex | Server writes version + variant rows; thumbnails/screenshots go to `_storage` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03-MIG | Tampering | additive schema | mitigate | All new fields `v.optional`; no destructive migration; append-only (existing rows round-trip — tested). |
| T-3-03-LEAK | Information Disclosure | thumbnail/screenshot storage path | mitigate | Thumbnails stored as `_storage` ids; no auth/Convex token is written into `previewState`/`evaluation` (the version object carries render artifacts only). |
| T-3-03-SC | Tampering | npm/pip/cargo installs | accept | No package install in this plan (Convex already installed). |
</threat_model>

<verification>
- `pnpm --filter @oneui/convex test` green.
- `pnpm --filter @oneui/convex typecheck` zero errors.
- Live schema pushed additively (no destructive migration).
</verification>

<success_criteria>
Artifact versions persist the full VER-01 object and are browsable (VER-02 via `getArtifactHistory`), best-of-N siblings cluster via `variantGroupId` and read back via `listVariantGroup` (CANVAS-05), and every change is additive append-only with no migration.
</success_criteria>

<output>
Create `.planning/phases/03-preview-eval-repair/03-03-SUMMARY.md` when done
</output>
