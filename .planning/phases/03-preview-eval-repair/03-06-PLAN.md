---
phase: 03-preview-eval-repair
plan: 06
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - apps/platform/src/app/(platform)/(experience-lab)/_canvas/shapes/ArtifactCardShape.tsx
  - apps/platform/src/app/(platform)/(experience-lab)/_canvas/frames/VariantGroupFrame.tsx
  - apps/platform/src/app/(platform)/(experience-lab)/_panels/VersionTimelinePanel.tsx
  - apps/platform/src/app/api/experience-lab/run/route.ts
  - apps/platform/src/app/(platform)/(experience-lab)/__tests__/versionTimeline.test.tsx
autonomous: true
requirements: [CANVAS-06, CANVAS-05, VER-01, VER-02, PREV-03, INPUT-05]
must_haves:
  truths:
    - "An artifact card renders a real-DOM live iframe of the preview (separate-origin URL from previewState), not raster, not dangerouslySetInnerHTML (CANVAS-06)"
    - "The card lifecycle progresses thumbnail → lightweight → live (PREV-03) keeping the canvas responsive with many cards"
    - "Sibling variant cards (shared variantGroupId) render inside one tldraw frame (CANVAS-05/D-14)"
    - "A per-card version-timeline panel browses the persisted version chain via getArtifactHistory (VER-02/D-14)"
    - "The user can iterate on an existing artifact (chat/action) seeding a new run from parentVersionId (INPUT-05)"
    - "The run route persists the full VER-01 version object (previewState, evaluation, thumbnail _storage id, originRunId) — none stay null after a successful run"
  artifacts:
    - path: "apps/platform/src/app/(platform)/(experience-lab)/_canvas/frames/VariantGroupFrame.tsx"
      provides: "tldraw frame grouping variant siblings (CANVAS-05)"
      contains: "FrameShapeUtil"
    - path: "apps/platform/src/app/(platform)/(experience-lab)/_panels/VersionTimelinePanel.tsx"
      provides: "VER-02 per-card version timeline"
      contains: "getArtifactHistory"
    - path: "apps/platform/src/app/(platform)/(experience-lab)/_canvas/shapes/ArtifactCardShape.tsx"
      provides: "live-iframe + lifecycle (CANVAS-06/PREV-03)"
      contains: "iframe"
  key_links:
    - from: "ArtifactCardShape"
      to: "previewState live URL"
      via: "sandboxed iframe (allow-scripts, no allow-same-origin)"
      pattern: "sandbox="
    - from: "VersionTimelinePanel"
      to: "Convex getArtifactHistory"
      via: "useQuery"
      pattern: "useQuery"
    - from: "run route"
      to: "parentVersionId iterate"
      via: "POST body seeds a new run from a prior version"
      pattern: "parentVersionId"
    - from: "run route persistArtifact call"
      to: "experienceArtifactVersions D-13 fields + _storage thumbnail"
      via: "pass run.previewState/evaluation + uploaded thumbnail storageId"
      pattern: "previewState|generateUploadUrl"
---

<objective>
Surface the closed quality loop on the canvas (D-14): the artifact card renders a real-DOM live iframe of the sandboxed preview (CANVAS-06) with the PREV-03 thumbnail→lightweight→live lifecycle, variant siblings cluster in a tldraw frame (CANVAS-05), a per-card version-timeline panel browses the persisted chain (VER-02), and the user can iterate on an existing artifact (INPUT-05) by seeding a new run from a `parentVersionId`. This is the user-facing slice that makes Phase 3 visible and interactive.

Purpose: VER-02/CANVAS-05/06 are user-observable behaviors; INPUT-05 is the iterate-on-artifact action. All chrome is token-only Jio DS + `<Surface>` (LAB-02), and the shapes extend tldraw base utils WITHOUT importing the existing Builder's utils (LAB-03 isolation).
Output: Extended `ArtifactCardShape`, new `VariantGroupFrame` + `VersionTimelinePanel`, an extended run route for iterate, and a jsdom test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-preview-eval-repair/03-RESEARCH.md
@.planning/phases/03-preview-eval-repair/03-PATTERNS.md
@.planning/phases/03-preview-eval-repair/03-02-SUMMARY.md
@.planning/phases/03-preview-eval-repair/03-03-SUMMARY.md
@.planning/phases/03-preview-eval-repair/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-iframe + lifecycle on the artifact card (CANVAS-06/PREV-03) + variant frame (CANVAS-05)</name>
  <read_first>
    - apps/platform/src/app/(platform)/(experience-lab)/_canvas/shapes/ArtifactCardShape.tsx (the file being modified — read in full; ShapeUtil pattern lines 56-91: static `type`, `props: RecordProps` with `T.number`/`T.jsonValue`, `getDefaultProps`, `getGeometry` Rectangle2d, `component`→`ArtifactCardBody`, `indicator`; existing `<Surface mode="subtle">` chrome + `ToggleGroup` IR/JSON inspector; `editor.markEventAsHandled` convention)
    - apps/platform/src/app/(platform)/(experience-lab)/_canvas/frames/RunGroupFrame.tsx (EXACT analog for VariantGroupFrame — `patchFrameBodyFill` + `RunGroupFrameShapeUtil extends FrameShapeUtil` lines 67-74; `RUN_FRAME_FILL` surface token; the "Run #N" name-prop label convention)
    - packages/experience-builder-preview/src/lifecycle.ts (the lifecycle state machine from Plan 02 — `PreviewLifecycleState`, `nextLifecycleState`, `framingForProfile`)
    - packages/experience-builder-preview/PREVIEW-DECISION.md (iframe sandbox attrs: `allow-scripts` WITHOUT `allow-same-origin`, frame from the separate preview origin)
    - .planning/phases/03-preview-eval-repair/03-PATTERNS.md (§ "ArtifactCardShape.tsx (EXTEND)", § "VariantGroupFrame.tsx (NEW)")
    - CLAUDE.md (Surface Context rule — never raw `<div style={{background}}>`; token-only)
  </read_first>
  <action>
    Extend `ArtifactCardShapeProps` with `previewUrl: T.string` (the immutable separate-origin URL from `previewState`), `thumbnailUrl: T.string`, `variantGroupId: T.string`, and a `lifecycle: T.string` render-state. In `ArtifactCardBody`, augment (do NOT remove) the existing IR/JSON inspector with a real-DOM preview region driven by the lifecycle from `lifecycle.ts`: `thumbnail` shows the `_storage` thumbnail image, `lightweight` shows a static frame, `live` embeds a `<iframe src={previewUrl} sandbox="allow-scripts" />` (NO `allow-same-origin` — PREV-01) pointing at the separate preview origin. NEVER use `dangerouslySetInnerHTML` and NEVER raster-flatten the web UI (CANVAS-06). Advance the lifecycle via `nextLifecycleState` (e.g. on viewport entry / hover) to bound canvas cost (PREV-03). Keep all chrome token-only with `<Surface>` containers (LAB-02). Create `VariantGroupFrame.tsx` mirroring `RunGroupFrame.tsx` EXACTLY: `patchFrameBodyFill` copied verbatim + a `VariantGroupFrameShapeUtil extends FrameShapeUtil` overriding `component` to patch the body fill to a surface token; the frame groups artifact cards sharing a `variantGroupId` with a "Variant Group" label (D-14). Do NOT import any `OneUI*ShapeUtil` from the Builder (LAB-03).
  </action>
  <verify>
    <automated>pnpm --filter @oneui/platform typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sandbox=\"allow-scripts\"\|<iframe" "apps/platform/src/app/(platform)/(experience-lab)/_canvas/shapes/ArtifactCardShape.tsx"` shows the live iframe with `allow-scripts` and NO `allow-same-origin`.
    - `grep -c "dangerouslySetInnerHTML" "apps/platform/src/app/(platform)/(experience-lab)/_canvas/shapes/ArtifactCardShape.tsx"` returns 0.
    - `grep -n "FrameShapeUtil\|patchFrameBodyFill\|variantGroup" "apps/platform/src/app/(platform)/(experience-lab)/_canvas/frames/VariantGroupFrame.tsx"` shows the frame util.
    - `grep -rn "OneUIFrameShapeUtil\|OneUIShape" "apps/platform/src/app/(platform)/(experience-lab)/_canvas/frames/VariantGroupFrame.tsx"` returns nothing (LAB-03 isolation).
    - `grep -n "nextLifecycleState\|thumbnail\|live" "apps/platform/src/app/(platform)/(experience-lab)/_canvas/shapes/ArtifactCardShape.tsx"` shows the lifecycle.
  </acceptance_criteria>
  <done>The artifact card renders a real-DOM sandboxed live iframe with a thumbnail→lightweight→live lifecycle; variant siblings group in an isolated tldraw frame.</done>
</task>

<task type="auto">
  <name>Task 2: Wire VER-01 persistence end-to-end + VersionTimelinePanel + iterate run route (VER-01/VER-02/CANVAS-05/INPUT-05)</name>
  <read_first>
    - apps/platform/src/app/(platform)/(experience-lab)/_panels/RunInspectorPanel.tsx (panel structure analog — read in full; `<Surface>` chrome, token-only, panel layout)
    - packages/convex/convex/experienceRuns.ts (the `getArtifactHistory` query VER-02 reads + the `listVariantGroup` query from Plan 03)
    - apps/platform/src/app/api/experience-lab/run/route.ts (the file being modified — read in full; the run delegation to `runExperienceWorkflow`, the `RunRequestBody.strict()` Zod body, the NDJSON frame streaming, the `foundationsLoader` injection)
    - packages/experience-builder-agents/src/index.ts (the `runExperienceWorkflow`/`runVariants` exports + `RunExperienceInput` carrying iterate context from Plan 04)
    - .planning/phases/03-preview-eval-repair/03-PATTERNS.md (§ "VersionTimelinePanel.tsx (NEW)")
    - .planning/phases/03-preview-eval-repair/03-RESEARCH.md (§ INPUT-05 "new run seeded from parentVersionId"; § VER-02 getArtifactHistory)
    - apps/platform/src/app/api/experience-lab/run/route.ts (the `persistArtifact` CALL site lines 92-103 — currently passes only `runId/brandId/artifactType/outputProfile/ir/validation/compiledBundle`; the new D-13 fields are NOT yet wired and stay null without this task)
    - apps/platform/src/app/api/composition/verify/route.ts (the VERIFIED `_storage` upload analog lines 163-171: `convex.mutation(api.renderedScreenshots.generateUploadUrl, {})` → POST the PNG → read back `{ storageId }: Id<'_storage'>`)
    - packages/convex/convex/experienceRuns.ts (the EXTENDED `persistArtifact` args from Plan 03 Task 2: `previewState`, `thumbnail` (`v.id('_storage')`), `evaluation`, `originRunId`, `variantGroupId`)
    - packages/experience-builder-agents/src/index.ts (the EXTENDED `RunExperienceResult` from Plan 04 Task 3 carrying `previewState`/`evaluation`; the per-profile `screenshots` from the executor; `variantGroupId` from `runVariants`)
  </read_first>
  <action>
    Create `VersionTimelinePanel.tsx`: read `useQuery(api.experienceRuns.getArtifactHistory, { artifactId })` and render the version chain (ordered by `parentVersionId` lineage), each entry showing version metadata (validation passed?, evaluation composite, created time) — a per-card timeline (D-14/VER-02). Token-only chrome, `<Surface>` containers, `@oneui/ui` components only (LAB-02). Extend the run route `RunRequestBody.strict()` Zod schema with optional iterate fields: `parentVersionId?` and `parentIr?` (or an `artifactId` to seed from). When present, the route seeds a new `runExperienceWorkflow` run from the prior version's IR (INPUT-05 — iterate via patch-based re-run, reusing the run→workflow seam) and streams the same NDJSON event frames. Keep the existing brand/sub-brand foundations-loader injection untouched; preserve `runtime='nodejs'`.

    VER-01 END-TO-END WIRING (the blocker fix): in the run route's existing `persistArtifact` CALL (lines 92-103), pass the new D-13 fields from the completed `runExperienceWorkflow` result — `...(run.previewState ? { previewState: run.previewState } : {})`, `...(run.evaluation ? { evaluation: run.evaluation } : {})`, `...(run.variantGroupId ? { variantGroupId: run.variantGroupId } : {})`, and `originRunId: persistedRunId`. For the `thumbnail`, BEFORE the `persistArtifact` call upload the first per-profile screenshot PNG (`run.screenshots?.[0]?.png`) to Convex `_storage` using the VERIFIED analog from `composition/verify/route.ts:163-171`: `const uploadUrl = await convex.mutation(api.renderedScreenshots.generateUploadUrl, {})`, POST the PNG buffer to `uploadUrl`, read back `{ storageId }: Id<'_storage'>`, then pass `thumbnail: storageId`. Guard the whole upload so a screenshot/upload failure is non-fatal (mirror the existing try/catch that wraps persistence) — a missing thumbnail must NOT abort persistence. Result: after all 6 plans run, a persisted `experienceArtifactVersions` row carries a non-null `previewState`, `thumbnail`, `evaluation`, `originRunId` (and `variantGroupId` on the artifact for best-of-N), so the VER-02 timeline (this task's panel) renders real evaluation/preview data, not blanks.
  </action>
  <verify>
    <automated>pnpm --filter @oneui/platform test -t versionTimeline</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "getArtifactHistory\|useQuery" "apps/platform/src/app/(platform)/(experience-lab)/_panels/VersionTimelinePanel.tsx"` shows the VER-02 read.
    - `grep -n "parentVersionId" apps/platform/src/app/api/experience-lab/run/route.ts` shows the INPUT-05 iterate seed in the body schema.
    - `grep -n "previewState\\|evaluation\\|thumbnail\\|variantGroupId\\|generateUploadUrl" apps/platform/src/app/api/experience-lab/run/route.ts` shows the new D-13 fields + the `_storage` thumbnail upload wired into the `persistArtifact` call.
    - A test (or the Task 3 jsdom/integration test) asserts that after a successful run, the `persistArtifact` mutation is invoked with non-empty `previewState`, `evaluation`, and a `thumbnail` storageId (VER-01 end-to-end — not null).
    - `grep -rn "<div style={{ background\|dangerouslySetInnerHTML" "apps/platform/src/app/(platform)/(experience-lab)/_panels/VersionTimelinePanel.tsx"` returns nothing (LAB-02 token-only / Surface rule).
    - `pnpm --filter @oneui/platform typecheck` zero errors.
  </acceptance_criteria>
  <done>The run route persists the FULL VER-01 version object (previewState/evaluation/thumbnail/originRunId + variantGroupId) including a `_storage` thumbnail upload; the per-card version timeline browses the persisted chain; the route accepts an iterate request seeded from a prior version.</done>
</task>

<task type="auto">
  <name>Task 3: jsdom canvas/panel test (CANVAS-05/06/VER-02) + existing-Builder isolation check</name>
  <read_first>
    - apps/platform/src/app/(platform)/(experience-lab)/__tests__/canvas.test.tsx (the jsdom canvas test idiom analog — read in full)
    - apps/platform/src/app/(platform)/(experience-lab)/_panels/VersionTimelinePanel.tsx (from Task 2)
    - apps/platform/src/app/(platform)/(experience-lab)/_canvas/shapes/ArtifactCardShape.tsx (from Task 1)
    - .planning/phases/03-preview-eval-repair/03-RESEARCH.md (§ Validation Architecture — CANVAS-06 "real-DOM live iframe", VER-02)
  </read_first>
  <behavior>
    - Test (CANVAS-06): an artifact card in `live` lifecycle renders an `<iframe>` with `sandbox="allow-scripts"` and NO `allow-same-origin`; in `thumbnail` it renders the thumbnail image, not an iframe.
    - Test (VER-02): `VersionTimelinePanel` with a mocked `getArtifactHistory` renders one timeline entry per version in chain order.
    - Test (CANVAS-05): cards sharing a `variantGroupId` are associated with the variant frame.
    - Test (isolation): the existing Builder canvas still mounts unchanged (no import from `(builder)` introduced — mirror the existing isolation assertion).
  </behavior>
  <action>
    Create `versionTimeline.test.tsx` (jsdom, mock Convex `useQuery`) asserting the CANVAS-06 lifecycle iframe behavior, the VER-02 timeline render, and the CANVAS-05 variant association. Add/extend an isolation assertion that no `(builder)` import crept into the new/modified files (mirror the existing Lab isolation test). Run the existing Builder smoke test to confirm LAB-03 still holds.
  </action>
  <verify>
    <automated>pnpm --filter @oneui/platform test -t "versionTimeline|canvas"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/platform test -t "versionTimeline|canvas"` passes including the lifecycle-iframe + timeline + variant assertions.
    - The isolation assertion confirms no `(builder)` import in the Phase 3 Lab files (LAB-03).
    - `pnpm check:literals` reports zero violations for the modified Lab files (LAB-02).
  </acceptance_criteria>
  <done>Canvas live-iframe lifecycle, version timeline, and variant grouping are tested green, and the existing Builder remains isolated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lab canvas → preview iframe | The card embeds an untrusted separate-origin preview |
| run route → workflow (iterate) | An iterate request seeds a new run from a prior version |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-06-IFRAME | Information Disclosure | live-iframe in ArtifactCardShape | mitigate | `<iframe sandbox="allow-scripts">` WITHOUT `allow-same-origin`, pointed at the separate preview origin; no token in src; no `dangerouslySetInnerHTML` (PREV-01/CANVAS-06). |
| T-3-06-ISO | Tampering | Lab shapes/panels | mitigate | Extend tldraw base utils without importing Builder utils; isolation test + Builder smoke test (LAB-03). |
| T-3-06-LIT | Tampering | Lab UI chrome | mitigate | Token-only `<Surface>` chrome; `pnpm check:literals` zero violations (LAB-02). |
| T-3-06-SC | Tampering | npm/pip/cargo installs | accept | No package install in this plan (uses installed tldraw/Convex/@oneui/ui). |
</threat_model>

<verification>
- `pnpm --filter @oneui/platform test` green (canvas + versionTimeline).
- `pnpm --filter @oneui/platform typecheck` zero errors.
- `pnpm check:literals` zero violations for Lab files.
- Existing Builder smoke test passes (LAB-03 isolation intact).
</verification>

<success_criteria>
The artifact card renders a real-DOM sandboxed live iframe with a bounded lifecycle (CANVAS-06/PREV-03), variants group on the canvas (CANVAS-05), the version history is browsable per card (VER-02), the user can iterate on an artifact (INPUT-05), and the existing Builder stays isolated and the Lab stays token-only.
</success_criteria>

<output>
Create `.planning/phases/03-preview-eval-repair/03-06-SUMMARY.md` when done
</output>
