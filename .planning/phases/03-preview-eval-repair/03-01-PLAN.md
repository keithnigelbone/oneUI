---
phase: 03-preview-eval-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/experience-builder-validation/src/astValidator.ts
  - packages/experience-builder-validation/src/fixtures/redteam.ts
  - packages/experience-builder-validation/src/astValidator.test.ts
autonomous: true
requirements: [VAL-04, VAL-05, VAL-06]
must_haves:
  truths:
    - "An inline hex / px / rem / unapproved-font literal on a real visual prop is a BLOCKING violation, not a warning"
    - "A fake var() that is not a manifest-allowed --Token is blocked; a genuine var(--Token) passes"
    - "100% of the red-team evasion corpus (aliased import, inline hex, fake var(), dynamic className, aliased font/icon) is blocked"
    - "validateAst still returns a JioValidationResult on every branch (passed iff zero blocking)"
  artifacts:
    - path: "packages/experience-builder-validation/src/astValidator.ts"
      provides: "VAL-04 literal blocking promoted from warning skeleton"
      contains: "literal-value-hook"
    - path: "packages/experience-builder-validation/src/fixtures/redteam.ts"
      provides: "Extended VAL-05 evasion corpus"
      contains: "REDTEAM_FIXTURES"
    - path: "packages/experience-builder-validation/src/astValidator.test.ts"
      provides: "VAL-04/05 literal + corpus coverage"
      contains: "literal"
  key_links:
    - from: "astValidator.ts checkLiteralHook"
      to: "acc.blocking"
      via: "promoted severity + walkNode push site"
      pattern: "acc\\.blocking\\.push.*[Ll]iteral"
---

<objective>
Tighten the deterministic AST validator into the Phase 3 objective compliance track (per D-06): promote the literal-value hook from warning-only (Phase 1 skeleton, `astValidator.ts:265`) to BLOCKING for VAL-04, extend coverage to radius/elevation/motion/font/icon literals, and grow the VAL-05 red-team corpus so 100% of the evasion set is blocked. This is the deterministic half of the quality loop — a blocking violation here short-circuits straight to repair with NO model call (D-06).

Purpose: VAL-04/05/06 are the hard pass/fail gate the evaluate step (Plan 04) consumes. Without blocking literals, "AI cannot bypass the Jio Design System" stays aspirational — literals slip through (RESEARCH Pitfall 5).
Output: A blocking literal scanner + extended corpus + tests, all flowing through the frozen `JioValidationResult.parse` contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-preview-eval-repair/03-RESEARCH.md
@.planning/phases/03-preview-eval-repair/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote literal hook to blocking + extend literal coverage (VAL-04/VAL-06)</name>
  <read_first>
    - packages/experience-builder-validation/src/astValidator.ts (the file being modified — read in full; `checkLiteralHook` lines 265-286, `blocking()` builder lines 163-169, `walkNode` push site line 368, `VISUAL_LITERAL_RE` line 263, `BRAND_ALLOWED_REGEX` import line 59, `validateAst` + `JioValidationResult.parse` line 431)
    - .planning/phases/03-preview-eval-repair/03-PATTERNS.md (§ "astValidator.ts (EXTEND — VAL-04 literal blocking)" — exact change instructions)
    - .planning/phases/03-preview-eval-repair/03-RESEARCH.md (§ Pitfall 5; § Security Domain "Literal value bypassing token system (VAL-04)")
  </read_first>
  <behavior>
    - Test: an inline hex (`#ff0000`) on a real visual prop (e.g. a `color`/`background`/`style` string) appears in `result.blocking` with code `literal-value-hook`, and `result.passed === false`.
    - Test: a `px`/`rem`/`em` literal on a visual prop is blocking.
    - Test: a fake `var(--NotAToken)` (a var() whose token is not manifest-allowed via BRAND_ALLOWED_REGEX) is blocking.
    - Test: a genuine `var(--Primary-Bold)` token reference passes (NOT in blocking).
    - Test: a radius/elevation/motion literal and an unapproved font-family / icon literal are blocking (VAL-04 "unapproved fonts/icons").
    - Test: `validateAst` still returns a contract-valid `JioValidationResult` (parse does not throw) for both pass and fail cases.
  </behavior>
  <action>
    In `checkLiteralHook`, change the pushed violation from `severity: 'warning'` to use the existing `blocking()` builder, keeping the existing code `literal-value-hook` (matches PATTERNS.md and the existing red-team fixtures; do NOT rename it) (drop the "(Full literal scan lands in P3.)" note text). At the `walkNode` call site (line ~368) push literal violations into `acc.blocking` instead of `acc.warnings`. Extend `VISUAL_LITERAL_RE` so it also matches radius/elevation/motion literals (e.g. raw `px`/`rem` already covered; add box-shadow-style multi-value literals and bare unit numbers used for radius/elevation) and unapproved font/icon literals (a `fontFamily`/`font`/`icon` string that is a raw family name rather than `var(--Typography-Font-*)`). Keep the genuine-token escape hatch: a value is allowed iff it contains `var(--` AND `BRAND_ALLOWED_REGEX.test(...)` passes on the token name (the ONE sanctioned regex, imported from `@oneui/shared/engine/tokenBoundary`) — this is what lets `var(--Primary-Bold)` pass and `var(--NotAToken)` fail. Do NOT add a string denylist; classification stays structural per-prop. Result must still flow through `JioValidationResult.parse(result)` at the end of `validateAst` (line 431) unchanged.
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-validation test -t literal</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "severity: 'warning'" packages/experience-builder-validation/src/astValidator.ts` shows the literal hook no longer warning-only (literal pushes to blocking).
    - `pnpm --filter @oneui/experience-builder-validation test -t literal` passes with the new blocking assertions.
    - A test asserting `var(--Primary-Bold)` is NOT blocking passes (token escape hatch preserved).
    - `grep -n "literal-value-hook" packages/experience-builder-validation/src/astValidator.ts` shows the code name is unchanged.
    - `pnpm --filter @oneui/experience-builder-validation typecheck` reports zero errors.
  </acceptance_criteria>
  <done>Literal color/spacing/type/radius/elevation/motion/font/icon values are blocking violations; genuine `var(--Token)` references pass; result is contract-valid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the red-team evasion corpus to 100% blocked (VAL-05)</name>
  <read_first>
    - packages/experience-builder-validation/src/fixtures/redteam.ts (the file being modified — read in full; `RedTeamFixture` interface lines 18-26, `REDTEAM_FIXTURES` array lines 29-109, existing 6 entries with `expectedBlockingCode`)
    - packages/experience-builder-validation/src/astValidator.test.ts (the corpus-assertion test to extend; read in full)
    - .planning/phases/03-preview-eval-repair/03-PATTERNS.md (§ "fixtures/redteam.ts (EXTEND — VAL-05 corpus)")
    - packages/experience-builder-validation/src/index.ts (barrel — confirms `REDTEAM_FIXTURES` export shape)
  </read_first>
  <behavior>
    - Test: iterating `REDTEAM_FIXTURES`, every fixture's `expectedBlockingCode` appears in `validateAst(fixture.ast).blocking` — 100% of the corpus is blocked, zero leaks.
    - Test: corpus length increased by the new evasion entries (inline-hex-on-visual-prop, fake `var()`, dynamic className, aliased non-Jio font, aliased non-Jio icon).
  </behavior>
  <action>
    Append `RedTeamFixture` entries to `REDTEAM_FIXTURES` (each `{ name, description, ast: ArtifactAst, expectedBlockingCode }`) covering the RESEARCH evasion vectors now caught by Task 1: (a) inline hex on a real visual prop → `expectedBlockingCode: 'literal-value-hook'`; (b) a fake `var()` whose token is not manifest-allowed → `'literal-value-hook'`; (c) a dynamic/computed className string carrying a literal → `'literal-value-hook'`; (d) an aliased non-Jio font import → `'non-jio-import'`; (e) an aliased non-Jio icon import → `'non-jio-import'`. Use `ArtifactAst`/`ArtifactAstNode` shapes exactly as the existing fixtures do (build the AST objects directly, NOT code text — the validator is structural). Extend the corpus-loop test in `astValidator.test.ts` (or add one tagged `redteam`) asserting `validateAst(fixture.ast).blocking.some(v => v.code === fixture.expectedBlockingCode)` for every fixture.
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-validation test -t redteam</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/experience-builder-validation test -t redteam` passes; every fixture is blocked.
    - `grep -c "expectedBlockingCode" packages/experience-builder-validation/src/fixtures/redteam.ts` returns ≥ 11 (6 original + 5 new).
    - No fixture relies on a string-denylist match (each is an `ArtifactAst` object).
  </acceptance_criteria>
  <done>The red-team corpus contains the new evasion vectors and the validator blocks 100% of them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-authored IR → validator | Untrusted generated component tree crosses into the deterministic compliance gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01-LIT | Tampering | `checkLiteralHook` in astValidator.ts | mitigate | Promote literal violations to BLOCKING; classify token refs only via `BRAND_ALLOWED_REGEX` (manifest-derived allowlist, not denylist). |
| T-3-01-ALI | Tampering | import allowlist (aliased font/icon smuggling) | mitigate | AST alias-resolution allowlist already structural; red-team corpus extended with aliased font/icon entries proving 100% block. |
| T-3-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (validator-only edits). Daytona supply-chain gate lives in Plan 05. |
</threat_model>

<verification>
- `pnpm --filter @oneui/experience-builder-validation test` green.
- `pnpm --filter @oneui/experience-builder-validation typecheck` zero errors.
- Red-team corpus 100% blocked (VAL-05 phase-gate criterion).
</verification>

<success_criteria>
Literal visual values are blocking (VAL-04), the validator is AST-allowlist-based and the red-team corpus is 100% blocked (VAL-05), and the validator confirms structural compliance feeding the compile/render gate (VAL-06 deterministic half).
</success_criteria>

<output>
Create `.planning/phases/03-preview-eval-repair/03-01-SUMMARY.md` when done
</output>
