---
phase: 03-preview-eval-repair
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/experience-builder-preview/src/PreviewExecutor.ts
  - packages/experience-builder-preview/src/IframeCspExecutor.ts
  - packages/experience-builder-preview/src/lifecycle.ts
  - packages/experience-builder-preview/src/index.ts
  - packages/experience-builder-preview/src/PreviewExecutor.test.ts
  - packages/experience-builder-preview/vitest.config.ts
  - packages/experience-builder-preview/tsconfig.json
  - packages/experience-builder-preview/package.json
autonomous: true
requirements: [PREV-01, PREV-02, PREV-03, PREV-04, CANVAS-06]
must_haves:
  truths:
    - "The workflow can call PreviewExecutor.render({bundle, brandId, profiles}) and receive per-profile screenshots + an immutable previewState + a rendered flag"
    - "IframeCspExecutor renders the compiled bundle on a separate origin via short-lived single-use token handoff — zero auth/Convex tokens reach the preview"
    - "Tests run credential-free against a mock executor via setPreviewExecutor (no Daytona, no live browser)"
    - "The lifecycle helper exposes thumbnail → lightweight → live states for desktop/mobile/fixed profiles"
  artifacts:
    - path: "packages/experience-builder-preview/src/PreviewExecutor.ts"
      provides: "The D-02 seam: PreviewExecutor interface + RenderInput/RenderResult + test override"
      contains: "interface PreviewExecutor"
    - path: "packages/experience-builder-preview/src/IframeCspExecutor.ts"
      provides: "MVP de-risk executor: token-handoff + Playwright capture"
      contains: "IframeCspExecutor"
    - path: "packages/experience-builder-preview/src/lifecycle.ts"
      provides: "PREV-03 thumbnail→lightweight→live state machine + profile framing"
      contains: "lifecycle"
    - path: "packages/experience-builder-preview/src/PreviewExecutor.test.ts"
      provides: "Credential-free seam + lifecycle + previewState tests"
      contains: "previewState"
  key_links:
    - from: "IframeCspExecutor"
      to: "playwrightRenderer token-handoff + capture semantics"
      via: "randomUUID 60s TTL cache + per-viewport screenshot"
      pattern: "randomUUID"
    - from: "PreviewExecutor.ts"
      to: "no vendor import"
      via: "seam contains no @daytonaio/sdk import"
      pattern: "PreviewExecutor"
---

<objective>
Build the net-new `experience-builder-preview` package: the `PreviewExecutor` seam (D-02) plus the `IframeCspExecutor` MVP de-risk path (reuses the verified `playwrightRenderer.ts` token-handoff + headless-capture harness) and the PREV-03 lifecycle state machine. This is the thinnest real preview slice that lets a compiled bundle render as real DOM on a separate, credential-free origin and be screenshotted — closing CANVAS-06 + PREV-01/02/03/04 without any vendor dependency. The Daytona prod executor (Plan 05) drops in behind the same seam.

Purpose: The seam keeps the agents/workflow vendor-free and testable credential-free, and the IframeCsp path ships the full preview→screenshot capability so the eval/repair/version loop (Plan 04) and canvas UX (Plan 06) have a real executor to call from day one (RESEARCH primary recommendation).
Output: A built, workspace-linked preview package exporting `PreviewExecutor`, `IframeCspExecutor`, `setPreviewExecutor`, lifecycle helpers, with credential-free tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-preview-eval-repair/03-RESEARCH.md
@.planning/phases/03-preview-eval-repair/03-PATTERNS.md
@packages/experience-builder-preview/PREVIEW-DECISION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the package build/test config + the PreviewExecutor seam (PREV-01/02)</name>
  <read_first>
    - packages/experience-builder-preview/package.json (the stub being extended — current scripts/deps)
    - packages/experience-builder-preview/PREVIEW-DECISION.md (the separate-origin + CSP + token-handoff authority — read in full)
    - packages/experience-builder-agents/src/modelAdapter.ts (the seam idiom analog: `CallModelArgs` interface lines 73-82, module-level `_callModelImpl` + `callModel` lines 105-114, `__setCallModelImpl` lines 121-129)
    - packages/experience-builder-validation/src/index.ts (barrel analog — exact shape to mirror)
    - packages/experience-builder-validation/vitest.config.ts (sibling test config to mirror for the new package)
    - .planning/phases/03-preview-eval-repair/03-RESEARCH.md (§ Pattern 1 — the verbatim `RenderInput`/`RenderResult`/`PreviewExecutor` shape; § "Recommended Project Structure")
    - .planning/phases/03-preview-eval-repair/03-PATTERNS.md (§ "PreviewExecutor.ts (interface, seam)")
  </read_first>
  <action>
    Add `vitest.config.ts`, `tsconfig.json`, and a build script + barrel to the package mirroring a sibling experience-builder package (use `@oneui/experience-builder-validation` as the template: same tsup/tsc build, same vitest config, same `index.ts` barrel comment style). Create `PreviewExecutor.ts` defining the seam VERBATIM from RESEARCH Pattern 1: `PreviewProfile` (`name: 'desktop'|'mobile'|'fixed'; width; height`), `RenderInput` (`bundle: string; brandId: string; profiles: PreviewProfile[]`), `RenderResult` (`screenshots: Array<{profile; png: Buffer}>; previewState: {url?; expiresAt?}; rendered: boolean`), and `interface PreviewExecutor { render(input): Promise<RenderResult> }`. Add the dependency-injection-for-tests seam copied from `modelAdapter.ts`: a module-level `_previewExecutorImpl`, an exported `getPreviewExecutor()`/`setPreviewExecutor(impl)` returning a restore fn. INVARIANT: this file imports NO `@daytonaio/sdk` and NO `playwright` — only impls import vendors. Export everything from `index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-preview typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/experience-builder-preview/vitest.config.ts` and `tsconfig.json` exist and mirror the validation package.
    - `grep -L "@daytonaio/sdk\|from 'playwright'" packages/experience-builder-preview/src/PreviewExecutor.ts` confirms no vendor import in the seam.
    - `grep -n "interface PreviewExecutor\|setPreviewExecutor\|RenderResult" packages/experience-builder-preview/src/PreviewExecutor.ts` shows all three.
    - `pnpm --filter @oneui/experience-builder-preview typecheck` zero errors.
  </acceptance_criteria>
  <done>The preview package builds and exports a vendor-free `PreviewExecutor` seam with a test override.</done>
</task>

<task type="auto">
  <name>Task 2: IframeCspExecutor (token-handoff + Playwright capture) + lifecycle state machine (PREV-03/04, CANVAS-06)</name>
  <read_first>
    - apps/platform/src/lib/playwrightRenderer.ts (EXACT analog — read in full; token-handoff `renderCache` Map + `TOKEN_TTL_MS` + `publishASTForRender`/`consume*` lines 42-70, `DEFAULT_VIEWPORTS` lines 25-29, capture loop with dynamic `import('playwright')` + `document.fonts.ready` + double-rAF settle + `page.screenshot` lines 145-200)
    - apps/platform/src/app/internal/render-ast/page.tsx (the render-target page that resolves a token and mounts the artifact inside the brand-CSS cascade)
    - packages/experience-builder-preview/PREVIEW-DECISION.md (separate-origin sandbox attrs + CSP controls table)
    - packages/experience-builder-preview/src/PreviewExecutor.ts (the seam created in Task 1 — implement against it)
    - .planning/phases/03-preview-eval-repair/03-PATTERNS.md (§ "IframeCspExecutor.ts" and § "lifecycle.ts")
  </read_first>
  <action>
    Create `IframeCspExecutor.ts` implementing `PreviewExecutor`. Copy the token-handoff pattern from `playwrightRenderer.ts`: an in-memory `Map` keyed by `randomUUID()` with a 60s TTL, a `publishBundleForRender(bundle): token` and server-side `consumeBundleForRender(token)` — nothing sensitive in the URL (PREV-01 token-handoff). `render()` writes the bundle to the cache, builds a separate-origin preview URL (`?t=<token>`), then captures per-profile via a dynamic `import('playwright')`: for each profile create `browser.newContext({ viewport, deviceScaleFactor: 2 })`, `goto(url, { waitUntil: 'networkidle' })`, await `document.fonts.ready` + a double-rAF transition-suppression wait (load-bearing for brand-CSS settle), then `page.screenshot({ type: 'png', fullPage: false })`. Return `RenderResult` with one `{profile, png}` per profile, a `previewState` carrying the immutable token URL + `expiresAt` (PREV-02), and `rendered: true`. Map PREV-03 desktop/mobile/fixed onto `DEFAULT_VIEWPORTS` (mobile 390×844, desktop 1440×900) plus a `fixed` profile. Create `lifecycle.ts` exporting a `PreviewLifecycleState` union (`'thumbnail' | 'lightweight' | 'live'`) and pure transition helpers (`nextLifecycleState`, `framingForProfile(profile)`) — the canvas card (Plan 06) drives this; keep it pure/testable. INVARIANT: the workflow never imports this directly — it is injected through the seam.
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-preview test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "randomUUID\|TOKEN_TTL\|consume" packages/experience-builder-preview/src/IframeCspExecutor.ts` shows the token-handoff (no token in URL except the opaque uuid).
    - `grep -n "fonts.ready\|requestAnimationFrame" packages/experience-builder-preview/src/IframeCspExecutor.ts` shows the brand-CSS settle wait.
    - `grep -n "thumbnail\|lightweight\|live" packages/experience-builder-preview/src/lifecycle.ts` shows the three lifecycle states.
    - Playwright is loaded via dynamic `import('playwright')` (not a top-level import) — `grep -n "import('playwright')\|await import" packages/experience-builder-preview/src/IframeCspExecutor.ts`.
  </acceptance_criteria>
  <done>A compiled bundle renders on a separate origin via token handoff and is screenshotted per profile; the lifecycle state machine exists; the executor is reachable only through the seam.</done>
</task>

<task type="auto">
  <name>Task 3: Credential-free seam + previewState + lifecycle tests (Wave-0 scaffold, PREV-01/02/03)</name>
  <read_first>
    - packages/experience-builder-agents/src/testModelMock.ts (the credential-free mock-injection test idiom analog)
    - packages/experience-builder-preview/src/PreviewExecutor.ts (the seam under test — from Task 1)
    - packages/experience-builder-preview/src/lifecycle.ts (the pure helpers under test — from Task 2)
    - .planning/phases/03-preview-eval-repair/03-RESEARCH.md (§ Validation Architecture — Wave-0 gap `PreviewExecutor.test.ts` for PREV-01/02/03)
  </read_first>
  <behavior>
    - Test (PREV-01): a mock executor injected via `setPreviewExecutor` is what `getPreviewExecutor()` returns; the restore fn restores the previous impl. No `@daytonaio/sdk`/`playwright` import is required to run the suite (credential-free).
    - Test (PREV-02): `render()` on the mock returns a `previewState` with a stable url + `expiresAt`; the same token URL is reproducible (immutable per version).
    - Test (PREV-03): `nextLifecycleState('thumbnail') === 'lightweight'`, `nextLifecycleState('lightweight') === 'live'`; `framingForProfile` returns the right width/height for desktop/mobile/fixed.
  </behavior>
  <action>
    Create `PreviewExecutor.test.ts` using the `setPreviewExecutor` seam to inject a deterministic mock executor (mirror `testModelMock.ts`'s queue idiom) so the suite runs with NO Daytona key and NO live browser. Assert the seam override/restore, the `previewState` immutability shape (PREV-02), and the `lifecycle.ts` transitions + per-profile framing (PREV-03). This is the RESEARCH Wave-0 gap closure for the preview package.
  </action>
  <verify>
    <automated>pnpm --filter @oneui/experience-builder-preview test -t PreviewExecutor</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/experience-builder-preview test` passes with zero env vars set (credential-free).
    - The test file imports `setPreviewExecutor`/`getPreviewExecutor` and asserts override + restore.
    - Lifecycle transition + framing assertions pass.
  </acceptance_criteria>
  <done>The preview package has a green credential-free test suite covering the seam, previewState immutability, and the lifecycle state machine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lab origin → preview origin | Untrusted compiled bundle crosses from authenticated Lab to a credential-free separate origin |
| preview iframe → host session | Generated code must not reach cookies/Convex tokens/auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02-EXFIL | Information Disclosure | preview iframe (IframeCspExecutor) | mitigate | Separate origin + `sandbox="allow-scripts"` WITHOUT `allow-same-origin` + strict CSP + token-handoff (no tokens in URL/body) per PREVIEW-DECISION.md (PREV-01). |
| T-3-02-TOKEN | Information Disclosure | token-handoff cache | mitigate | `randomUUID()` single-use, 60s TTL, server-side `consume` only; nothing sensitive in the URL. |
| T-3-02-CRED | Information Disclosure | Playwright workers | mitigate | Credential-free contexts; no auth context passed to the preview origin (PREV-04). |
| T-3-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installed in this plan (IframeCsp reuses installed `playwright`). Daytona supply-chain gate is Plan 05. |
</threat_model>

<verification>
- `pnpm --filter @oneui/experience-builder-preview test` green, credential-free.
- `pnpm --filter @oneui/experience-builder-preview typecheck` zero errors.
- Seam file has no vendor import (CI import-guard friendly).
</verification>

<success_criteria>
A compiled bundle renders as real DOM on a separate, credential-free origin (CANVAS-06, PREV-01), is screenshotted per desktop/mobile/fixed profile by credential-free Playwright (PREV-03/04), each version carries an immutable preview URL/state (PREV-02), and the whole path is reachable only through a vendor-free, test-injectable seam.
</success_criteria>

<output>
Create `.planning/phases/03-preview-eval-repair/03-02-SUMMARY.md` when done
</output>
