# Phase 02.1: Close FND-01/FND-04 — wire brand foundations from Convex into run route → workflow - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-01
**Phase:** 02.1-close-fnd-01-fnd-04-wire-brand-foundations-from-convex-into
**Areas discussed:** Sub-brand scope, Fetch location, Caching & determinism, Sub-brand UX, Merge rule

---

## Sub-brand scope

| Option | Description | Selected |
|--------|-------------|----------|
| Brand-level only, defer sub-brand | Wire the parent brand's foundations end-to-end now; add sub-brand selector + override merge as its own slice. | |
| Include sub-brand now | Also add sub-brand selection + merge subBrandConfigs overrides; fully satisfies FND-01's "brand/sub-brand" in one pass. | ✓ |

**User's choice:** Include sub-brand now
**Notes:** Expands scope but closes FND-01 completely. Justified by existing `subBrandConfigs` infra + `applySubBrandAccentsToFoundation` util making merge reuse-by-contract.

---

## Fetch location

| Option | Description | Selected |
|--------|-------------|----------|
| Workflow resolve step (injected loader) | Real Mastra resolveStep calling an injected foundationsLoader; route provides Convex-backed loader, tests mock; agents package stays Convex-free. | ✓ |
| In the Node run route | Route fetches via ConvexHttpClient before invoking the workflow; simpler but resolution leaks out of Mastra (weakens ORCH-04). | |

**User's choice:** Workflow resolve step (injected loader)
**Notes:** Honors ORCH-04, preserves package isolation + credential-free testability via the same DI-seam idiom as `__setCallModelImpl`/`__setCompileImpl`.

---

## Caching & determinism

| Option | Description | Selected |
|--------|-------------|----------|
| Always fetch fresh | One lightweight Convex query per run; designers edit foundations and re-run expecting the change. | ✓ |
| Memoize per brand (D-05) | Reuse djb2 cache keyed on brandId; faster repeats but risks stale foundations after an edit. | |

**User's choice:** Always fetch fresh
**Notes:** Freshness wins for a design Lab. Determinism still served by existing plan/design/copy step memoization keyed on resolved inputs.

---

## Sub-brand UX

| Option | Description | Selected |
|--------|-------------|----------|
| Optional dependent Select | Second Select from subBrandConfigs.getByParentBrand(brandId), "(Parent brand only)" default, disabled until brand chosen; sub-brand optional. | ✓ |
| Always require sub-brand when any exist | Force a pick if the brand has sub-brand configs; blocks parent-only generation. | |

**User's choice:** Optional dependent Select
**Notes:** Matches existing brand-Select pattern (D-02). Requires new optional `subBrandConfigId` prop on PromptCardShape threaded through the run request.

---

## Merge rule

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse applySubBrandAccentsToFoundation | Sub-brand owns primary/secondary/sparkle/brand-bg; rest inherits from parent — exactly as platform global injection + ExperienceCanvas merge. | ✓ |
| Sub-brand fully overrides | Standalone foundation set, no inheritance; new logic, drift risk. | |

**User's choice:** Reuse applySubBrandAccentsToFoundation
**Notes:** No new merge logic; identical to live-platform sub-brand rendering, avoiding drift.

---

## Claude's Discretion

- Exact mapping of `getBrandOverviewData` fields → resolver's `BrandFoundations.colorConfig` (confirm against `precompute.ts` `PrecomputeInput`).
- Whether the loader returns raw overview data (resolver maps) or pre-mapped `BrandFoundations` (loader maps).
- Whether `theme` (light/dark/dim) needs threading into resolution or stays presentation-only.
- Test structure for the new end-to-end route + resolve-step coverage.

## Deferred Ideas

- Foundation caching/memoization (rejected this phase; revisit with version/TTL guard only if round-trip cost matters).
- Non-web output-profile foundation coverage (FND-02 — Phase 4).
- Theme deep-threading into resolution (only if verification shows the engine needs it).
