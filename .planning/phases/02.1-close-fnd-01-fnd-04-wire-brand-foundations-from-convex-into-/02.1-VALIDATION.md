---
phase: 02.1
slug: close-fnd-01-fnd-04-wire-brand-foundations-from-convex-into
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-01
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.5 (`run`) |
| **Config file** | `packages/experience-builder-agents/vitest.config.ts`, `apps/platform/vitest.config.ts` |
| **Quick run command** | `pnpm --filter @oneui/experience-builder-agents test` (agents) · `pnpm --filter platform test` (route/panel) |
| **Full suite command** | `pnpm test` + `pnpm typecheck` |
| **Estimated runtime** | ~30–60 seconds (per-package quick run); full suite longer |

---

## Sampling Rate

- **After every task commit:** Run the touched package's quick run (`workflow.test.ts` for agents; `runRoute`/`requestPanel` for platform)
- **After every plan wave:** Run `pnpm --filter @oneui/experience-builder-agents test` + `pnpm --filter platform test` + `pnpm typecheck`
- **Before `/gsd-verify-work`:** Full `pnpm test` green + `pnpm typecheck` zero errors
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

| Task ID | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| resolveStep injected-loader writes `brandFoundations`; real-shaped overview → real `ThemeConfig` E2E | — | FND-01 / FND-04 | — | N/A | unit (workflow) | `vitest run workflow.test.ts` | ✅ extend `workflow.test.ts` | ⬜ pending |
| Loader maps overview (`color.config`) → `BrandFoundations` (guards Pitfall 1) | — | FND-04 | — | malformed id → null → defaults | unit (loader) | `vitest run` | ❌ W0 new loader test | ⬜ pending |
| Sub-brand path: loader applies `applySubBrandAccentsToFoundation`, merged accents reach resolver | — | FND-01 | T-cross-brand | merge only 4 accent fields, no parent leak | unit (workflow/loader) | `vitest run` | ❌ W0 new test | ⬜ pending |
| Route builds Convex loader, injects it, forwards `subBrandConfigId` | — | FND-04 | T-input-validation | `.strict()` body rejects unknown fields; `v.id()` rejects malformed | unit (route) | `vitest run runRoute.test.ts` | ✅ extend `runRoute.test.ts` | ⬜ pending |
| Sub-brand Select renders, disabled until brand chosen, populated from `getByParentBrand`, "(Parent brand only)" default | — | FND-01 (UI) | — | N/A | unit (component) | `vitest run requestPanel.test.tsx` | ✅ extend `requestPanel.test.tsx` | ⬜ pending |
| Partial/empty real brand resolves via system defaults (no spurious gap) | — | D-08 (FND-01) | — | N/A | unit (resolver) | `vitest run foundationResolver.test.ts` | ✅ add `color.config` overview fixture | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] **Spike A1** — verify `ConvexHttpClient` can invoke the Convex query D-06 names. `getBrandOverviewDataInternal` is an `internalQuery`; the public twin `getBrandOverviewData` (`foundations.ts:977`) shares the identical handler. Confirm which the HTTP client can call and pin the loader to it.
- [ ] New test: loader maps overview (`color.config`) → `BrandFoundations` correctly (guards Pitfall 1) — covers FND-04
- [ ] New test: sub-brand merge path through the loader/workflow — covers FND-01 sub-brand
- [ ] Extend `workflow.test.ts` with a `foundationsLoader`-injection happy-path + a loader-returns-null (system-defaults) case
- [ ] Extend `runRoute.test.ts` to assert the route injects a `foundationsLoader` and forwards `subBrandConfigId`
- [ ] Extend `requestPanel.test.tsx` for the dependent sub-brand Select

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| A real brand selected in the live Lab yields a visibly real-themed artifact (vs empty-foundation default) | FND-01 / FND-04 | Requires live Convex data + running dev server; the automated test uses a mock loader with real-shaped data | Run `pnpm dev`, open the Experience Lab route, pick a configured brand + sub-brand, run a generation, confirm the resolved `ThemeConfig` reflects the brand's real tokens |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (A1 spike + new loader/sub-brand tests)
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
