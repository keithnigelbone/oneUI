---
phase: 02.1-close-fnd-01-fnd-04-wire-brand-foundations-from-convex-into
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/experience-builder-agents/src/workflow.ts
  - packages/experience-builder-agents/src/workflow.test.ts
  - packages/experience-builder-agents/src/subBrandFoundationsLoader.test.ts
autonomous: true
requirements: [FND-01, FND-04]
must_haves:
  truths:
    - "resolveStep calls the injected foundationsLoader and writes ctx.request.brandFoundations when nothing pre-supplied it"
    - "A real-shaped overview returned by a mock loader yields a real ThemeConfig end-to-end (outcome === 'artifact', no spurious gap)"
    - "A loader that returns null still completes the run via engine system defaults (D-08), never a gap"
    - "The overview -> BrandFoundations mapping reads color.config (NOT the color record); a sub-brand row merged via applySubBrandAccentsToFoundation carries the sub-brand's 4 accent roles + inherited parent semantic roles"
    - "The agents package remains Convex-free (no @oneui/convex import, no ConvexHttpClient)"
  artifacts:
    - path: "packages/experience-builder-agents/src/workflow.ts"
      provides: "FoundationsLoader type + FoundationsLoaderInput + extended RunExperienceInput + real resolveStep"
      contains: "FoundationsLoader"
    - path: "packages/experience-builder-agents/src/workflow.test.ts"
      provides: "foundationsLoader injection happy-path + loader-returns-null system-defaults case"
      contains: "foundationsLoader"
    - path: "packages/experience-builder-agents/src/subBrandFoundationsLoader.test.ts"
      provides: "Pitfall-1 mapping guard + sub-brand merge guard"
      contains: "applySubBrandAccentsToFoundation"
  key_links:
    - from: "resolveStep"
      to: "ctx.request.brandFoundations"
      via: "writes loaded foundations into RunContext"
      pattern: "ctx\\.request\\.brandFoundations\\s*="
    - from: "resolveStep"
      to: "ctx.request.foundationsLoader"
      via: "await ctx.request.foundationsLoader({ brandId, subBrandConfigId? })"
      pattern: "foundationsLoader\\("
---

<objective>
Define the `FoundationsLoader` injection contract in the Convex-free `experience-builder-agents` package and turn the no-op `resolveStep` into a real Mastra step that calls the injected loader and writes `ctx.request.brandFoundations` into `RunContext`. This is the agents-side half of closing FND-01/FND-04: the workflow now resolves real foundations when a loader is supplied, while the package stays Convex-free (the loader implementation lives in the route, Plan 02).

This plan also pins the A1 open question (internal vs public Convex query) at the contract level so Plan 02's loader implementation has an unambiguous target.

Purpose: Without this, every real run resolves against empty foundations because nothing populates `brandFoundations`. `planStep`/`generateStep` already read `ctx.request.brandFoundations` — the only missing link is `resolveStep` populating it (per D-04, ORCH-04: resolution is orchestration → belongs in the workflow).
Output: `FoundationsLoader`/`FoundationsLoaderInput` types, extended `RunExperienceInput` (`foundationsLoader?`, `subBrandConfigId?`), a real `resolveStep`, and three test surfaces (extended `workflow.test.ts` + new `subBrandFoundationsLoader.test.ts`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-close-fnd-01-fnd-04-wire-brand-foundations-from-convex-into-/02.1-CONTEXT.md
@.planning/phases/02.1-close-fnd-01-fnd-04-wire-brand-foundations-from-convex-into-/02.1-RESEARCH.md
@.planning/phases/02.1-close-fnd-01-fnd-04-wire-brand-foundations-from-convex-into-/02.1-PATTERNS.md
@.planning/phases/02.1-close-fnd-01-fnd-04-wire-brand-foundations-from-convex-into-/02.1-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING / Wave 0]: Pin the A1 internal-vs-public Convex query and define the FoundationsLoader contract</name>
  <files>packages/experience-builder-agents/src/workflow.ts</files>
  <read_first>
    - packages/experience-builder-agents/src/workflow.ts (the file being modified — `RunExperienceInput` at lines 126-133, the `BrandFoundations` import at line 37)
    - packages/experience-builder-agents/src/foundationResolver.ts (the `BrandFoundations` type definition this loader returns)
    - packages/experience-builder-agents/src/irGenerator.ts (the `__setCompileImpl` DI idiom — the typed-function-with-clean-signature shape to mirror)
    - packages/convex/convex/foundations.ts (lines ~975-989 — the public `getBrandOverviewData` and `internalQuery` `getBrandOverviewDataInternal` twins share one handler)
    - .planning/phases/02.1-close-fnd-01-fnd-04-wire-brand-foundations-from-convex-into-/02.1-RESEARCH.md § "Loader Signature", § "Assumptions Log" (A1)
  </read_first>
  <action>
    A1 SPIKE (decide + record, no live round-trip needed): `getBrandOverviewDataInternal` is declared with `internalQuery` and `getBrandOverviewData` with `query`, both sharing `getBrandOverviewDataHandler` (verified at foundations.ts:975-989). `ConvexHttpClient` (from `convex/browser`) is a public-API client and CANNOT invoke an `internalQuery` — internal queries are callable only from other Convex functions, not external clients. Therefore the loader (built in Plan 02) MUST target the PUBLIC `api.foundations.getBrandOverviewData`, NOT the `…Internal` twin named in D-06. Record this decision in the plan SUMMARY as: "A1 resolved — loader uses public getBrandOverviewData (identical handler); deviates from D-06's literal `…Internal` naming because ConvexHttpClient cannot call internalQuery." This is the only place D-06's literal naming bends.

    Then define the loader contract in `workflow.ts`, Convex-free (referencing only the already-imported local `BrandFoundations` type from `./foundationResolver`): add an exported `FoundationsLoaderInput` interface with `brandId: string` and optional `subBrandConfigId?: string`, and an exported `FoundationsLoader` type alias `= (input: FoundationsLoaderInput) => Promise<BrandFoundations | null>`. Mirror the typed-function-with-clean-signature shape of the `__setCompileImpl` idiom. Extend `RunExperienceInput` (lines 126-133) with two NEW optional fields: `foundationsLoader?: FoundationsLoader` (D-05 — injected per-run) and `subBrandConfigId?: string` (D-02). Keep the existing `brandFoundations?`, `prompt?`, `requestedComponents?` fields unchanged. Do NOT import `@oneui/convex` or `ConvexHttpClient` anywhere in this package (D-05 isolation invariant).
  </action>
  <verify>
    <automated>cd /Users/nunomarcelino/Documents/Code/OneUIStudio_BaseUI_v4 && grep -q "export type FoundationsLoader" packages/experience-builder-agents/src/workflow.ts && grep -q "foundationsLoader?: FoundationsLoader" packages/experience-builder-agents/src/workflow.ts && grep -q "subBrandConfigId?: string" packages/experience-builder-agents/src/workflow.ts && ! grep -Eq "@oneui/convex|ConvexHttpClient" packages/experience-builder-agents/src/workflow.ts && pnpm --filter @oneui/experience-builder-agents test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `workflow.ts` exports `FoundationsLoaderInput` (with `brandId: string`, `subBrandConfigId?: string`) and `FoundationsLoader` (`= (input) => Promise<BrandFoundations | null>`).
    - `RunExperienceInput` carries `foundationsLoader?: FoundationsLoader` and `subBrandConfigId?: string`; existing fields untouched.
    - No `@oneui/convex` import and no `ConvexHttpClient` reference anywhere under `packages/experience-builder-agents/` (grep returns nothing).
    - SUMMARY records the A1 decision: loader targets public `getBrandOverviewData`.
    - `pnpm --filter @oneui/experience-builder-agents test` still exits 0 (existing tests unaffected by type additions).
  </acceptance_criteria>
  <done>The loader contract is defined, the A1 query target is pinned to the public query, and the package remains Convex-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make resolveStep call the injected loader and write brandFoundations; cover with tests</name>
  <files>packages/experience-builder-agents/src/workflow.ts, packages/experience-builder-agents/src/workflow.test.ts, packages/experience-builder-agents/src/subBrandFoundationsLoader.test.ts</files>
  <read_first>
    - packages/experience-builder-agents/src/workflow.ts (the no-op `resolveStep` at lines 200-211 — keep its exact `emit(...started)`/`emit(...completed)` bracket; `planStep` at ~263 and `generateStep` at ~393 already spread `...(ctx.request.brandFoundations ? { brandFoundations } : {})`)
    - packages/experience-builder-agents/src/workflow.test.ts (the existing harness: `coveredRequest`, the `schemaAwareModelImpl` mock + `__setCallModelImpl` seam, the `beforeEach`/`afterEach` model-restore)
    - packages/experience-builder-agents/src/foundationResolver.test.ts (the real overview-shaped `CONFIGURED_BRAND` fixture at lines ~27-38 — reuse its shape as the loader return; the existing partial/empty-brand-resolves cases at ~45-56)
    - packages/shared/src/utils/subBrandFoundation.ts (`applySubBrandAccentsToFoundation` signature at ~50; `SubBrandAccentFields` shape at ~8-16; `INHERITED_SEMANTIC_ROLES`)
    - packages/convex/convex/schema.ts (§ subBrandConfigs, lines ~200-213 — the row shape is already SubBrandAccentFields-compatible)
    - .planning/phases/02.1-close-fnd-01-fnd-04-wire-brand-foundations-from-convex-into-/02.1-PATTERNS.md § "EXTEND workflow.test.ts", § "NEW subBrandFoundationsLoader.test.ts"
  </read_first>
  <behavior>
    - resolveStep with an injected `foundationsLoader` returning a real-shaped `BrandFoundations` (color scales + appearanceConfig accents) → run `outcome === 'artifact'` (real ThemeConfig reached the resolver, no spurious gap).
    - resolveStep with a `foundationsLoader` returning `null` → run still completes via engine system defaults (D-08), outcome is NOT a foundation gap.
    - resolveStep with no `foundationsLoader` → behaves exactly as before (no regression in existing workflow tests).
    - resolveStep with `subBrandConfigId` present on the request → loader is called with `{ brandId, subBrandConfigId }` (assert the loader received the sub-brand id).
    - subBrandFoundationsLoader.test.ts (pure-logic): an overview record `{ color: { config: {...} }, presetSelection, appearanceConfig }` maps so `colorConfig === overview.color.config` (NOT `overview.color`) — Pitfall 1 guard. A `SubBrandAccentFields`-shaped row passed through `applySubBrandAccentsToFoundation(overview, sub)` produces `appearanceConfig.accents` carrying the sub-brand's primary/secondary/sparkle/brand-bg with the parent's inherited semantic roles (assert against `INHERITED_SEMANTIC_ROLES`).
  </behavior>
  <action>
    Rewrite the body of `resolveStep` (workflow.ts:200-211) keeping its exact `emit(ctx, {...status:'started'...})` / `emit(ctx, {...status:'completed'...})` bracket. Between them, when `ctx.request.foundationsLoader` exists AND `ctx.request.brandFoundations` is not already set, `await ctx.request.foundationsLoader({ brandId: ctx.request.brandId, ...(ctx.request.subBrandConfigId ? { subBrandConfigId: ctx.request.subBrandConfigId } : {}) })` using the conditional-optional-spread idiom (never pass `undefined`). If the loader returns a non-null value, assign it to `ctx.request.brandFoundations`. Do nothing else — `planStep`/`generateStep` already consume `ctx.request.brandFoundations`, so no downstream change is needed.

    In `workflow.test.ts`: reuse the existing `coveredRequest` + `__setCallModelImpl` harness verbatim. Add (1) a happy-path test injecting a `mockLoader: FoundationsLoader` that returns a real-shaped `BrandFoundations` (reuse the `foundationResolver.test.ts` `CONFIGURED_BRAND` fixture shape) and asserting `result.outcome === 'artifact'`; (2) a `loader-returns-null` test asserting the run still completes via system defaults (not a foundation gap, D-08); (3) a test asserting the loader is invoked with `subBrandConfigId` when the request carries one.

    Create NEW `subBrandFoundationsLoader.test.ts` testing the two pure-logic concerns independently of Convex: the Pitfall-1 `color.config` mapping and the `applySubBrandAccentsToFoundation` merge (import the util from `@oneui/shared`). Use a real overview-shaped fixture and a `SubBrandAccentFields`-shaped row. Do NOT import any Convex client — this test exercises the mapping/merge logic only, keeping the package Convex-free. (The route's loader factory itself is exercised in Plan 02's `runRoute.test.ts`.)
  </action>
  <verify>
    <automated>cd /Users/nunomarcelino/Documents/Code/OneUIStudio_BaseUI_v4 && pnpm --filter @oneui/experience-builder-agents test 2>&1 | tail -8 && grep -Eq "ctx\.request\.brandFoundations\s*=" packages/experience-builder-agents/src/workflow.ts && grep -q "foundationsLoader(" packages/experience-builder-agents/src/workflow.ts</automated>
  </verify>
  <acceptance_criteria>
    - `resolveStep` body contains `await ctx.request.foundationsLoader(` guarded by `if (ctx.request.foundationsLoader && !ctx.request.brandFoundations)` and assigns the non-null result to `ctx.request.brandFoundations`.
    - The `emit(...'started')` / `emit(...'completed')` bracket of `resolveStep` is preserved.
    - `workflow.test.ts` contains a `foundationsLoader` happy-path asserting `outcome === 'artifact'`, a loader-returns-null case asserting completion via defaults, and a subBrandConfigId-forwarding assertion.
    - `subBrandFoundationsLoader.test.ts` asserts `colorConfig === overview.color.config` (Pitfall 1) and that `applySubBrandAccentsToFoundation` merges the 4 accent roles + inherited semantic roles.
    - `pnpm --filter @oneui/experience-builder-agents test` exits 0 with all new tests green; no `@oneui/convex`/`ConvexHttpClient` import added.
  </acceptance_criteria>
  <done>resolveStep populates brandFoundations from the injected loader; the agents-side FND-01/FND-04 wiring is proven by tests (happy-path E2E artifact, null-loader defaults, sub-brand forwarding, Pitfall-1 mapping, sub-brand merge).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route → agents workflow | The route injects a `foundationsLoader` (carrying a per-request Convex client) into the agents package. The agents package treats the loader as an opaque async function — it never sees Convex credentials or shapes (D-05 isolation). |
| agents package isolation | `experience-builder-agents` MUST NOT import `@oneui/convex`/`ConvexHttpClient`. The injected-function seam is the isolation boundary; a hard Convex import would be a regression caught by CI import guards. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-01 | Tampering | `experience-builder-agents` package isolation | mitigate | Loader is injected as a typed function through `RunExperienceInput`; no `@oneui/convex`/`ConvexHttpClient` import — enforced by the grep in Task 1's verify + existing Phase 1 CI import guards. |
| T-02.1-02 | Information Disclosure | `resolveStep` foundation data | accept | `resolveStep` only writes the loader's already-resolved `BrandFoundations` into in-memory `RunContext`; no logging of foundation contents. The loader (Plan 02) owns the Convex access boundary. Low-value design data, no PII. |
| T-02.1-03 | Denial of Service | `resolveStep` awaiting the loader | accept | A slow/failing loader returns `null` (or rejects) → resolver falls back to system defaults (D-08); the route already runs Node with `maxDuration` bound. No new unbounded loop introduced. |
| T-02.1-SC | Tampering | npm/pip/cargo installs | mitigate | No packages added this plan (RESEARCH: zero external packages). No install task; nothing to audit. |
</threat_model>

<verification>
- `pnpm --filter @oneui/experience-builder-agents test` exits 0 with the new `foundationsLoader` injection, null-loader, sub-brand-forwarding, and `subBrandFoundationsLoader.test.ts` cases green.
- `pnpm typecheck` reports zero errors for the agents package after the type additions.
- Convex-free invariant holds: `grep -RE "@oneui/convex|ConvexHttpClient" packages/experience-builder-agents/src` returns nothing.
</verification>

<success_criteria>
- `FoundationsLoader`/`FoundationsLoaderInput` exported from `workflow.ts`; `RunExperienceInput` carries `foundationsLoader?` + `subBrandConfigId?`.
- `resolveStep` calls the injected loader (when present and `brandFoundations` unset) and writes `ctx.request.brandFoundations`.
- A real-shaped mock loader yields `outcome === 'artifact'`; a null loader still completes via system defaults (D-08).
- A1 resolved and recorded: loader targets public `getBrandOverviewData`.
- The agents package remains Convex-free.
</success_criteria>

<output>
Create `.planning/phases/02.1-close-fnd-01-fnd-04-wire-brand-foundations-from-convex-into-/02.1-01-SUMMARY.md` when done.
</output>
