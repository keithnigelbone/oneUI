---
phase: 04.1-chat-first-lab-ux
plan: 04
type: execute
wave: 3
depends_on: ["04.1-03"]
files_modified:
  - "apps/platform/src/app/(platform)/(experience-lab)/_chat/parts/CampaignPlanCard.tsx"
  - "apps/platform/src/app/(platform)/(experience-lab)/_chat/parts/CampaignPlanCard.module.css"
  - "apps/platform/src/app/(platform)/(experience-lab)/_chat/parts/renderLabMessagePart.tsx"
  - "apps/platform/src/app/(platform)/(experience-lab)/__tests__/campaignPlanCard.test.tsx"
autonomous: true
requirements: [CHAT-06, CHAT-07]
user_setup: []

must_haves:
  truths:
    - "The campaign-plan HITL renders as an inline interactive Jio-DS card in the chat stream (D-08)."
    - "The card shows brief + audience + message hierarchy + the 3 creative directions + the recommended one; direction selection + frame count happen inline (D-08)."
    - "Selecting a direction + frame count posts {runId, brandId, directionIndex, frameCount, subBrandConfigId?} to the existing /resume route and resumes the suspended workflow (D-10)."
    - "The inline-card framework dispatch handles a second data-part type without refactor (D-09/CHAT-07)."
  artifacts:
    - path: "apps/platform/src/app/(platform)/(experience-lab)/_chat/parts/CampaignPlanCard.tsx"
      provides: "Inline interactive campaign-plan card rendering CampaignPlanT + posting selection to /resume"
      contains: "experience-lab/resume"
    - path: "apps/platform/src/app/(platform)/(experience-lab)/_chat/parts/renderLabMessagePart.tsx"
      provides: "Dispatch extended with the data-campaign-plan part (D-09 framework, second consumer)"
      contains: "data-campaign-plan"
  key_links:
    - from: "renderLabMessagePart.tsx"
      to: "CampaignPlanCard.tsx"
      via: "data-campaign-plan part dispatch"
      pattern: "CampaignPlanCard"
    - from: "CampaignPlanCard.tsx"
      to: "/api/experience-lab/resume"
      via: "resume POST with the existing ResumeRequestBody shape"
      pattern: "experience-lab/resume"
    - from: "CampaignPlanCard.onResolved"
      to: "useLabConversation NDJSON consumer → canvasCallbacks.placeArtifact"
      via: "onResolved feeds the resume response (same NDJSON stream) back into the conversation hook so carousel frames place on the canvas"
      pattern: "onResolved"
---

<objective>
Build the named deliverable of this phase: the inline interactive campaign-plan card (D-08), rendered in the chat stream via the inline-card framework (D-09), posting a direction + frame-count selection to the UNCHANGED `/api/experience-lab/resume` route (D-10). This proves the framework with its first real consumer and is the concrete proof-point distinguishing 04.1 from the Phase-4 canvas implementation.

Purpose: Move the campaign HITL from a canvas plan-card shape (retired as rendering only) to an inline chat card that resumes the suspended Mastra workflow — without touching the suspend/resume contract, the plan schema, or the resume route.

Output: `CampaignPlanCard.tsx` + CSS, and the `renderLabMessagePart.tsx` dispatch extended with the `data-campaign-plan` part (the second consumer the D-09 framework was designed for).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-CONTEXT.md
@.planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-RESEARCH.md
@.planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-PATTERNS.md
@.planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-UI-SPEC.md
@apps/platform/src/app/api/experience-lab/resume/route.ts
@packages/experience-builder-core/src/contracts/campaignPlan.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inline CampaignPlanCard rendering CampaignPlanT + resume POST (D-08/D-10)</name>
  <files>apps/platform/src/app/(platform)/(experience-lab)/_chat/parts/CampaignPlanCard.tsx, apps/platform/src/app/(platform)/(experience-lab)/__tests__/campaignPlanCard.test.tsx</files>
  <read_first>
    - packages/experience-builder-core/src/contracts/campaignPlan.ts (the `CampaignPlanT` shape it renders: briefSummary, audience, messageHierarchy[], directions[] (exactly 3: name/concept/copyAngle/leadRole/surfaceMood/layoutMotif), recommendedDirectionIndex, recommendedFrameCount — lines 102-139)
    - apps/platform/src/app/api/experience-lab/resume/route.ts (read-only contract — `ResumeRequestBody` {runId, brandId, directionIndex:number, frameCount:number, subBrandConfigId?} lines 48-59; do NOT modify, do NOT invent a new schema)
    - packages/ui/src/components/ChatSurface/parts/ToneGuardCard.tsx (analog A — data-part card skeleton, `{part}` prop, token-only CSS module)
    - apps/platform/src/app/(platform)/(experience-lab)/_canvas/runStream.ts (the result frame carrying `campaignPlan` + `campaignRunId`, lines 77-90; resume response is the SAME NDJSON stream)
    - .planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-UI-SPEC.md (Component Inventory for the card: Surface subtle body, RadioGroup directions, Stepper frame count, one primary Button "Generate frames", Recommended Badge; copy + a11y/focus management)
  </read_first>
  <behavior>
    - Test 1 (CHAT-06): given a `CampaignPlanT` (3 directions, recommendedDirectionIndex), the card renders brief/audience/message-hierarchy/3 direction tiles with the recommended one marked, a frame-count `Stepper`, and a single "Generate frames" primary button.
    - Test 2 (CHAT-06 resume): clicking "Generate frames" with a selected direction + frame count POSTs `{ runId: campaignRunId, brandId, directionIndex, frameCount, subBrandConfigId? }` to `/api/experience-lab/resume` (mocked fetch) — EXACTLY the existing body, no extra fields.
    - Test 3 (Anthropic-safe): no new Zod schema with `.int()/.min()/.max()` is introduced for the resume payload (clamping is the route's job; client clamp optional).
  </behavior>
  <action>Create `CampaignPlanCard.tsx` taking `{ plan: CampaignPlanT, campaignRunId, brandId, subBrandConfigId?, onResolved? }`. Compose from Jio-DS (deep imports): `<Surface mode="subtle">` body, sections `Brief`/`Audience`/`Message hierarchy`/`Creative directions` (UI-SPEC copy), a `RadioGroup`/`Radio` for the 3 directions defaulting to `recommendedDirectionIndex`, a `Badge appearance="primary"` "Recommended" marker on that tile, a `Stepper` labelled `Frames per direction` defaulting to `recommendedFrameCount`, and a single `Button variant="bold" appearance="primary"` "Generate frames" (the one accent action). On confirm, POST to `/api/experience-lab/resume` with EXACTLY `{ runId: campaignRunId, brandId, directionIndex, frameCount, ...(subBrandConfigId ? {subBrandConfigId} : {}) }` (copy from RESEARCH.md Code Examples / `resume/route.ts:48-59`) — do NOT invent a new schema (Pitfall 6); the response is the SAME NDJSON stream → invoke `onResolved(response)` so the conversation hook (`useLabConversation`) consumes it through the existing `readNdjson` path and the returned `carouselFrames` reach `canvasCallbacks.placeArtifact` on the canvas (the Plan 03 placement seam). a11y (UI-SPEC): `role="group"` + `aria-labelledby` the title; on mount move focus to the heading; after resume return focus to the composer. Use `ComponentAppearance` from `@oneui/shared` if exposing an appearance prop. Un-skip / fill `campaignPlanCard.test.tsx` (incl. the suspend-not-failure assertion shared with Plan 03's RunTurn test).</action>
  <verify>
    <automated>pnpm --filter @oneui/platform test -- campaignPlanCard -x</automated>
  </verify>
  <acceptance_criteria>
    - Tests 1–3 pass.
    - Carousel frames placed on canvas after resume: `onResolved` feeds the resume NDJSON stream back through the `useLabConversation` consumer so the returned `carouselFrames` reach `canvasCallbacks.placeArtifact` (asserted via the mocked `resume-carousel` fixture from Plan 01 — the resolve callback fires with the carousel result frame).
    - `grep -nE "\.int\(\)|\.min\(|\.max\(" CampaignPlanCard.tsx` returns no matches on a number schema (Anthropic-safe, Pitfall 6); the resume body is the exact `{runId,brandId,directionIndex,frameCount,subBrandConfigId?}` set.
    - The card POSTs to `/api/experience-lab/resume` and does not modify the resume route, `workflow.ts`, or `CampaignPlanSchema`.
    - a11y: card is a labelled `role="group"`; one primary accent button only (UI-SPEC color rule).
    - No `(builder)`/`ExperienceCanvas` import; no `ai`/`@ai-sdk` import; deep `@oneui/ui-internal` imports.
  </acceptance_criteria>
  <done>The inline card renders the full plan, selection + frame count post the existing resume body, and no read-only contract is modified.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the dispatch with the data-campaign-plan part + token-only CSS (D-09/CHAT-07)</name>
  <files>apps/platform/src/app/(platform)/(experience-lab)/_chat/parts/renderLabMessagePart.tsx, apps/platform/src/app/(platform)/(experience-lab)/_chat/parts/CampaignPlanCard.module.css</files>
  <read_first>
    - apps/platform/src/app/(platform)/(experience-lab)/_chat/parts/renderLabMessagePart.tsx (Plan 03 — the dispatch with the run-progress part + the marked extension point)
    - packages/ui/src/components/ChatSurface/parts/index.tsx (analog — `defaultRenderMessagePart` discriminator + fall-through-to-null convention)
    - packages/ui/src/components/ChatSurface/parts/ToneGuardCard.module.css (analog — token-only data-part card CSS)
    - .planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-UI-SPEC.md (Spacing/Typography/Shape/Color for the card + Focus Halo)
  </read_first>
  <action>Extend `renderLabMessagePart.tsx` at the Plan-03 extension point: add `PART_CAMPAIGN_PLAN = 'data-campaign-plan'` and return `<CampaignPlanCard plan={part.plan} campaignRunId={part.campaignRunId} brandId={part.brandId} subBrandConfigId={part.subBrandConfigId} />` for that type, keeping the `null` fall-through for unknown parts. This is the proof the D-09 framework absorbs a second consumer without refactor (CHAT-07). Author `CampaignPlanCard.module.css` token-only: card radius `--Shape-M`/`--Shape-L`, section gaps `--Spacing-L`/`--Spacing-2XL`, direction-tile gutter `--Spacing-Gutter`, titles `--Title-M-*`, body `--Body-M-*`, badges `--Label-XS-*`, all with `font-family: var(--Typography-Font-Primary)`. Tinting via `<Surface mode="subtle">` only (no `background` on raw divs, no decorative strokes); role-tint override allowed via inline `style={{ '--Surface-Fill-Subtle': 'var(--Secondary-Subtle)' }}`. Net-new interactive focus (direction tiles) uses Focus Halo with `--Surface-Halo-Gap`.</action>
  <verify>
    <automated>pnpm check:literals && pnpm --filter @oneui/platform test -- campaignPlanCard runTurn -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "data-campaign-plan" renderLabMessagePart.tsx` shows the second part wired; `data-run-progress` from Plan 03 still dispatches (no regression to the RunTurn test).
    - Unknown part types still return `null` (fall-through preserved).
    - `pnpm check:literals` clean; no legacy `--Typography-Size-*`/`--Typography-Weight-*`; no disallowed px literals.
    - Card tinting is Surface-driven; no `background:` on a raw container; no decorative border on the tinted Surface.
  </acceptance_criteria>
  <done>The dispatch handles both run-progress and campaign-plan parts with the fall-through intact, and the card CSS is token-only V2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inline card → resume route | client-selected directionIndex/frameCount cross into the suspended-workflow resume |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-10 | Tampering/DoS | resume payload (oversized frameCount / out-of-range directionIndex) | mitigate | The card posts the existing `ResumeRequestBody`; clamping (frameCount 1..10, directionIndex 0..len-1) is enforced at the route edge AND in the workflow — the card does not bypass it. No new schema, no privilege escalation via resume. |
| T-04.1-11 | Elevation | resume runId/brandId | mitigate | runId/brandId ride from the result frame (`campaignRunId`/run's brand); the route re-hydrates the plan from Convex by runId and applies the existing brand-scope guard (rejects the `'jio'` placeholder). Not weakened. |
| T-04.1-12 | Tampering (Stored XSS) | rendering CampaignPlanT fields | mitigate | The card renders structured `CampaignPlanT` string fields via React escaping, never `dangerouslySetInnerHTML`; IR/plan stays markup-free. |
</threat_model>

<verification>
- `pnpm --filter @oneui/platform test -- campaignPlanCard runTurn -x` green.
- `pnpm check:literals` clean.
- `pnpm typecheck` clean.
- No modification to `resume/route.ts`, `workflow.ts`, `plannerAgent.ts`, or `campaignPlan.ts` (read-only contracts).
</verification>

<success_criteria>
- Inline interactive campaign-plan card renders the full plan and resumes the workflow via the unchanged resume route (D-08/D-10).
- The D-09 framework absorbs the second part type without refactor (CHAT-07).
- No read-only-contract edits; token-only Jio-DS; Anthropic-safe payload.
</success_criteria>

<output>
Create `.planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-04-SUMMARY.md` when done.
</output>
