---
phase: 04.1-chat-first-lab-ux
plan: 05
type: execute
wave: 3
depends_on: ["04.1-03"]
files_modified:
  - "apps/platform/src/app/(platform)/(experience-lab)/_chat/useChatCanvasFocus.ts"
  - "apps/platform/src/app/(platform)/(experience-lab)/_chat/ExperienceLabChat.tsx"
  - "apps/platform/src/app/(platform)/(experience-lab)/_chat/ExperienceLabChat.module.css"
  - "apps/platform/src/app/(platform)/(experience-lab)/__tests__/selectionBinding.test.tsx"
autonomous: true
requirements: [CHAT-08]
user_setup: []

must_haves:
  truths:
    - "Selecting an artifact card on the canvas focuses the conversation on that artifact and scrolls to its originating turn (D-02)."
    - "Chat visibly indicates which artifact is in focus (a neutral indicator in the aboveComposer slot)."
    - "Clicking a turn's focus action selects + zooms the originating shape on the canvas (reverse binding)."
    - "Follow-up messages target the focused artifact (the chat-first realization of the Phase-3 iterate-on-artifact flow)."
  artifacts:
    - path: "apps/platform/src/app/(platform)/(experience-lab)/_chat/useChatCanvasFocus.ts"
      provides: "Two-way binding glue: tldraw selection ↔ focused turn id, with focus/scroll/zoom actions"
      contains: "getSelectedShapeIds"
  key_links:
    - from: "useChatCanvasFocus.ts"
      to: "editor.sideEffects.registerAfterChangeHandler"
      via: "reactive tldraw selection observation (not polling)"
      pattern: "registerAfterChangeHandler"
    - from: "ExperienceLabChat.tsx"
      to: "useChatCanvasFocus.ts"
      via: "focused-artifact indicator in aboveComposer + scroll-to-turn"
      pattern: "useChatCanvasFocus"
---

<objective>
Build the chat↔canvas two-way selection binding (D-02 / CHAT-08): selecting an artifact card on the canvas focuses the conversation on that artifact (scroll to its originating turn + visible focus indicator), and a turn's focus action selects + zooms the shape on the canvas. This is the net-new integration glue that makes the chat-first iterate flow work.

Purpose: Realize the Phase-3 iterate-on-artifact flow in the chat-first model — follow-up messages target the focused artifact. Depends on Plan 03 (the chat host + the preserved tldraw selection-observation hook).

Output: `useChatCanvasFocus.ts` binding hook + the `aboveComposer` focused-artifact indicator wired into the chat host, using the existing reactive tldraw side-effect registration (no polling).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-CONTEXT.md
@.planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-RESEARCH.md
@.planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-PATTERNS.md
@.planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-UI-SPEC.md
@apps/platform/src/app/(platform)/(experience-lab)/_canvas/ExperienceLabCanvas.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two-way selection binding hook (tldraw selection ↔ focused turn)</name>
  <files>apps/platform/src/app/(platform)/(experience-lab)/_chat/useChatCanvasFocus.ts, apps/platform/src/app/(platform)/(experience-lab)/__tests__/selectionBinding.test.tsx</files>
  <read_first>
    - apps/platform/src/app/(platform)/(experience-lab)/_canvas/ExperienceLabCanvas.tsx (analog — the selection observation `registerAfterChangeHandler('instance_page_state'/'shape', sync)` + `getSelectedShapeIds()` at lines 84-96, preserved by Plan 03)
    - apps/platform/src/app/(platform)/(experience-lab)/_canvas/shapes/ArtifactCardShape.tsx (the shape whose props carry the originating-run / turn linkage)
    - .planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-RESEARCH.md (Pattern 4 — map selected ArtifactCardShape → originating turn id; reverse `editor.select`/`zoomToSelection`; anti-pattern: do NOT poll the editor)
    - .planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-PATTERNS.md (selection-binding "No Analog Found" entry — net-new glue)
  </read_first>
  <behavior>
    - Test 1 (CHAT-08 forward): with a tldraw editor harness, selecting an `ArtifactCardShape` resolves its originating turn id and sets `focusedTurnId` + requests a scroll-to-turn.
    - Test 2 (CHAT-08 reverse): invoking `focusOnCanvas(turnId)` calls `editor.select(shapeId)` + `editor.zoomToSelection()` for the linked shape.
    - Test 3 (no-poll): the binding uses `registerAfterChangeHandler` and unregisters on cleanup; no `setInterval`/polling.
  </behavior>
  <action>Create `useChatCanvasFocus.ts` taking `(editor, { messages, scrollToTurn })` and returning `{ focusedArtifact, focusOnCanvas(turnId), clearFocus() }`. Use the EXISTING reactive `editor.sideEffects.registerAfterChangeHandler('instance_page_state', sync)` + `registerAfterChangeHandler('shape', sync)` pattern (ExperienceLabCanvas 84-96) — in `sync`, read `getSelectedShapeIds()`; if the selected shape is an `ArtifactCardShape`, resolve its originating turn id (from the shape's run/artifact linkage cross-referenced to the message that created it) and set the focused state + call `scrollToTurn`. Reverse: `focusOnCanvas(turnId)` maps the turn → its shape id and calls `editor.select(shapeId)` + `editor.zoomToSelection()`. Return the cleanup that unregisters the handlers. Anti-pattern (locked): do NOT poll the editor. Mark the focused artifact's linkage so follow-up messages target it (the iterate flow). Un-skip / fill `selectionBinding.test.tsx` using a tldraw editor harness (as in `canvas.test.tsx`).</action>
  <verify>
    <automated>pnpm --filter @oneui/platform test -- selectionBinding -x</automated>
  </verify>
  <acceptance_criteria>
    - Tests 1–3 pass.
    - `grep -nE "setInterval|setTimeout.*poll" useChatCanvasFocus.ts` returns no polling; `registerAfterChangeHandler` is used and cleaned up.
    - Forward binding resolves a selected ArtifactCardShape → originating turn id; reverse calls `editor.select` + `zoomToSelection`.
    - No `(builder)`/`ExperienceCanvas` import; no `ai`/`@ai-sdk` import.
  </acceptance_criteria>
  <done>The binding hook maps selection both directions via reactive side-effects, with cleanup and no polling.</done>
</task>

<task type="auto">
  <name>Task 2: Focused-artifact indicator in the chat host (aboveComposer) + focus target wiring</name>
  <files>apps/platform/src/app/(platform)/(experience-lab)/_chat/ExperienceLabChat.tsx, apps/platform/src/app/(platform)/(experience-lab)/_chat/ExperienceLabChat.module.css</files>
  <read_first>
    - apps/platform/src/app/(platform)/(experience-lab)/_chat/ExperienceLabChat.tsx (Plan 03 — the ChatSurface host; `aboveComposer` slot + onRegenerate hook)
    - packages/ui/src/components/ChatSurface/ChatSurface.shared.ts (`aboveComposer` slot semantics; messages region already `aria-live="polite"`)
    - .planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-UI-SPEC.md (Copywriting: `Editing: {artifact name} · Run #{n}` + `Clear focus`; Color: focused indicator is a neutral Chip/Badge; a11y: focus state announced)
    - CLAUDE.md (Surface Context, Typography V2, Focus Halo)
  </read_first>
  <action>Wire `useChatCanvasFocus` into `ExperienceLabChat.tsx`: pass the tldraw `editor` (threaded from the shell) + `messages` + a `scrollToTurn` callback; render the focused-artifact indicator in the `aboveComposer` slot as a neutral `Chip`/`Badge` (`@oneui/ui-internal`) with copy `Editing: {artifact name} · Run #{n}` and a `Clear focus` action (UI-SPEC copy), and a `Focus on canvas` action per assistant turn that calls `focusOnCanvas(turnId)`. The indicator must be visible AND programmatic (announce the focus change; the ChatSurface messages region is already `aria-live="polite"`). When an artifact is focused, follow-up `onSubmit`/`onRegenerate` target that artifact (the iterate flow — re-run last prompt against the focused artifact). Author the indicator styles in `ExperienceLabChat.module.css` token-only: neutral Chip via component defaults, V2 label typography (`--Label-S-*`), pill shape, Focus Halo with `--Surface-Halo-Gap` on the clear/focus actions. No `background` on raw divs (Surface Context).</action>
  <verify>
    <automated>pnpm check:literals && pnpm --filter @oneui/platform test -- selectionBinding experienceLabChat -x</automated>
  </verify>
  <acceptance_criteria>
    - The focused-artifact indicator renders in `aboveComposer` with `Editing: … · Run #…` + `Clear focus`; selecting a canvas artifact updates it and scrolls to the turn (selectionBinding test green, no experienceLabChat regression).
    - A per-turn `Focus on canvas` action calls `focusOnCanvas` (reverse binding).
    - `pnpm check:literals` clean; indicator uses V2 label tokens + pill shape; Focus Halo via `--Surface-Halo-Gap`, never `--Surface-Main`.
    - No `(builder)`/`ExperienceCanvas` import; no `ai`/`@ai-sdk` import; deep `@oneui/ui-internal` imports.
  </acceptance_criteria>
  <done>The chat visibly + programmatically indicates the focused artifact, both binding directions work end to end, and follow-ups target the focused artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canvas selection → chat focus state | client-side editor selection drives chat focus (no server trust crossed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-13 | Tampering | focused-artifact → follow-up run targeting | mitigate | The focused artifact only seeds the next run's iterate target; the run still posts the unchanged `.strict()`-validated `RunRequestBody` to the run route. No new trust or fields granted by the binding. |
| T-04.1-14 | Denial of Service | selection observation handler | accept | Uses the existing reactive `registerAfterChangeHandler` with cleanup (no polling); identical perf posture to the already-shipped canvas selection sync. |
</threat_model>

<verification>
- `pnpm --filter @oneui/platform test -- selectionBinding experienceLabChat -x` green.
- `pnpm check:literals` clean.
- `pnpm typecheck` clean.
- `pnpm ci:gates` green (single-`ai` + literals + eslint boundary).
</verification>

<success_criteria>
- Two-way chat↔canvas selection binding works (forward + reverse) via reactive side-effects, no polling (D-02/CHAT-08).
- Chat visibly + programmatically indicates the focused artifact; follow-ups target it.
- Token-only Jio-DS; no Lab-boundary/single-`ai` violations.
</success_criteria>

<output>
Create `.planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-05-SUMMARY.md` when done.
</output>
