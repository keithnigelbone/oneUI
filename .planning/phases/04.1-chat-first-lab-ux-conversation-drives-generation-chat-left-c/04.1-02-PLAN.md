---
phase: 04.1-chat-first-lab-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "apps/platform/src/app/(platform)/(experience-lab)/_chat/ComposerControlStrip.tsx"
  - "apps/platform/src/app/(platform)/(experience-lab)/_chat/ComposerControlStrip.module.css"
autonomous: true
requirements: [CHAT-03]
user_setup: []

must_haves:
  truths:
    - "A compact control strip exposes brand / sub-brand / artifact-type / output-profile selectors."
    - "The sub-brand Select stays skip-gated until the brandId resolves against the live brands list (no ArgumentValidationError crash)."
    - "Selecting an artifact type that invalidates the current output profile auto-snaps the profile to a valid one."
    - "Control-strip state is free-form React state (lifted to the caller), NOT a tldraw shape prop."
  artifacts:
    - path: "apps/platform/src/app/(platform)/(experience-lab)/_chat/ComposerControlStrip.tsx"
      provides: "Brand/sub-brand/type/profile selector strip for the chat composer leadingInline slot (D-07)"
      contains: "isResolvedBrandId"
    - path: "apps/platform/src/app/(platform)/(experience-lab)/_chat/ComposerControlStrip.module.css"
      provides: "Token-only layout for the control strip (Surface minimal, V2 typography, f-step spacing)"
      contains: "--Spacing"
  key_links:
    - from: "ComposerControlStrip.tsx"
      to: "api.subBrandConfigs.getByParentBrand"
      via: "useQuery with 'skip' sentinel until brandId resolves"
      pattern: "isResolvedBrandId \\? .* : 'skip'"
    - from: "ComposerControlStrip.tsx"
      to: "getValidProfilesForType"
      via: "profile-options-by-type filtering"
      pattern: "getValidProfilesForType"
---

<objective>
Build the composer control strip (D-07 / CHAT-03): a compact selector strip — brand, sub-brand, artifact type, output profile — lifted from `RequestPanel.tsx`, that the chat composer renders in its `leadingInline` slot. Selections are set once and reused per turn until changed.

Purpose: This replaces the retired right-dock `RequestPanel` as the generation-context surface. It is an independent net-new file with no dependency on the chat host (the host consumes it via props), so it builds in parallel with Plan 01.

Output: `ComposerControlStrip.tsx` + its CSS module — controlled-component selectors emitting state up to the (future) chat host, preserving the Phase-02.1 sub-brand skip-gate verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-CONTEXT.md
@.planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-PATTERNS.md
@.planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-UI-SPEC.md
@apps/platform/src/app/(platform)/(experience-lab)/_panels/RequestPanel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lift RequestPanel selectors into a controlled ComposerControlStrip</name>
  <files>apps/platform/src/app/(platform)/(experience-lab)/_chat/ComposerControlStrip.tsx, apps/platform/src/app/(platform)/(experience-lab)/__tests__/composerControlStrip.test.tsx</files>
  <read_first>
    - apps/platform/src/app/(platform)/(experience-lab)/_panels/RequestPanel.tsx (analog — lift selector logic verbatim: brand options 50-64/122-131, sub-brand skip-gate 147-166, profile-by-type 170-177, type-change auto-snap 202-218)
    - apps/platform/src/app/(platform)/(experience-lab)/__tests__/requestPanel.test.tsx (analog — port the skip-gate + selector assertions)
    - packages/ui/src/components/ChatComposer/ChatComposer.shared.ts (the `leadingInline` slot the strip renders into, line 51)
    - .planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-PATTERNS.md (ComposerControlStrip section — the KEY DIFFERENCE: replace every `editor.updateShape` write with `setState`)
  </read_first>
  <behavior>
    - Test 1 (CHAT-03 selectors): renders four `Select`s (Brand, Sub-brand, Artifact type, Output profile) with options from the mocked Convex queries + `ARTIFACT_TYPES` + `getValidProfilesForType`.
    - Test 2 (CHAT-03 skip-gate, Pitfall 4): with an unresolved/stale brandId, `getByParentBrand` is called with `'skip'` (never a raw id) — no crash; the sub-brand Select shows the "Select a brand first" placeholder.
    - Test 3 (auto-snap): changing artifact type to one whose profiles exclude the current profile fires `onChange` to snap to the first valid profile.
  </behavior>
  <action>Create `ComposerControlStrip.tsx` as a controlled component taking `{ value: { brandId, subBrandConfigId?, artifactType, outputProfile }, onChange, disabled? }` and rendering four `Select`s via the `@oneui/ui-internal/components/Select/Select` deep import. Lift VERBATIM from `RequestPanel.tsx`: the `brandOptions` mapping from `useQuery(api.brands.list)`, the `ARTIFACT_TYPE_OPTIONS`, the `getValidProfilesForType` profile filtering, and the type-change auto-snap. CRITICAL — lift the `isResolvedBrandId` memo + `'skip'` sentinel exactly (RequestPanel 147-166): the `getByParentBrand` query MUST stay skipped until brandId resolves against the live `brands` list (Pitfall 4). KEY DIFFERENCE from the analog: replace every `editor.updateShape`/`updateProp` write with a call to the `onChange` prop — the strip holds NO tldraw shape, only lifted React state. Use field labels verbatim from RequestPanel (`Brand`, `Sub-brand`, `Artifact type`, `Output profile`); sub-brand placeholder copy per UI-SPEC (`Select a brand first` / `(Parent brand only)`). Import `ComponentAppearance` from `@oneui/shared` only if exposing an appearance prop (not required here). Then un-skip / fill the `composerControlStrip.test.tsx` scaffold from Plan 01.</action>
  <verify>
    <automated>pnpm --filter @oneui/platform test -- composerControlStrip -x</automated>
  </verify>
  <acceptance_criteria>
    - Test 1–3 pass.
    - `grep -n "'skip'" ComposerControlStrip.tsx` shows the sub-brand query guarded by the `isResolvedBrandId ? {...} : 'skip'` sentinel (Pitfall 4 preserved).
    - No `editor.updateShape` / tldraw shape write in the file (it is free-form controlled state).
    - No import from `(builder)`/`ExperienceCanvas`; no `ai`/`@ai-sdk` import.
    - All `@oneui/ui` imports use the `@oneui/ui-internal/components/*` deep-import alias.
  </acceptance_criteria>
  <done>The strip renders the four selectors as a controlled component, preserves the 02.1 skip-gate, auto-snaps invalid profiles, and writes nothing to tldraw.</done>
</task>

<task type="auto">
  <name>Task 2: Token-only CSS module for the control strip</name>
  <files>apps/platform/src/app/(platform)/(experience-lab)/_chat/ComposerControlStrip.module.css</files>
  <read_first>
    - apps/platform/src/app/(platform)/(experience-lab)/_panels/RequestPanel.module.css (analog — selector layout, but adapt to a horizontal compact strip)
    - .planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-UI-SPEC.md (Spacing scale, Typography V2 table, Color/Surface, Shape, Focus Halo sections)
    - CLAUDE.md (Surface Context mandate, Typography V2 rules, Focus Halo pattern, zero-tolerance literals)
  </read_first>
  <action>Author a compact horizontal strip layout using ONLY tokens: gaps via `--Spacing-3XS`/`--Spacing-5XS`, field labels via `--Label-S-FontSize` + `--Label-S-LineHeight` + `--Label-FontWeight-Medium` + `font-family: var(--Typography-Font-Primary)`, selector radius via `--Shape-3XS`. The strip background, if tinted, must be expressed by wrapping in `<Surface mode="minimal">` (Surface Context mandate) — do NOT set `background` on a raw container div in this module. No decorative borders on a tinted Surface. Net-new interactive focus styling (if any beyond what `Select` owns) uses the Focus Halo pattern with `--Surface-Halo-Gap` (never `--Surface-Main` directly). Zero literal colors/px/weights (only `0`, `100%`, `9999px` permitted by check:literals).</action>
  <verify>
    <automated>pnpm check:literals && pnpm --filter @oneui/platform test -- composerControlStrip -x</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check:literals` passes over the Lab route (no literal colors/px/weights in the new module).
    - `grep -nE "[0-9]+px" ComposerControlStrip.module.css | grep -v '9999px'` returns no disallowed px literals.
    - No `--Typography-Size-*` / `--Typography-Weight-*` legacy tokens; only V2 role tokens.
    - No `background:` on a raw container (Surface Context is the tinting mechanism).
  </acceptance_criteria>
  <done>The strip is laid out token-only, V2 typography, Surface-driven tinting, and passes check:literals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brand selection → Convex query | a selected brandId is passed to `getByParentBrand` which validates `v.id('brands')` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-04 | Denial of Service | sub-brand query with stale brandId | mitigate | Preserve the `isResolvedBrandId` + `'skip'` guard verbatim (Pitfall 4) so an unresolved id never reaches the validated Convex arg and crashes the canvas. Test 2 asserts. |
| T-04.1-05 | Tampering | client-selected artifact-type/profile pair | accept | Type/profile pairing is server-validated downstream by the unchanged run route `RunRequestBody.strict()`; the strip only auto-snaps for UX. No new trust granted. |
</threat_model>

<verification>
- `pnpm --filter @oneui/platform test -- composerControlStrip -x` green.
- `pnpm check:literals` clean.
- `pnpm typecheck` clean for the new files.
</verification>

<success_criteria>
- Four selectors render as a controlled strip with the 02.1 skip-gate intact.
- Token-only, V2 typography, Surface-driven tinting.
- No tldraw writes; no Lab-boundary or single-`ai` violations.
</success_criteria>

<output>
Create `.planning/phases/04.1-chat-first-lab-ux-conversation-drives-generation-chat-left-c/04.1-02-SUMMARY.md` when done.
</output>
