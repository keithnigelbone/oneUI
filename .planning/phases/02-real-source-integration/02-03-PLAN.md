---
phase: 02-real-source-integration
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/experience-builder-agents/src/agents/plannerAgent.ts
  - packages/experience-builder-agents/src/agents/plannerAgent.test.ts
  - packages/experience-builder-agents/src/adapters/voiceAdapter.ts
  - packages/experience-builder-agents/src/adapters/voiceAdapter.test.ts
  - packages/experience-builder-agents/src/adapters/designAdapter.ts
  - packages/experience-builder-agents/src/adapters/designAdapter.test.ts
  - packages/experience-builder-agents/src/cache.ts
  - packages/experience-builder-agents/src/cache.test.ts
  - packages/experience-builder-agents/src/backgroundRun.ts
  - packages/experience-builder-agents/src/backgroundRun.test.ts
  - packages/experience-builder-agents/src/workflow.ts
  - packages/experience-builder-agents/src/index.ts
autonomous: true
requirements: [GEN-02, GEN-03, GEN-04]
must_haves:
  truths:
    - "A net-new planner agent takes prompt + artifactType + resolved coverage and returns a schema-valid plan { sections, messageHierarchy, primaryCTA, screenCount } via Output.object"
    - "The ToV adapter builds a markup-free copy spec per section from compileVoiceRules + runToneGuard, with the model call routed through callModel"
    - "The Design adapter emits a layout/component spec fragment per section from compileCompositionRules, constrained to retrieved registry ids"
    - "Planner, Design, and ToV ADVISE only (emit structured spec fragments into run context); the IR Generator is the only step that emits IR (assembler-last, D-01)"
    - "A deterministic cache memoizes the planner/ToV/Design steps keyed on a canonical-input hash, so identical inputs do not re-call the model (D-05 Response Caching)"
    - "The per-step ExperienceBuilderEvent stream is the P2 Run-Tracing surface: every new step emits started/completed events (D-05 Run Tracing — @mastra/observability deferred to P5 per pinned-version availability)"
    - "A long generation/compile run is submitted as a Mastra background task whose progress extends the P1 NDJSON ExperienceBuilderEvent stream, resolving to the same result as the inline path (D-05 Background Tasks + streaming progress)"
    - "No Lab file imports apps/platform executors, @/lib, or ai/@ai-sdk outside modelAdapter.ts"
  artifacts:
    - path: "packages/experience-builder-agents/src/agents/plannerAgent.ts"
      provides: "GEN-04 net-new lightweight LLM planner agent (the one new eval surface)"
      contains: "Output.object"
    - path: "packages/experience-builder-agents/src/adapters/voiceAdapter.ts"
      provides: "GEN-02 ToV adapter (compileVoiceRules + runToneGuard + callModel)"
      contains: "compileVoiceRules"
    - path: "packages/experience-builder-agents/src/adapters/designAdapter.ts"
      provides: "GEN-03 Design adapter (compileCompositionRules + callModel)"
      contains: "compileCompositionRules"
    - path: "packages/experience-builder-agents/src/cache.ts"
      provides: "D-05 Response Caching: deterministic memoization keyed on a canonical-input hash"
      exports: ["cacheKey"]
    - path: "packages/experience-builder-agents/src/backgroundRun.ts"
      provides: "D-05 Background Tasks: submit a long run as a Mastra createBackgroundTask, streaming progress as ExperienceBuilderEvents"
      contains: "createBackgroundTask"
  key_links:
    - from: "packages/experience-builder-agents/src/workflow.ts"
      to: "plannerAgent + designAdapter + voiceAdapter"
      via: "plan -> design -> copy steps inserted before generate-ir (assembler-last)"
      pattern: "plan|design|copy"
    - from: "packages/experience-builder-agents/src/adapters/voiceAdapter.ts"
      to: "@oneui/shared/engine compileVoiceRules + runToneGuard"
      via: "node-safe prompt-compiler reuse (NOT the route executor — Pitfall A)"
      pattern: "compileVoiceRules|runToneGuard"
---

<objective>
Harden the Plan-01 walking spine into the full assembler-last pipeline (D-01). After this plan, a generation run plans sections via a net-new LLM planner agent (GEN-04), chooses layout/composition via the existing Design engine wrapped as a Mastra tool (GEN-03), and produces brand-aligned copy via the existing Tone-of-Voice engine wrapped as a Mastra tool (GEN-02) — all ADVISING the IR Generator, which remains the only step that commits IR. This plan also lands all three D-05 Mastra-native run-plumbing features: Response Caching (deterministic memoization of repeatable steps), Run Tracing (the per-step `ExperienceBuilderEvent` stream IS the P2 trace surface — `@mastra/observability` deferred to P5 per pinned-version availability), and Background Tasks + streaming progress (long runs submitted via `createBackgroundTask`, progress extending the P1 NDJSON event stream).

Purpose: Wire the real advisor agents into the proven Plan-01 spine without each agent emitting its own IR (the rejected "agents fight" failure mode) — the IR Generator assembles their fragments last — and complete the D-05 run-plumbing trio in-scope.
Output: `plannerAgent.ts`, `voiceAdapter.ts`, `designAdapter.ts`, `cache.ts`, `backgroundRun.ts`, and the `plan` -> `design` -> `copy` workflow steps inserted before `generate-ir`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-real-source-integration/02-CONTEXT.md
@.planning/phases/02-real-source-integration/02-RESEARCH.md
@.planning/phases/02-real-source-integration/02-PATTERNS.md
@.planning/phases/02-real-source-integration/02-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Net-new lightweight planner agent (GEN-04) + deterministic cache (D-05)</name>
  <files>packages/experience-builder-agents/src/agents/plannerAgent.ts, packages/experience-builder-agents/src/agents/plannerAgent.test.ts, packages/experience-builder-agents/src/cache.ts, packages/experience-builder-agents/src/cache.test.ts, packages/experience-builder-agents/src/index.ts</files>
  <read_first>
    - packages/experience-builder-agents/src/modelAdapter.ts (the `callModel` seam from Plan 01 — the ONLY model touchpoint; Output.object via callModel)
    - packages/experience-builder-agents/src/testModelMock.ts (Plan 01 shared model mock — reuse for credential-free planner tests)
    - packages/shared/src/engine/__tests__/cacheKey.test.ts (the repo `cacheKey`/`computeInputHash` pattern cache.ts mirrors)
    - packages/shared/src/engine/precompute.ts (lines 234-243 `computeInputHash` usage idiom — stable hash over canonical inputs)
    - .planning/phases/02-real-source-integration/02-RESEARCH.md (lines 54 GEN-04 output schema; lines 154-159 Pitfall B "response caching that isn't" — cache is a Lab-owned memoization wrapper, NOT MastraServerCache as a prompt cache)
    - .planning/phases/02-real-source-integration/02-PATTERNS.md (lines 129-135 plannerAgent pattern; lines 182-188 cache.ts pattern)
    - @oneui/shared/agent (CLAUDE_MODEL = claude-sonnet-4-6 — default planner model via the adapter)
  </read_first>
  <behavior>
    - Given a mocked model returning a valid plan, plannerAgent returns a schema-valid `{ sections, messageHierarchy, primaryCTA, screenCount }` (Output.object / structuredOutput).
    - The plan's section count is consistent with `screenCount` (no zero-section plan for a multi-screen artifact).
    - cacheKey produces an identical hash for identical canonical inputs `{ brandId, artifactType, outputProfile, prompt, requestedComponents, model }` and a different hash when any field changes.
    - A memoized step wrapped by the cache calls the underlying model exactly once for two identical-input invocations (cache hit on the second).
    - Cache state does NOT leak across Vitest cases: a fresh cache instance (or a `clearCache()` in `beforeEach`) means a new test case starts with zero entries, so the "exactly one call" assertion is sound and order-independent.
  </behavior>
  <action>
    Create `cache.ts` (D-05 Response Caching): a thin Lab-owned deterministic memoization wrapper. Export `cacheKey(input)` producing a stable hash over the canonical inputs `{ brandId, artifactType, outputProfile, prompt, requestedComponents, model }` (mirror the repo `computeInputHash` pattern). Export a `memoize`-style helper that wraps an async step and serves a cached result on a key hit. Back it with an in-memory store for dev (the Convex durable path is a later phase); do NOT treat `MastraServerCache` as an automatic prompt cache (Pitfall B). This wraps the repeatable planner/ToV/Design steps only.

    TEST ISOLATION (no leaky singleton): the cache MUST NOT rely on a module-level singleton that persists across Vitest cases. Either expose a `createCache()` factory returning a fresh instance per use (the memoize helper takes a cache instance), OR export a `clearCache()` the tests call in `beforeEach`. The `cache.test.ts` resets cache state between cases so the "exactly one model call per two identical inputs" assertion is sound and independent of test ordering.

    Create `agents/plannerAgent.ts` (GEN-04, D-02): a net-new lightweight planner. Input: `{ prompt, artifactType, resolvedCoverage }`. Output (via `callModel` with `Output.object({ schema })`): `{ sections: [...], messageHierarchy, primaryCTA, screenCount }`. Default the model to `CLAUDE_MODEL` via the adapter. Route the model call ONLY through `callModel` (no `ai`/`@ai-sdk` import here). Memoize via `cache.ts`. Export `plannerAgent`/`runPlanner` from index.ts. Add `plannerAgent.test.ts` (credential-free, model mocked) + `cache.test.ts` covering the four behaviors.
  </action>
  <verify>
    <automated>cd packages/experience-builder-agents && pnpm test plannerAgent cache 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/experience-builder-agents test plannerAgent cache` passes all four behaviors.
    - The cache-hit test asserts the underlying mocked model is invoked exactly once across two identical-input calls.
    - `cache.test.ts` resets cache state in `beforeEach` (fresh `createCache()` instance or `clearCache()`); no module-level singleton leaks across cases (`grep -c "beforeEach\|createCache\|clearCache" packages/experience-builder-agents/src/cache.test.ts` >= 1).
    - `grep -rn "from 'ai'\|@ai-sdk" packages/experience-builder-agents/src/agents packages/experience-builder-agents/src/cache.ts` returns ZERO matches.
    - The plan output is asserted schema-valid (not free-form JSON).
    - Tests need no `ANTHROPIC_API_KEY`.
  </acceptance_criteria>
  <done>The planner agent returns a schema-valid plan via Output.object routed through callModel, and the deterministic cache memoizes identical-input steps to one model call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ToV (GEN-02) + Design (GEN-03) adapters as Mastra tools reusing the node-safe engine prompt-compilers</name>
  <files>packages/experience-builder-agents/src/adapters/voiceAdapter.ts, packages/experience-builder-agents/src/adapters/voiceAdapter.test.ts, packages/experience-builder-agents/src/adapters/designAdapter.ts, packages/experience-builder-agents/src/adapters/designAdapter.test.ts, packages/experience-builder-agents/src/index.ts</files>
  <read_first>
    - packages/experience-builder-agents/src/mockGeneration.ts (gate-then-assemble + discriminated result idiom the adapters mirror; lines 52-54, 63-146)
    - packages/shared/src/engine/voiceCompiler.ts (`compileVoiceRules` L233 — config -> prompt string; this is a PROMPT-BUILDER, not the LLM call)
    - packages/shared/src/engine/index.ts (barrel: `compileVoiceRules`/`runToneGuard` lines 267-268; `compileCompositionRules`/`getDefaultCompositionConfig` line 309)
    - apps/platform/src/app/api/agent/executors/voice.ts (lines 111-145 — model-call SHAPE + the `runToneGuard` validation-loop reference ONLY; this file returns an HTTP Response, imports `ai`, reads env keys, uses @/lib — DO NOT import it, Pitfall A)
    - apps/platform/src/app/api/agent/executors/design.ts (model-call shape reference ONLY; also couples to a Convex HTTP client + @/lib — DO NOT import, Pitfall A)
    - packages/experience-builder-agents/src/modelAdapter.ts (the callModel seam — adapters route the model call here)
    - packages/experience-builder-registry/src/queryRegistry.ts (the ids designAdapter must constrain chosen components to — Pitfall #9)
    - .planning/phases/02-real-source-integration/02-PATTERNS.md (lines 81-127 voiceAdapter + designAdapter patterns; Pitfall A at lines 327-328)
  </read_first>
  <behavior>
    - voiceAdapter builds a per-section, markup-free copy spec by calling `compileVoiceRules` for the prompt and `runToneGuard` on the model output (validation loop), with the model call mocked.
    - voiceAdapter output contains no HTML/JSX/markup in any string field (markup-free, D-01 "advises").
    - designAdapter emits a per-section layout/component spec fragment from `compileCompositionRules` + `getDefaultCompositionConfig`, with the model mocked.
    - Every component the designAdapter chooses is a member of the retrieved `queryRegistry()` ids; a hallucinated component is rejected/dropped (Pitfall #9).
    - Neither adapter imports from `apps/platform`, `@/lib`, or `ai`/`@ai-sdk`.
  </behavior>
  <action>
    Create `adapters/voiceAdapter.ts` (GEN-02): a Mastra tool (`createTool({ id, description, inputSchema, outputSchema, execute })`) whose `execute` reuses the node-safe `compileVoiceRules` + `runToneGuard` from `@oneui/shared/engine` to build the prompt + validate the output, and routes the model call through `callModel` (NOT the route executor — Pitfall A). It emits a structured, markup-free copy spec fragment PER SECTION (D-01 "advises") — it does NOT produce IR. Mirror the `mockGeneration.ts` discriminated-result idiom.

    Create `adapters/designAdapter.ts` (GEN-03): a sibling Mastra tool reusing `compileCompositionRules` + `getDefaultCompositionConfig` from `@oneui/shared/engine`, model call via `callModel`. It emits a per-section layout/component spec fragment (D-01 "advises"), constraining chosen components to the retrieved `queryRegistry()` ids (Pitfall #9). Do NOT import `executors/design.ts` (Convex HTTP client + @/lib coupling — Pitfall A).

    Export both tools from index.ts. Add `voiceAdapter.test.ts` + `designAdapter.test.ts` (credential-free, model mocked via Plan-01 `testModelMock`) covering the behaviors above.
  </action>
  <verify>
    <automated>cd packages/experience-builder-agents && pnpm test voiceAdapter designAdapter 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/experience-builder-agents test voiceAdapter designAdapter` passes all behaviors.
    - `grep -rn "apps/platform\|@/lib\|from 'ai'\|@ai-sdk" packages/experience-builder-agents/src/adapters` returns ZERO matches (Pitfall A + single-seam).
    - voiceAdapter reuses `compileVoiceRules` + `runToneGuard` (`grep -c "compileVoiceRules\|runToneGuard" voiceAdapter.ts` >= 2).
    - designAdapter reuses `compileCompositionRules` (`grep -c "compileCompositionRules" designAdapter.ts` >= 1) and asserts chosen components are registry members.
    - The copy-spec markup-free test asserts no string field contains `<` tag-like markup.
    - Tests need no `ANTHROPIC_API_KEY`.
  </acceptance_criteria>
  <done>ToV and Design are wired as Mastra tools reusing only the node-safe engine prompt-compilers (internals untouched), each emitting a markup-free advisory spec fragment, with the model call through the single seam.</done>
</task>

<task type="auto">
  <name>Task 3: Insert plan -> design -> copy steps into the workflow spine (assembler-last, D-01)</name>
  <files>packages/experience-builder-agents/src/workflow.ts, packages/experience-builder-agents/src/index.ts</files>
  <read_first>
    - packages/experience-builder-agents/src/workflow.ts (the spine + `ORDERED_STEPS` + `createStep` pattern; AFTER Plan 01 it runs intent -> resolve -> retrieve -> generate-ir(real) -> compile -> validate; the per-step `emit` idiom lines 149-151; the `RunContext` interface lines 137-147)
    - packages/experience-builder-agents/src/agents/plannerAgent.ts (Task 1 — `runPlanner`)
    - packages/experience-builder-agents/src/adapters/voiceAdapter.ts + designAdapter.ts (Task 2 tools)
    - packages/experience-builder-agents/src/irGenerator.ts (Plan 01 — `generateIR` now consumes the advisory fragments from run context)
    - .planning/phases/02-real-source-integration/02-RESEARCH.md (lines 186-234 the assembler-last pipeline diagram; lines 414-416 open `step` field — new step names need no schema change)
    - .planning/phases/02-real-source-integration/02-PATTERNS.md (lines 204-210 workflow.ts spine extension)
  </read_first>
  <action>
    Edit workflow.ts to insert three new steps between `retrieve` and `generate-ir`, matching the D-01 assembler-last topology: `plan` (calls `runPlanner`), `design` (calls the designAdapter tool per section), `copy` (calls the voiceAdapter tool per section). Each step writes its structured fragment into `RunContext` (extend the interface with `plan`, `designSpec`, `copySpec` fields) — these are ADVISORY; none emits IR. The `generate-ir` step (irGenerator) reads these fragments from context and assembles the final IR (it is the only committing step — D-01; it does NOT call the agents mid-generation). Each new step emits `started`/`completed` `ExperienceBuilderEvent`s (the `step` field is open `z.string()`, no schema change). Add the three steps to `ORDERED_STEPS` and the `.then(...)` spine in order. Wrap the planner/design/copy step bodies with the `cache.ts` memoization so identical inputs reuse results (D-05 Response Caching). The gap short-circuit + `ctx.halted` branching stays the workflow's job (ORCH-04). Update workflow.test.ts expectations for the new ordered step names if it asserts the step sequence.

    D-05 RUN TRACING (explicit, in-scope): the per-step `started`/`completed` `ExperienceBuilderEvent` stream emitted by every step IS the P2 Run-Tracing surface — this satisfies the D-05 Run-Tracing sub-requirement for P2. This is honored by D-05's own locked caveat that some Mastra-native features may be Cloud-only/newer-API: RESEARCH verified `@mastra/observability@1.14.0` is NOT installed at the pinned `@mastra/core@1.37.1` (Pitfall C / Open Q3 RESOLVED), so richer `Observability` spans are the genuine P5 (PROD-02) extension, NOT a P2 omission. Do NOT pass an `observability` instance to `Mastra` (it would silently fall back to `NoOpObservability`); the event stream is the trace surface. Cite D-05 + the RESEARCH availability finding in a code comment on the per-step `emit` so the rationale is in the code, not buried.
  </action>
  <verify>
    <automated>cd packages/experience-builder-agents && pnpm test workflow 2>&1 | tail -25 && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/experience-builder-agents test workflow` passes; the asserted ordered step sequence includes `intent, resolve-foundation, retrieve, plan, design, copy, generate-ir, compile, validate`.
    - A workflow test asserts the planner/design/copy steps emit `started`+`completed` events and write advisory fragments into context, and that ONLY `generate-ir` produces the `ir-produced` event (assembler-last invariant, D-01).
    - `pnpm --filter @oneui/experience-builder-agents typecheck` is clean.
    - The gap short-circuit still halts before `generate-ir` when resolve/retrieve gaps (existing behavior preserved).
  </acceptance_criteria>
  <done>The workflow runs the full assembler-last pipeline: plan/design/copy advise (memoized), generate-ir commits the only IR, compile + validate follow — all sequencing in the workflow (ORCH-04). The per-step event stream is the explicit D-05 Run-Tracing surface.</done>
</task>

<task type="auto">
  <name>Task 4: Background Tasks + streaming progress (D-05) — submit a long run via createBackgroundTask</name>
  <files>packages/experience-builder-agents/src/backgroundRun.ts, packages/experience-builder-agents/src/backgroundRun.test.ts, packages/experience-builder-agents/src/index.ts</files>
  <read_first>
    - packages/experience-builder-agents/src/workflow.ts (the `runExperienceWorkflow` runner + the per-step `emit` -> `ExperienceBuilderEvent` idiom lines 149-151; `RunExperienceResult` shape — the background path must resolve to the SAME result)
    - packages/experience-builder-agents/src/modelAdapter.ts (`toV6WorkflowStream` + `AI_SDK_STREAM_VERSION='v6'` — the existing v6 transport the streaming progress rides; do NOT add a second `ai` import)
    - .planning/phases/02-real-source-integration/02-RESEARCH.md (Background Tasks + Streaming Progress section — `@mastra/core/background-tasks` exports `createBackgroundTask`, `BackgroundTaskManager`, in-package at the pinned version, NO Cloud dependency; D-05 states background/streaming extends the P1 NDJSON `ExperienceBuilderEvent` stream)
    - packages/experience-builder-core/src/contracts/events.ts (the `ExperienceBuilderEvent` `step` field is open `z.string()` — progress frames need no schema change)
    - apps/platform/src/app/api/experience-lab/run/route.ts (the P1 NDJSON stream the background progress extends — read-only reference; do NOT edit here, Plan 04 owns the route)
  </read_first>
  <action>
    Create `backgroundRun.ts` (D-05 Background Tasks): wrap the existing `runExperienceWorkflow` so a long generation/compile run is submitted as a Mastra background task via `createBackgroundTask` from `@mastra/core/background-tasks` (in-package at the pinned `@mastra/core@1.37.1`, NO Cloud dependency — RESEARCH-verified). As the background task runs, stream progress by pushing `ExperienceBuilderEvent`s (reuse the workflow's per-step `emit` events; the `step` field is open `z.string()` so progress frames need no schema change) — this EXTENDS the P1 NDJSON event stream over the existing `@mastra/ai-sdk` v6 transport (`modelAdapter.toV6WorkflowStream`); do NOT add a second `ai` import. The background path MUST resolve to the same `RunExperienceResult` as the inline `runExperienceWorkflow` path (same events, same IR/validation/bundle, same outcome) — it is a transport/progress wrapper, not a different pipeline. Export `runExperienceWorkflowBackground` (or equivalent) from index.ts. Sequencing/branching stays in the workflow (ORCH-04); the background wrapper only governs submission + progress emission. Add `backgroundRun.test.ts` (credential-free, model mocked) asserting the behaviors below.
  </action>
  <verify>
    <automated>cd packages/experience-builder-agents && pnpm test backgroundRun 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/experience-builder-agents test backgroundRun` passes.
    - A background-task run emits >= 1 progress `ExperienceBuilderEvent` during execution (`grep -c "ExperienceBuilderEvent\|type: 'step'" packages/experience-builder-agents/src/backgroundRun.test.ts` >= 1 and the test asserts the progress event count > 0).
    - The background path resolves to the SAME `RunExperienceResult` as the inline `runExperienceWorkflow` path for identical input (test asserts deep-equality of `outcome` + `ir` + `events` set, model mocked deterministically).
    - `backgroundRun.ts` imports `createBackgroundTask` from `@mastra/core/background-tasks` (`grep -c "createBackgroundTask" packages/experience-builder-agents/src/backgroundRun.ts` >= 1).
    - `grep -rn "from 'ai'\|@ai-sdk" packages/experience-builder-agents/src/backgroundRun.ts` returns ZERO matches (single-`ai`-seam intact); `pnpm why ai` resolves one version.
    - Tests need no `ANTHROPIC_API_KEY`.
  </acceptance_criteria>
  <done>A long run is submitted as a Mastra in-package background task whose progress extends the P1 NDJSON ExperienceBuilderEvent stream and resolves to the same result as the inline path — completing the D-05 run-plumbing trio in-scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM advisory output -> run context | Planner/Design/ToV fragments are untrusted model output; copy must be markup-free, design ids must be registry members before the IR Generator consumes them |
| Lab adapter -> engine | Adapters call only the public node-safe engine prompt-compilers; the route executors (with env keys + @/lib + Convex coupling) are NOT crossed |
| adapter model call -> Anthropic | All model calls funnel through callModel; key confined there |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | designAdapter (LLM picks an unregistered/hallucinated component) | mitigate | Constrain chosen components to retrieved `queryRegistry()` ids; reject/drop non-members (Pitfall #9); proven by the registry-membership test |
| T-02-09 | Tampering | voiceAdapter (copy spec carries smuggled JSX/HTML) | mitigate | Markup-free copy spec asserted in test; the IR Generator's `parseIR` markup-free guard is the final gate before compile (Pitfall #2) |
| T-02-10 | Elevation of Privilege / Info Disclosure | Lab adapter importing apps/platform executors (couples to @/lib, Convex HTTP client, ai, env keys) | mitigate | Reuse only the node-safe engine prompt-compilers; grep gate asserts no `apps/platform`/`@/lib`/`ai` import in adapters (Pitfall A, isolation invariant) |
| T-02-11 | Spoofing | Prompt-injection into the planner/IR Generator influencing emitted code | mitigate | Planner output is schema-constrained (Output.object); designAdapter component choices constrained to registry ids; IR Generator's markup-free + registry guards reject injected components/markup downstream |
| T-02-12 | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — all three D-05 features use already-installed in-package APIs (`cache.ts` memoization for Response Caching; the existing `ExperienceBuilderEvent` stream for Run Tracing; `@mastra/core/background-tasks` `createBackgroundTask` for Background Tasks). Richer `@mastra/observability` spans are a P5 (PROD-02) extension per its pinned-version availability, not a P2 D-05 omission. Single-`ai`-version gate unaffected |
</threat_model>

<verification>
- `pnpm --filter @oneui/experience-builder-agents test` green (plannerAgent, cache, voiceAdapter, designAdapter, workflow, backgroundRun, plus Plan-01 suites).
- `pnpm --filter @oneui/experience-builder-agents typecheck` clean.
- No `apps/platform`/`@/lib`/`ai`/`@ai-sdk` import outside `modelAdapter.ts`.
- `pnpm why ai` resolves a single `ai` version.
</verification>

<success_criteria>
- A net-new planner agent plans sections/hierarchy/CTA/screen-count via Output.object (GEN-04).
- The existing ToV agent produces brand-aligned per-section copy wired as a Mastra tool, internals untouched (GEN-02).
- The existing Design agent chooses layout/composition within Jio constraints wired as a Mastra tool, internals untouched (GEN-03).
- Planner/Design/ToV advise; the IR Generator commits the only IR (assembler-last, D-01).
- All three D-05 Mastra-native run-plumbing features land in-scope: Response Caching (deterministic memoization), Run Tracing (the per-step `ExperienceBuilderEvent` stream IS the P2 trace surface; `@mastra/observability` deferred to P5 per pinned-version availability), and Background Tasks + streaming progress (`createBackgroundTask` extending the P1 NDJSON event stream).
</success_criteria>

<output>
Create `.planning/phases/02-real-source-integration/02-03-SUMMARY.md` when done.
</output>
