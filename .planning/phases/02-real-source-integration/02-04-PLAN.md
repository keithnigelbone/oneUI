---
phase: 02-real-source-integration
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - packages/convex/convex/schema.ts
  - packages/convex/convex/experienceRuns.ts
  - apps/platform/src/app/api/experience-lab/run/route.ts
  - package.json
  - scripts/check-experience-gates.ts
autonomous: true
requirements: [GEN-06, REG-04]
user_setup:
  - service: convex
    why: "Additive schema field must be applied to the live Convex deployment before bundle persistence verifies"
    dashboard_config:
      - task: "Run npx convex dev (or deploy) so the new compiledBundle field is applied"
        location: "local Convex dev process / Convex deployment"
must_haves:
  truths:
    - "experienceArtifactVersions has an additive optional compiledBundle field; existing rows (no bundle) still round-trip — no data migration"
    - "persistArtifact accepts and stores the compiled bundle alongside the IR (append-only, never raw markup as source of truth)"
    - "The Lab run route passes the workflow's compiled bundle through to persistArtifact only for a valid-IR run"
    - "The Convex schema is pushed/validated against the live deployment BEFORE bundle-persistence is verified (type-check alone is a false positive)"
    - "pnpm ci:gates runs the REG-04 freshness gate and the GEN-06 acceptance (tsc + validateAst + snapshot) and hard-fails on divergence"
  artifacts:
    - path: "packages/convex/convex/schema.ts"
      provides: "Additive compiledBundle field on experienceArtifactVersions (D-07 persistence)"
      contains: "compiledBundle"
    - path: "scripts/check-experience-gates.ts"
      provides: "CI orchestration entry that runs the REG-04 + GEN-06-acceptance package tests"
  key_links:
    - from: "apps/platform/src/app/api/experience-lab/run/route.ts"
      to: "packages/convex/convex/experienceRuns.ts persistArtifact"
      via: "compiledBundle passed through persistRun -> persistArtifact"
      pattern: "compiledBundle"
    - from: "package.json ci:gates"
      to: "the REG-04 freshness test + GEN-06 compiler acceptance"
      via: "check-experience-gates script in the ci:gates chain"
      pattern: "check:experience-gates|experience-gates"
---

<objective>
Persist the GEN-06 canonical compiled bundle and lock the new Phase-2 gates into CI. After this plan, a valid-IR run persists its codegen bundle alongside the IR in `experienceArtifactVersions` (additive, append-only, no migration — D-07), the live Convex schema is pushed before any bundle-persistence verification (avoiding the type-check false-positive flagged in the schema note), and `pnpm ci:gates` runs the REG-04 freshness gate plus the GEN-06 acceptance triad so drift or a broken compiler hard-fails CI.

Purpose: Make the compiled bundle durable (the artifact P3 sandboxes / P5 exports) and make the Phase-2 guarantees enforced by CI rather than convention.
Output: The `compiledBundle` schema field + mutation arg, the run-route pass-through, the Convex schema push step, and the `ci:gates` wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-real-source-integration/02-CONTEXT.md
@.planning/phases/02-real-source-integration/02-RESEARCH.md
@.planning/phases/02-real-source-integration/02-PATTERNS.md
@.planning/phases/02-real-source-integration/02-01-SUMMARY.md
@.planning/phases/02-real-source-integration/02-02-SUMMARY.md
@.planning/phases/02-real-source-integration/02-03-SUMMARY.md
@.claude/skills/convex-schema-rules.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive compiledBundle field + persistArtifact arg + run-route pass-through, then push the Convex schema</name>
  <files>packages/convex/convex/schema.ts, packages/convex/convex/experienceRuns.ts, apps/platform/src/app/api/experience-lab/run/route.ts</files>
  <read_first>
    - packages/convex/convex/schema.ts (the `experienceArtifactVersions` table lines 2025-2039 — `ir: v.any()`, `validation: v.optional(v.any())`; add ONE additive `v.optional()` field beside them)
    - packages/convex/convex/experienceRuns.ts (the `persistArtifact` mutation lines 89-132 — args + the `experienceArtifactVersions` insert lines 117-124; add `compiledBundle` arg + insert it)
    - apps/platform/src/app/api/experience-lab/run/route.ts (the `persistRun` helper lines 56-106 — the `persistArtifact` call lines 90-97 inside the `run.outcome === 'artifact'` branch; `RunExperienceResult` now carries `bundle` from Plan 01 Task 3)
    - .claude/skills/convex-schema-rules.md (project Convex schema conventions)
    - .planning/phases/02-real-source-integration/02-PATTERNS.md (lines 233-250 the schema-edit pattern; additive `v.optional()`, structured/text, never raw markup as source of truth)
    - <schema_note in planning context>: type-checks pass without a live schema push because types come from generated `_generated`, not the live deployment — the push is mandatory before verification
  </read_first>
  <action>
    Add an additive `compiledBundle: v.optional(v.object({ code: v.string(), meta: v.optional(v.any()) }))` field to `experienceArtifactVersions` in schema.ts — beside `ir`/`validation`. It stores the GEN-06 canonical codegen string + meta (D-07); `v.optional` so existing rows round-trip (append-only, no migration). NEVER raw markup as a source of truth — the IR remains canonical; this is the compiled output.

    Add a matching `compiledBundle: v.optional(...)` arg to the `persistArtifact` mutation and include it in the `experienceArtifactVersions` insert.

    In the run route's `persistRun`, pass `run.bundle` through to `persistArtifact` (only inside the existing `run.outcome === 'artifact' && run.ir` branch — a gap/error run persists no artifact, FND-03). This route is the Lab's own file (NOT the forbidden ExperienceCanvas/(builder)) — editing it is in-scope.

    AFTER the schema edit, push/validate the schema against the live Convex deployment (`npx convex dev --once` or the repo's equivalent codegen/deploy command) so the `compiledBundle` field is applied and the generated types regenerate. This MUST happen before the bundle-persistence verification below — a `pnpm typecheck` alone passes against the old generated types and would be a false positive (per the schema note).
  </action>
  <verify>
    <automated>(cd packages/convex && npx convex dev --once 2>&1 | tail -8 || echo "convex push attempted"); pnpm --filter @oneui/convex typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "compiledBundle" packages/convex/convex/schema.ts` >= 1 and the field is `v.optional(...)`.
    - `grep -c "compiledBundle" packages/convex/convex/experienceRuns.ts` >= 1 (arg + insert).
    - `grep -c "bundle" apps/platform/src/app/api/experience-lab/run/route.ts` >= 1 (pass-through inside the artifact branch).
    - The Convex schema push/codegen command ran (generated types include the new field) BEFORE the typecheck verification — not a type-check-only pass.
    - `pnpm typecheck` is clean after the schema regenerates.
    - The forbidden files were NOT edited: `git diff --name-only` shows no change to `apps/platform/src/design-tools/ExperienceCanvas/**`, the `(builder)` route group, or `FoundationStyleProvider.tsx`.
  </acceptance_criteria>
  <done>The compiled bundle persists additively alongside the IR (append-only, no migration), the run route passes it through for valid-IR runs only, and the live schema is pushed before verification so the typecheck is genuine.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the REG-04 freshness gate + GEN-06 acceptance into pnpm ci:gates</name>
  <files>package.json, scripts/check-experience-gates.ts</files>
  <read_first>
    - package.json (the `ci:gates` chain line 134; the existing gate-script naming idiom lines 111-122, e.g. `check:single-ai`, `check:literals`; `smoke:builder`)
    - scripts/check-single-ai-version.ts (the existing gate-script shape to mirror for a new tsx gate script)
    - packages/experience-builder-registry/src/queryRegistry.freshness.test.ts (Plan 02 — the REG-04 gate to invoke)
    - packages/experience-builder-agents/src/compiler.test.ts (Plan 01 — the GEN-06 acceptance triad to invoke)
    - .planning/phases/02-real-source-integration/02-RESEARCH.md (lines 449-452 Sampling Rate — phase gate is `pnpm ci:gates` green incl. REG-04 + GEN-06 acceptance)
    - .planning/phases/02-real-source-integration/02-VALIDATION.md (Wave 0: wire REG-04 + GEN-06 acceptance into ci:gates)
  </read_first>
  <action>
    Add `scripts/check-experience-gates.ts` (a tsx gate mirroring `check-single-ai-version.ts`'s shape) that runs the Phase-2 enforcement tests and exits non-zero on failure: the REG-04 freshness test (`pnpm --filter @oneui/experience-builder-registry test freshness`) and the GEN-06 compiler acceptance test (`pnpm --filter @oneui/experience-builder-agents test compiler`). Add a `check:experience-gates` script in package.json invoking it, and insert `pnpm check:experience-gates` into the `ci:gates` chain (alongside the existing `check:single-ai`/`smoke:builder` gates, before `typecheck`). Keep the existing `check:single-ai` gate in the chain (single-`ai`-version invariant). Do NOT remove or reorder existing gates.
  </action>
  <verify>
    <automated>pnpm check:experience-gates 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check:experience-gates` runs the REG-04 freshness test + the GEN-06 compiler acceptance test and exits 0 when both pass.
    - `grep -c "check:experience-gates" package.json` >= 2 (script definition + ci:gates chain entry).
    - The `ci:gates` chain still contains `check:single-ai` and `smoke:builder` (`grep -c "check:single-ai" package.json` >= 1) — no existing gate removed.
    - Intentionally breaking the freshness derivation (temporary local edit) makes `pnpm check:experience-gates` exit non-zero (hard-fail proven), then revert.
  </acceptance_criteria>
  <done>`pnpm ci:gates` now enforces REG-04 registry freshness and the GEN-06 acceptance triad, hard-failing CI on drift or a broken compiler, with the single-`ai` gate intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiled bundle -> Convex persistence | The codegen string is stored as the compiled output; the IR remains canonical, the bundle is never the source of truth and never raw markup |
| CI gate boundary | The REG-04 + GEN-06 gates are the enforcement point that makes the Phase-2 guarantees provable rather than conventional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | schema.ts (persisting raw markup as a source of truth) | mitigate | `compiledBundle` is the compiled codegen string ONLY; IR stays canonical `v.any()` structured JSON; field is additive `v.optional` |
| T-02-14 | Repudiation | False-positive verification (typecheck passes against stale generated types without a live schema push) | mitigate | Mandatory Convex schema push/codegen BEFORE bundle-persistence verification (per the schema note); acceptance asserts the push ran |
| T-02-15 | Tampering | Registry/compiler drift slipping past CI | mitigate | `check:experience-gates` runs REG-04 + GEN-06 acceptance in `ci:gates`; hard-fail proven by the temporary-break acceptance step |
| T-02-16 | Elevation of Privilege | Accidentally editing the forbidden ExperienceCanvas/(builder)/FoundationStyleProvider | mitigate | Acceptance asserts `git diff --name-only` shows no change to those paths; the edited route is the Lab's own file |
| T-02-17 | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `pnpm typecheck` clean after the Convex schema regenerates with `compiledBundle`.
- `pnpm check:experience-gates` green; breaking the gate proves hard-fail.
- `pnpm ci:gates` includes the new gate and the intact `check:single-ai`/`smoke:builder` gates.
- No forbidden file edited (`git diff --name-only` clean for ExperienceCanvas/(builder)/FoundationStyleProvider).
</verification>

<success_criteria>
- The GEN-06 compiled bundle persists durably alongside the IR (additive, append-only — D-07).
- The live Convex schema is pushed before bundle-persistence verification (no false-positive typecheck).
- `pnpm ci:gates` enforces REG-04 freshness + GEN-06 acceptance, with the single-`ai` invariant preserved.
</success_criteria>

<output>
Create `.planning/phases/02-real-source-integration/02-04-SUMMARY.md` when done.
</output>
