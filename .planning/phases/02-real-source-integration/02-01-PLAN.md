---
phase: 02-real-source-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/experience-builder-agents/src/modelAdapter.ts
  - packages/experience-builder-agents/src/foundationResolver.ts
  - packages/experience-builder-agents/src/foundationResolver.test.ts
  - packages/experience-builder-agents/src/irGenerator.ts
  - packages/experience-builder-agents/src/irGenerator.test.ts
  - packages/experience-builder-agents/src/compiler.ts
  - packages/experience-builder-agents/src/compiler.test.ts
  - packages/experience-builder-agents/src/testModelMock.ts
  - packages/experience-builder-agents/src/workflow.ts
  - packages/experience-builder-agents/src/index.ts
autonomous: true
requirements: [FND-04, GEN-05, GEN-06]
must_haves:
  truths:
    - "A real (non-placeholder) brand prompt resolves real Jio foundations via buildThemeConfig and returns { ok: true, theme }"
    - "A partially-configured brand resolves successfully (system defaults fill it) and does NOT gap-report (D-09)"
    - "A genuinely unresolvable profile still returns a typed foundation gap with no dimension numbers (FND-03)"
    - "The IR Generator produces a parseIR-valid JioExperienceIR via Output.object structured output, constrained to retrieved registry component ids"
    - "The IR Generator re-prompts with appended error on a Zod-parse OR compiled-AST-validate failure, caps at ~3 attempts, then emits a gap (D-06)"
    - "The compiler emits a canonical React + Jio CSS code string whose imports resolve to @oneui/ui, and the codegen string is snapshot-stable for a fixed IR"
    - "callModel is the ONLY module path that imports ai / @ai-sdk; pnpm why ai still resolves a single version"
  artifacts:
    - path: "packages/experience-builder-agents/src/irGenerator.ts"
      provides: "GEN-05 LLM IR Generator with per-section decomposition + in-gen retry (replaces mockGeneration 1:1)"
      contains: "Output.object"
    - path: "packages/experience-builder-agents/src/compiler.ts"
      provides: "GEN-06 IR->AST->React+Jio CSS compiler + credential-free acceptance triad"
      exports: ["compile"]
    - path: "packages/experience-builder-agents/src/testModelMock.ts"
      provides: "Shared deterministic, credential-free model mock for GEN-05 tests"
  key_links:
    - from: "packages/experience-builder-agents/src/foundationResolver.ts"
      to: "@oneui/shared/engine buildThemeConfig"
      via: "real resolution body swap"
      pattern: "buildThemeConfig"
    - from: "packages/experience-builder-agents/src/irGenerator.ts"
      to: "packages/experience-builder-agents/src/modelAdapter.ts callModel"
      via: "single model seam"
      pattern: "callModel"
    - from: "packages/experience-builder-agents/src/compiler.ts"
      to: "@oneui/shared/codegen astToReactComponent + @oneui/experience-builder-validation validateAst"
      via: "irToAst -> astToReactComponent + validateAst(irToArtifactAst(ir))"
      pattern: "astToReactComponent|validateAst"
---

<objective>
Build the thinnest real end-to-end generation path — the Phase-2 walking slice. After this plan, a real prompt for a real brand resolves REAL Jio foundations (FND-04), an LLM IR Generator commits a valid `JioExperienceIR` via `Output.object` with in-gen retry (GEN-05), and a deterministic compiler turns that IR into a canonical React + Jio CSS code string proven by the credential-free acceptance triad (GEN-06). The ToV/Design/planner advisors are added in Plan 02 (assembler-last D-01); this slice proves the spine works with minimal advisory input.

Purpose: De-risk the three genuinely net-new pieces (the model seam, the structured-output IR Generator + retry loop, the compiler) first, so the advisor layer in Plan 02 wires into a proven pipeline.
Output: A real-model `callModel` seam, a real foundation resolver, `irGenerator.ts`, `compiler.ts`, a shared model-mock test helper, and the `generate-ir` + `compile` workflow steps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-real-source-integration/02-CONTEXT.md
@.planning/phases/02-real-source-integration/02-RESEARCH.md
@.planning/phases/02-real-source-integration/02-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the real model seam (callModel) + extend the resolver to real buildThemeConfig (FND-04)</name>
  <files>packages/experience-builder-agents/src/modelAdapter.ts, packages/experience-builder-agents/src/foundationResolver.ts, packages/experience-builder-agents/src/foundationResolver.test.ts, packages/experience-builder-agents/src/testModelMock.ts</files>
  <read_first>
    - packages/experience-builder-agents/src/modelAdapter.ts (the `callModel` stub at lines 61-66; KEEP `AI_SDK_STREAM_VERSION='v6'` line 30 + `toV6WorkflowStream` lines 44-50 untouched)
    - packages/experience-builder-agents/src/foundationResolver.ts (success arm lines 99-119; mockTheme lines 71-78; isCoveredProfile + gap arm to KEEP verbatim)
    - packages/experience-builder-agents/src/foundationResolver.test.ts (existing P1 test; gap-arm "no numeric dimensions" assertions lines 61-72 and determinism lines 78-85 must carry over)
    - packages/shared/src/engine/buildThemeConfig.ts (signature lines 102-138; `ThemeConfigAppearanceInput` permissive shape lines 31-51; `ensureNeutralRole`/`synthesizeBrandBgIfMissing` ARE the D-09 system defaults)
    - packages/shared/src/engine/precompute.ts (THE node-safe mapping to reuse — resolves Open Q1 / A2 MEDIUM-confidence: this file is the server-side equivalent of `useBrandCSS` with no React/`FoundationStyleProvider` dependency. `PrecomputeInput` shape lines 90-102; the exact field source names `colorConfig` + `presetSelection` + `appearanceConfig` lines 92-94; the chain `buildAvailableScales(colorConfig, presetSelection)` -> `buildThemeConfig(availableScales, appearanceConfig)` lines 246-249) — reuse this chain verbatim; do NOT hand-roll a divergent adapter
    - packages/shared/src/engine/index.ts (barrel: `buildAvailableScales` line 19, `buildThemeConfig`/`ThemeConfigAppearanceInput` lines 172-173) — node-safe, do NOT import FoundationStyleProvider (forbidden file)
    - apps/platform/src/app/api/agent/executors/voice.ts (lines 111-145 — model-call SHAPE reference ONLY: `anthropic(model)` + `generateText`; do NOT import this file)
  </read_first>
  <action>
    Implement `callModel` in modelAdapter.ts as the single typed model seam wrapping `@ai-sdk/anthropic`'s `anthropic(model)` and `ai`'s `generateText` with an `Output.object({ schema })` option. Signature accepts `{ schema, prompt, system?, model? }` and returns the parsed structured output read from `result.experimental_output` (VERIFIED accessor: `GenerateTextResult.experimental_output` in `ai@6.0.111`). Default model id to `CLAUDE_MODEL` (`claude-sonnet-4-6`) from `@oneui/shared/agent`. Read `ANTHROPIC_API_KEY` server-side only; never log it. This MUST remain the ONLY module importing `ai`/`@ai-sdk/*` (ORCH-04 / Pitfall #1). Put NO sequencing/branching/retry here — those live in irGenerator.ts/workflow.ts.

    Add `packages/experience-builder-agents/src/testModelMock.ts`: a shared, credential-free model-mock factory that lets tests inject a deterministic `callModel` replacement (e.g. a queue of fail-then-succeed structured outputs) without `ANTHROPIC_API_KEY`. Export a factory the GEN-05 tests (Task 2) reuse. To keep `callModel` mockable, expose it via a small indirection (a module-level seam the tests can override), not a hard import — mirror the repo's existing dependency-injection-for-tests idiom.

    Swap the foundationResolver.ts success arm (line 118 `foundationResolved(mockTheme())`) for the real call per D-09, REUSING the existing node-safe `precompute.ts` chain (Open Q1 / A2 resolution) rather than hand-rolling a new adapter: derive `availableScales` via `buildAvailableScales(colorConfig, presetSelection)` then call `buildThemeConfig(availableScales, appearanceConfig)`. Source the three inputs from the brand's Convex foundations record (the `getBrandOverviewData`-shaped data) using the EXACT `PrecomputeInput` field names (precompute.ts lines 92-94): `colorConfig`, `presetSelection`, and `appearanceConfig`. If `buildThemeConfig` returns `null`, return `foundationGap({ artifactType, outputProfile, reason: 'unresolvable profile' })` (FND-03). Otherwise `foundationResolved(theme)`. Delete `mockScaleDefinition`/`mockTheme`; KEEP `isCoveredProfile` and the existing uncovered-profile gap arm verbatim. Implement FND-04 per D-04/D-09 and the Open Q1 resolution (precompute.ts chain — never import `FoundationStyleProvider`). Extend the resolver input type (add the three brand-foundation fields above) without changing the frozen `FoundationResolveResult` return contract.

    Extend foundationResolver.test.ts: keep the gap-arm "no dimension numbers" + determinism assertions; add (a) a configured-brand case asserting `{ ok: true, theme }` with `theme.appearances.primary` populated, (b) a partial-brand case (configured primary only) asserting it resolves (NOT gaps) because `ensureNeutralRole` fills the neutral default (D-09), (c) an unresolvable case (empty scales) asserting the gap arm with no numeric dimensions. Mock the Convex-foundations input; the resolver has no model so no model mock here.
  </action>
  <verify>
    <automated>cd packages/experience-builder-agents && pnpm test foundationResolver 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/experience-builder-agents test foundationResolver` passes including the new configured/partial/unresolvable cases.
    - `grep -rn "from 'ai'\|@ai-sdk" packages/experience-builder-agents/src | grep -v modelAdapter.ts` returns ZERO matches (single-seam invariant; comments in other files are fine but no imports).
    - `pnpm why ai` resolves exactly one `ai` version.
    - `mockScaleDefinition`/`mockTheme` are deleted from foundationResolver.ts (`grep -c mockTheme packages/experience-builder-agents/src/foundationResolver.ts` returns 0).
    - `callModel` no longer throws the P1 "not available" error.
  </acceptance_criteria>
  <done>callModel is a real, single-seam structured-output model call reading `experimental_output`; the resolver resolves real foundations with D-09 partial-brand defaults and gaps only on a null/unresolvable result; the shared test model mock exists and is credential-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IR Generator with Output.object, per-section decomposition, and in-gen retry (GEN-05)</name>
  <files>packages/experience-builder-agents/src/irGenerator.ts, packages/experience-builder-agents/src/irGenerator.test.ts, packages/experience-builder-agents/src/index.ts</files>
  <read_first>
    - packages/experience-builder-agents/src/mockGeneration.ts (the file irGenerator REPLACES 1:1: discriminated `MockGenerateResult` shape lines 52-54; gate-then-assemble structure lines 63-146; defensive safeParse lines 139-145)
    - packages/experience-builder-agents/src/compiler.ts (created in Task 3 — irGenerator calls `compile()` for the compiled-AST retry check; if Task 3 lands second, stub the import and wire after)
    - packages/experience-builder-agents/src/modelAdapter.ts (the `callModel` seam from Task 1 — the ONLY model touchpoint)
    - packages/experience-builder-agents/src/testModelMock.ts (Task 1 — fail-then-succeed mock for the retry test)
    - packages/experience-builder-core/src/ir/schema.ts (`JioExperienceIR` Zod schema, `parseIR`; brandId line 186; markup-free string refinement is the Pitfall #2 guard)
    - .planning/phases/02-real-source-integration/02-RESEARCH.md (lines 369-389 GEN-05 retry example; lines 145-152 Output.object idiom + per-section decomposition; Pitfall D lines 343-345)
    - packages/experience-builder-registry/src/queryRegistry.ts (component ids the IR `type` must be constrained to — Pitfall #9)
  </read_first>
  <behavior>
    - Given a mocked model returning a parseIR-valid IR, generateIR returns `{ ok: true, ir }` and the ir passes `parseIR`.
    - Given a mocked model that returns Zod-invalid output then valid output, generateIR re-prompts with the appended error and succeeds on attempt 2 (in-gen retry, D-06).
    - Given a mocked model that returns IR failing the compiled-AST `validateAst` then nothing valid within the cap, generateIR returns `{ ok: false }` with a gap after ~3 attempts (cap enforced).
    - The IR `type` of every component instance is one of the retrieved `queryRegistry()` ids; an IR naming an unregistered id is rejected (component gap), never compiled (Pitfall #9 / REG-03).
    - Smuggled JSX/HTML in any IR string field is rejected by the markup-free Zod guard -> gap, never compile (Pitfall #2).
  </behavior>
  <action>
    Create `irGenerator.ts` to replace `mockGeneration.ts` 1:1. Preserve the discriminated result shape (`{ ok: true; ir } | { ok: false; foundationGap?; componentGap? }`) and the gate-then-assemble structure. Swap the hand-shaped IR for a real `callModel` invocation using `Output.object({ schema: JioExperienceIR })`. Implement per-section decomposition (roadmap mandate): generate the section skeleton first, then fill each section's component instances in a bounded call passing the planner's section list + message hierarchy as shared context for global coherence; assemble into one IR. Constrain the model's component `type` choices to the retrieved `queryRegistry()` ids. Implement the in-gen retry loop (D-06) HERE (orchestration, never an AI-SDK callback): on a `parseIR` failure OR a `compile()` `validateAst` failure of the compiled output, append the error to the prompt and re-call, cap at ~3 attempts, then emit a gap. Keep a defensive `parseIR` guard before returning `ok: true`. Export `generateIR` from index.ts. Add `irGenerator.test.ts` covering the five behaviors above using `testModelMock` (credential-free).

    STUB-THEN-WIRE (Task 2 ↔ Task 3 ordering): `irGenerator` calls `compile()` (Task 3) for the compiled-AST retry check, while `compile()` is independent of `irGenerator`. Because this plan's tasks may execute in order, implement Task 2 against a thin local seam: import `compile` from `./compiler` and, if Task 3 has not yet landed, create a minimal placeholder `compiler.ts` exporting a `compile(ir, ctx)` signature stub that Task 3 then fully implements (do NOT duplicate compile logic in irGenerator). The retry loop references `compile()` through this import so wiring is a no-op once Task 3 replaces the stub. The irGenerator unit test mocks `compile` (inject the validateAst result) so Task 2 is independently testable before Task 3 is complete.
  </action>
  <verify>
    <automated>cd packages/experience-builder-agents && pnpm test irGenerator 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/experience-builder-agents test irGenerator` passes all five behaviors.
    - The retry test asserts the model mock was called exactly the expected number of times (fail-then-succeed = 2; cap-reached = ~3) — proving the loop and the cap.
    - The unregistered-component test asserts a `componentGap` result, NOT a thrown error and NOT a compiled artifact.
    - No `ANTHROPIC_API_KEY` is required to run the test (model fully mocked).
  </acceptance_criteria>
  <done>generateIR commits a valid IR via Output.object, decomposes per-section, retries with error feedback under a hard cap, constrains component ids to the registry, and rejects smuggled markup — all proven credential-free.</done>
</task>

<task type="auto">
  <name>Task 3: GEN-06 compiler (IR -> AST -> React + Jio CSS) + credential-free acceptance triad, wired into the workflow</name>
  <files>packages/experience-builder-agents/src/compiler.ts, packages/experience-builder-agents/src/compiler.test.ts, packages/experience-builder-agents/src/workflow.ts, packages/experience-builder-agents/src/index.ts</files>
  <read_first>
    - packages/experience-builder-agents/src/workflow.ts (the `irToArtifactAst` bridge lines 100-114 with `LAYOUT_TYPE_REMAP` Stack->Container lines 54-60 — REUSE it; the `.then(...)` spine lines 275-285; `ORDERED_STEPS` line 301; `createStep` pattern lines 163-200; the `generateStep` lines 202-243 that swaps `mockGenerate` for `generateIR`; the per-step `emit` event idiom lines 149-151)
    - packages/shared/src/codegen/astToReact.ts (`astToReactComponent` lines 132-164; `importSource` defaults to `@oneui/ui` — the approved-import constraint, D-07)
    - packages/experience-builder-core/src/ir/irToAst.ts (`irToAst(ir): ASTRoot` lines 92-122; sections -> 'Stack', root -> 'Container')
    - packages/experience-builder-validation/src/index.ts (`validateAst(ast, context): JioValidationResult` — the D-08 step-2 allowlist check, Pitfall #5)
    - .planning/phases/02-real-source-integration/02-RESEARCH.md (lines 285-298 compile() example; lines 441-444 the acceptance-triad mechanics)
    - .planning/phases/02-real-source-integration/02-PATTERNS.md (lines 153-178 compiler.ts pattern)
  </read_first>
  <action>
    Create `compiler.ts` exporting `compile(ir, ctx)` that: (1) maps `irToAst(ir)` to the ASTRoot; (2) emits the canonical React + Jio CSS code string via `astToReactComponent(ast, { importSource: '@oneui/ui', componentName: 'GeneratedArtifact' })` — this string is the persisted bundle (D-07); (3) runs `validateAst(irToArtifactAst(ir), ctx)` REUSING the existing `irToArtifactAst` bridge from workflow.ts (do NOT reinvent the Stack->Container remap). Return `{ bundle, validation }`. Keep an optional ephemeral `ASTRenderer` path note in comments only — D-07 says the render is internal-convenience, never persisted; do NOT wire it into the bundle. Export `compile` from index.ts.

    Add `compiler.test.ts` proving the D-08 acceptance triad credential-free: (leg 1) write the emitted module to a fixture and assert its `@oneui/ui` imports are present and the module is type-shaped to compile — assert via `pnpm typecheck` pointed at the fixture path (the package typecheck already gates the repo); (leg 2) `validateAst` returns `passed: true` for a compliant fixed-IR fixture and `passed: false` for an IR with a literal/unregistered component (Pitfall #5); (leg 3) `expect(bundle).toMatchSnapshot()` on `astToReactComponent` output for the fixed IR fixture — proving same-IR -> same-string determinism.

    Wire the workflow: in workflow.ts swap `mockGenerate` (line 210) for `generateIR` in the `generate-ir` step, and insert a new `compile` step AFTER `generate-ir` and BEFORE `validate` that calls `compile(ctx.ir, ctx)` and stores `ctx.bundle = result.bundle`. Add the `compile` step to `ORDERED_STEPS` and the `.then(...)` spine. Emit `started`/`completed` `ExperienceBuilderEvent`s for the new `compile` step (the `step` field is open `z.string()` so no schema change). Surface `bundle` on `RunExperienceResult` (add the field) so Plan 04 can persist it. The gap short-circuit (`ctx.halted`) stays the workflow's job (ORCH-04). Remove `mockGeneration.ts`'s use from workflow.ts; keep `irToArtifactAst` exported for the compiler.
  </action>
  <verify>
    <automated>cd packages/experience-builder-agents && pnpm test compiler workflow 2>&1 | tail -25 && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/experience-builder-agents test compiler` passes all three acceptance-triad legs.
    - The snapshot test produces a stable snapshot (re-running yields no diff) — `grep -c "GeneratedArtifact" packages/experience-builder-agents/src/__snapshots__/*.snap` >= 1 and the emitted string imports from `@oneui/ui` (`grep -c "@oneui/ui" <snapshot>` >= 1).
    - The negative validate leg asserts `passed: false` for a non-compliant IR fixture (literal value or unregistered component).
    - `pnpm --filter @oneui/experience-builder-agents test workflow` still passes (the spine now includes `generate-ir` via `generateIR` + the new `compile` step).
    - `pnpm --filter @oneui/experience-builder-agents typecheck` is clean.
    - `RunExperienceResult` carries an optional `bundle` field.
  </acceptance_criteria>
  <done>The compiler emits a canonical @oneui/ui-importing code string proven by tsc + validateAst + a stable snapshot, with NO browser/jsdom render; the workflow spine runs generate-ir (real) -> compile -> validate and surfaces the bundle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output -> IR | Untrusted model completion crosses into the canonical IR; the markup-free Zod schema is the validation gate |
| Resolver input -> foundations | Brand foundation config from Convex crosses into `buildThemeConfig`; only structural data, no executable content |
| modelAdapter -> Anthropic | `ANTHROPIC_API_KEY` confined to this server-side seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | irGenerator.ts (LLM smuggles JSX/HTML via IR string fields) | mitigate | `parseIR` markup-free Zod refinement + `.strict()` reject -> gap, never compile (Pitfall #2); proven by the smuggled-markup test in Task 2 |
| T-02-02 | Tampering | irGenerator.ts (LLM names an unregistered/near-match component) | mitigate | Constrain IR `type` to retrieved `queryRegistry()` ids; `getRegistryItem` exact membership -> componentGap (REG-03); proven by the unregistered-component test |
| T-02-03 | Information Disclosure | modelAdapter.ts (`ANTHROPIC_API_KEY` leaking into IR/bundle/events/logs) | mitigate | Key read server-side only in `callModel`; never logged, never written into IR/bundle/events; bundle is codegen string only |
| T-02-04 | Tampering | foundationResolver.ts (fabricated dimensions for an uncovered profile) | mitigate | Gap arm carries NO dimension numbers (FND-03); proven by the "no numeric dimensions" assertion carried over from P1 |
| T-02-05 | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan (all deps already installed per RESEARCH); single-`ai`-version gate (`pnpm why ai`) re-checked in acceptance |
</threat_model>

<verification>
- `pnpm --filter @oneui/experience-builder-agents test` green (foundationResolver, irGenerator, compiler, workflow).
- `pnpm --filter @oneui/experience-builder-agents typecheck` clean.
- `pnpm why ai` resolves a single `ai` version.
- No `ai`/`@ai-sdk` import outside `modelAdapter.ts`.
</verification>

<success_criteria>
- A real brand + covered web profile resolves real foundations (FND-04) with D-09 partial-brand defaults.
- The IR Generator commits a valid IR via Output.object with per-section decomposition + in-gen retry under a hard cap (GEN-05).
- The compiler emits a canonical @oneui/ui-importing React + Jio CSS string proven by the credential-free triad (GEN-06).
- The workflow spine runs intent -> resolve(real) -> retrieve -> generate-ir(real) -> compile -> validate.
</success_criteria>

<output>
Create `.planning/phases/02-real-source-integration/02-01-SUMMARY.md` when done.
</output>
