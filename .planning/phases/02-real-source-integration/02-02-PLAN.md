---
phase: 02-real-source-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/experience-builder-registry/src/queryRegistry.freshness.test.ts
autonomous: true
requirements: [REG-04]
must_haves:
  truths:
    - "A test independently re-derives the expected registry from the live JIO_WEB_ALPHA_COMPONENTS catalog x *_GENERATED_PROPS meta and asserts deep-equality with queryRegistry() output"
    - "The gate hard-fails on ANY divergence: an added, removed, or changed component id"
    - "The gate hard-fails on ANY per-item metadata divergence: changed props, variants, or slots"
    - "KNOWN_DRIFT_EXCLUSIONS (Modal, Text) are the ONLY tolerated exclusions, read from the exported constant — not re-hardcoded in the test"
  artifacts:
    - path: "packages/experience-builder-registry/src/queryRegistry.freshness.test.ts"
      provides: "REG-04 derive-equals-live CI freshness gate (D-10)"
      contains: "KNOWN_DRIFT_EXCLUSIONS"
  key_links:
    - from: "packages/experience-builder-registry/src/queryRegistry.freshness.test.ts"
      to: "@oneui/ui/registry/jioAlphaCatalog JIO_WEB_ALPHA_COMPONENTS + @oneui/shared/meta/generated"
      via: "independent re-derivation deep-equality assertion"
      pattern: "toEqual"
---

<objective>
Close the registry/metadata drift surface flagged in `.planning/codebase/CONCERNS.md` (Modal/Text meta + catalog-slug drift) with a CI freshness gate. After this plan, any add/remove/change of a Jio component or its metadata that is not reflected in the derived Lab registry hard-fails CI (REG-04, D-10). This is a pure, deterministic, credential-free test — zero new runtime code, zero hand-maintained component list.

Purpose: Guarantee "derive-don't-copy" stays true forever — the Lab registry can never silently drift from the live component-metadata source of truth.
Output: `queryRegistry.freshness.test.ts` asserting derive-equals-live on ids AND per-item props/variants/slots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-real-source-integration/02-CONTEXT.md
@.planning/phases/02-real-source-integration/02-RESEARCH.md
@.planning/phases/02-real-source-integration/02-PATTERNS.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: REG-04 derive-equals-live registry freshness gate (D-10)</name>
  <files>packages/experience-builder-registry/src/queryRegistry.freshness.test.ts</files>
  <read_first>
    - packages/experience-builder-registry/src/queryRegistry.ts (whole module — it derives from `JIO_WEB_ALPHA_COMPONENTS` x `*_GENERATED_PROPS`; `KNOWN_DRIFT_EXCLUSIONS` exported at line ~135)
    - packages/experience-builder-registry/src/queryRegistry.test.ts (the existing "derivation traceability" block lines 118-149 — already asserts count traces to catalog-minus-exclusions and Button variants trace to generated meta; this gate HARDENS that block into a deep-equality hard-fail)
    - .planning/phases/02-real-source-integration/02-RESEARCH.md (lines 261-279 the freshness-gate pattern; lines 446-447 determinism note)
    - .planning/phases/02-real-source-integration/02-PATTERNS.md (lines 214-229 freshness-test pattern)
    - .planning/codebase/CONCERNS.md (the Modal/Text meta + catalog-slug drift this gate must catch going forward)
  </read_first>
  <action>
    Create `queryRegistry.freshness.test.ts` implementing the REG-04 gate per D-10. Independently re-derive the EXPECTED set from the live sources (`JIO_WEB_ALPHA_COMPONENTS` from `@oneui/ui/registry/jioAlphaCatalog`, filtered by the imported `KNOWN_DRIFT_EXCLUSIONS`, plus the `*_GENERATED_PROPS` meta from `@oneui/shared/meta/generated`) — derive it independently of `queryRegistry()` (re-implement the derivation in the test so a bug in `queryRegistry` cannot mask a divergence). Assert `expect(derived.sort()).toEqual(live.sort())` on component ids — hard-fail on any added/removed/changed id. Then add a second assertion block iterating each registered item and deep-equality-asserting its props, variants, and slots against the corresponding `*_GENERATED_PROPS` entry — hard-fail on any metadata divergence. Import `KNOWN_DRIFT_EXCLUSIONS` from `queryRegistry` (do NOT re-hardcode `['Modal','Text']` in the test — read the exported constant so the exclusion list has a single source of truth). Pure test, no network, no credentials.
  </action>
  <verify>
    <automated>cd packages/experience-builder-registry && pnpm test freshness 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @oneui/experience-builder-registry test freshness` passes green against the current live sources.
    - The test imports `KNOWN_DRIFT_EXCLUSIONS` from `./queryRegistry` (`grep -c "KNOWN_DRIFT_EXCLUSIONS" packages/experience-builder-registry/src/queryRegistry.freshness.test.ts` >= 1) and does NOT re-declare `['Modal','Text']` literally.
    - The id assertion uses `expect(...).toEqual(...)` (deep equality hard-fail), not a count-only or subset check.
    - A per-item props/variants/slots deep-equality block exists (`grep -c "variants\|slots\|props" <file>` >= 1).
    - The test requires no `ANTHROPIC_API_KEY` and makes no network call.
  </acceptance_criteria>
  <done>A pure, deterministic freshness gate asserts the derived registry deep-equals the live catalog x meta (minus known-drift) on ids AND per-item metadata, hard-failing on any divergence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live meta -> Lab registry | Component metadata source of truth crosses into the Lab's derived registry; drift here would let an unapproved/changed component slip past REG-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | Registry drift (Lab registry silently diverges from live component metadata; Pitfall #7) | mitigate | Derive-equals-live deep-equality gate hard-fails CI on ANY id or metadata divergence (D-10); the gate re-derives independently so a `queryRegistry` bug cannot mask drift |
| T-02-07 | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; pure test over already-installed sources |
</threat_model>

<verification>
- `pnpm --filter @oneui/experience-builder-registry test freshness` green.
- The gate is independent of `queryRegistry`'s own derivation (re-derived in the test).
- Wiring this gate into `pnpm ci:gates` is handled in Plan 04 (CI orchestration).
</verification>

<success_criteria>
- The Lab registry is provably in sync with the live component-metadata source of truth, and any future drift hard-fails CI (REG-04, D-10).
</success_criteria>

<output>
Create `.planning/phases/02-real-source-integration/02-02-SUMMARY.md` when done.
</output>
