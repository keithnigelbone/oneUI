# Dependencies
node_modules/
yarn.lock
package-lock.json

# Build outputs
dist/
build/
!cdn-release-full-pipeline/build/
.next/
.turbo/
.convex/
storybook-static/
coverage/
*.tsbuildinfo

# Fallow analyzer cache (binary, ~4 MB)
.fallow/

# Generated CSS dumps (pnpm dump:step-css)
temp/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db
.vercel

# Claude Code
.claude/worktrees/
.claude/scheduled_tasks.lock

# Playwright MCP captures
.playwright-mcp/
.playwright-cli/

# Perf harness output — regenerated per run. perf-baseline.json IS committed.
perf-current.json
.env*.local

# CDN PoC artefacts (regenerated by release pipeline + pnpm pack)
cdn-dist/
cdn-dist.zip
packed/

# v3 release pipeline outputs (regenerated by pnpm pack:all)
release/
packages/ui/foundation.css
packages/ui/cdn-bootstrap/jio.ts

# Layers-4 vendored tarballs for perf comparison — needed on disk, not in git
layers-react4-bundle/
layers-react4-bundle.zip

# Perf harness multi-export CSVs (regenerated per run)
oneui-perf-multi-*.csv

android
ios
# Showcase / smoke-test scratch dirs
tmp/
