/**
 * Design-system `Icon` (native) — Figma component parity.
 *
 * For the lower-level resolver (raw `name` / `icon={Component}` with explicit
 * pixel `size` and `color`) import from `@oneui/ui-native/icons` instead.
 */

export {
  Icon,
  default,
  ICON_SIZES,
  type IconProps,
  type DesignIconProps,
  type IconSize,
  type IconAppearance,
  type IconEmphasis,
} from './Icon.native';
