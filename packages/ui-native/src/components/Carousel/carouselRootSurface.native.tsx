/**
 * Carousel-only surface re-anchor — keeps slide button groups on the page-root
 * Primary palette when the carousel sits inside `<Surface mode="bold">`.
 * Not exported from the public theme barrel; Carousel internals only.
 */

export { RootSurfaceProvider as CarouselRootSurfaceProvider } from '../../theme/SurfaceContext';
