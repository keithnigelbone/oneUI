/**
 * Spinner (native) barrel.
 */

export { Spinner } from './Spinner.native';
export type { SpinnerProps, SpinnerSize, SpinnerGeometry } from './interface';
export { useSpinnerGeometry, SPINNER_SVG_STROKE_MAP, SPINNER_VIEWBOX } from './interface';
