module.exports = {
  // react-native-asset uses this to link fonts into android/app/src/main/assets/fonts
  // and to register them in ios/<AppName>/Info.plist.
  // Run `npm run link-assets` once after `react-native init` creates the native dirs.
  assets: ['./assets/fonts'],
};
