/**
 * Logo.recipe.ts
 *
 * Recipe definition for the Logo component.
 * No design decisions — the SVG content controls its own shape and colors.
 * The recipe is kept for registry consistency (empty decisions array).
 */

import type { ComponentRecipeDefinition } from '@oneui/shared';

export const LOGO_RECIPE_DEFINITION: ComponentRecipeDefinition = {
  componentName: 'Logo',
  decisions: [],
  resolutionMap: {},
};
