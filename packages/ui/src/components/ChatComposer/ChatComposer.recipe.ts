/**
 * ChatComposer.recipe.ts
 *
 * ChatComposer has no brand-level design decisions today. The empty recipe
 * keeps support tooling aligned while component styling remains token-driven.
 */

import type { ComponentRecipeDefinition } from '@oneui/shared';

export const CHAT_COMPOSER_RECIPE_DEFINITION: ComponentRecipeDefinition = {
  componentName: 'ChatComposer',
  decisions: [],
  resolutionMap: {},
};
