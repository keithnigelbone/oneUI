/**
 * CheckboxField.recipe.ts
 *
 * No mid-tier recipe decisions yet — layout is driven by the token manifest +
 * inner `Checkbox` recipe. Empty recipe keeps Convex / editor wiring consistent
 * with other field components.
 */

import type { ComponentRecipeDefinition } from '@oneui/shared';

export const CHECKBOX_FIELD_RECIPE_DEFINITION: ComponentRecipeDefinition = {
  componentName: 'CheckboxField',
  decisions: [],
  resolutionMap: {},
};
