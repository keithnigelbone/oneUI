/**
 * Divider.recipe.ts
 *
 * Divider has no brand-level design decisions. Empty recipe present so
 * support tooling can treat it consistently with other shipped components.
 */

import type { ComponentRecipeDefinition } from '@oneui/shared';

export const DIVIDER_RECIPE_DEFINITION: ComponentRecipeDefinition = {
  componentName: 'Divider',
  decisions: [],
  resolutionMap: {},
};
