/**
 * Shared subpath export for @oneui/ui/components/Modal/shared
 * No CSS/DOM imports — safe for React Native usage.
 */
export { useModalState } from './Modal.shared';
export type {
  ModalProps,
  ModalSize,
  ModalCloseReason,
  ModalOpenChangeDetails,
  DividerVisibility,
  DividerScrollPosition,
  FooterOrientation,
  HeaderAlign,
} from './Modal.shared';
