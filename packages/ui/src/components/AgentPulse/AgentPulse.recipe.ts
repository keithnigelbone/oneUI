/**
 * AgentPulse.recipe.ts
 *
 * AgentPulse has no brand-level design decisions — appearance and state
 * are picked per usage. Empty recipe present so the support-matrix gate
 * is satisfied alongside the other component roles.
 */

import type { ComponentRecipeDefinition } from '@oneui/shared';

export const AGENT_PULSE_RECIPE_DEFINITION: ComponentRecipeDefinition = {
  componentName: 'AgentPulse',
  decisions: [],
  resolutionMap: {},
};
