/**
 * Spinner.recipe.ts
 *
 * Spinner has no brand-level design decisions. Empty recipe present so
 * support tooling can treat it consistently with other shipped components.
 */

import type { ComponentRecipeDefinition } from '@oneui/shared';

export const SPINNER_RECIPE_DEFINITION: ComponentRecipeDefinition = {
  componentName: 'Spinner',
  decisions: [],
  resolutionMap: {},
};
