/**
 * Shared documentation showcase primitives.
 *
 * These are consumed by BOTH Storybook stories and the platform component docs
 * so the two render identically. Keep them free of Storybook and platform (app)
 * imports.
 */

export {
  SurfaceLadder,
  SURFACE_LADDER_MODES,
  type SurfaceLadderProps,
} from './SurfaceLadder';
