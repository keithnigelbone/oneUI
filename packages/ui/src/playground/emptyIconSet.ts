/**
 * Empty icon module used only by the playground bundle.
 *
 * The playground Icon implementation falls non-Lucide sets back to Lucide.
 * This keeps legacy async icon imports from resolving deprecated packages that
 * are not needed in the generated preview runtime.
 */
export {};
