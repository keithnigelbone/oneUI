; Expo's Metro tooling reaches into deep package internals (e.g. metro-cache's
; ./src/stores/FileStore) that pnpm's strict `exports` enforcement blocks.
; Use a hoisted linker for this app so the install layout matches what Expo
; expects — same as plain npm/yarn. The rest of the monorepo is unaffected.
node-linker=hoisted
