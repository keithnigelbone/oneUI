# Expo / native build output
.expo/
android/
ios/
dist/
web-build/

# NOTE: .rnstorybook/storybook.requires.ts is committed so the app typechecks
# and builds immediately after `pnpm install`. It is auto-regenerated by the
# Metro integration on `expo start`, or manually via `pnpm storybook-generate`.
