# Expo CLI local cache (per-machine; do not commit)
.expo/
.expo-shared/

# Expo build artefacts
dist/
web-build/

# Native build outputs
android/
ios/

# Local TypeScript build cache
*.tsbuildinfo

# Local env (if added later)
.env
.env.local

# Exported brand snapshots (regenerate with `pnpm --filter @oneui/native-sample export:brands`)
/brand-data/
/brand-data-old/
