/**
 * Re-exports for backward compatibility — prefer `./badge/badge-qa-support`.
 */
export {
  BadgeTags,
  BADGE_TAG_SET,
  BADGE_SHOWCASE_AXE_SCOPE,
  openBadgeTestScenarios,
  expectBadgeVisible,
  expectSectionVisible,
  qaLog,
  qaStep,
  runAxeScan,
  WCAG_AA_TAGS,
  writeAxeArtifact,
  writeAxeHtmlReport,
} from './badge/badge-qa-support';
