/**
 * Full Cartesian matrices in {@link ./DividerFigmaValidationShowcase.tsx}:
 * 3 attention × 3 contentAlign × 2 roundCaps × 3 size × 3 content = 162 per orientation.
 * Kept in a plain `.ts` file so Playwright specs can import without pulling CSS Modules.
 */
export const HORIZONTAL_FULL_MATRIX_COUNT = 162;
export const VERTICAL_FULL_MATRIX_COUNT = 162;
