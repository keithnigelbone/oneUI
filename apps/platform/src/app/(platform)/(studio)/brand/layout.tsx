/**
 * brand/layout.tsx
 *
 * Brand Configuration section layout
 * Handles routing between: overview, tokens, sync, export
 */

export default function BrandLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
