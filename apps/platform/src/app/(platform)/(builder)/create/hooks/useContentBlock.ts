/**
 * Placeholder hook for future Content Block editor integration.
 * ProjectWorkspace currently applies `contentBlockData` from AI tools via Convex.
 */
export function useContentBlock() {
  return { ready: true as const };
}
