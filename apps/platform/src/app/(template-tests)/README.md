# Template Tests

Disposable routes for evaluating how well OneUI components and foundation tokens compose into product-grade vertical templates.

Current routes:

- `/template-tests/marketing` - telecom-style marketing website inspired by jio.com patterns.

Delete this entire `(template-tests)` route group to remove the experiment.
